# 060task 03：理想报告结构与统一渲染整改方案

> 日期：2026-08-12
> 状态：待确认目标方案
> 目标：先确认理想报告长相和格式，再决定代码和模板怎么改

## 一、推荐的统一报告结构

不同功能保留自己的专业章节，但统一外层结构：

### 1. 报告基本信息

- 功能名称；
- 企业和任务；
- 法域；
- 生成时间和版本；
- 输入文件及附件角色；
- 报告状态。

### 2. 执行结论

报告开头直接回答：

- 当前能否继续；
- 风险等级；
- 适用路径；
- 是否被材料门禁阻断；
- 缺少什么；
- 满足哪些条件后可以重新执行。

示例：

```text
当前结论：暂缓执行
风险等级：高
阻断原因：缺少签署版标准合同和第三国法律分析
下一步：补齐材料后重新运行
```

### 3. 输入事实

事实单独成表，不混入判断：

- 出口方；
- 接收方；
- 国家/地区；
- 角色关系；
- 数据类别；
- 数据规模；
- 处理目的；
- 传输频率；
- 文件和附件角色。

### 4. 路径和适用性判断

说明：

- 为什么进入当前功能；
- 适用哪条路径；
- 选择条件是否满足；
- 其他路径为什么不适用；
- 还有哪些前置条件。

### 5. 核心问题清单

每个问题只建立一条结构化主记录：

| 编号 | 问题 | 事实依据 | 法律依据 | 风险 | 状态 |
|---|---|---|---|---|---|

### 6. 模块专属分析

只放该功能独有的判断：

- SCC：模块、条款、Annex、TIA、批注建议；
- TIA：第三国法律、SCC 有效性、补充措施、残余风险；
- PIPIA：数据范围、合法性、告知同意、接收方、备案材料；
- DPIA：必要性、比例性、自动化决策、风险矩阵；
- CPRA：消费者权利、出售/共享、敏感信息、退出机制。

### 7. 整改清单

每项包括：

- 问题编号；
- 整改动作；
- 责任角色；
- 需要提交的证据；
- 完成判断方式；
- 复审时间。

### 8. 证据和引用

明确区分：

- 用户声明；
- 上传文件证据；
- 系统解析结果；
- 确定性规则；
- 法律检索结果；
- 模型解释。

### 9. 附录

- 结构化事实 JSON；
- findings/decision JSON；
- 文件清单和哈希；
- 引用 map；
- Trace 摘要；
- 产物清单。

## 二、推荐的格式关系

推荐采用“一份结构化报告，多种渲染格式”：

```text
Report IR / report.json
├── Markdown
├── DOCX
├── PDF
└── 前端报告展示
```

推荐职责：

- JSON/Report IR：唯一机器事实和结论来源；
- Markdown：人工审核主版本；
- DOCX：正式交付版本；
- PDF：从同一 IR 渲染，不从另一份 DOCX 重新生成；
- 前端：读取同一结构化报告，不重新拼接业务结论。

## 三、生成职责建议

### 确定性代码负责

- Schema 校验；
- 事实归一化；
- 路径选择；
- 风险等级；
- finding；
- 材料缺口；
- decision；
- 引用绑定；
- 是否允许继续。

### 模型负责

- 解释规则结果；
- 把事实组织成可读文字；
- 生成模块专属分析；
- 提出基于已有 finding 的整改说明。

模型不应负责：

- 自己创建新的风险等级；
- 自己决定是否可以继续；
- 自己补造缺失事实；
- 自己生成另一套 findings；
- 改变确定性规则的结论。

## 四、重复控制规则

1. 一个 finding 只有一个完整主展示位置。
2. 摘要只能引用 finding 编号和短句。
3. 详细分析不能重新创建同义 finding。
4. 模板不能再写一遍模型已经生成的完整结论。
5. 所有章节使用同一个 `decision` 和风险等级。
6. 同一法规引用只绑定一个结构化引用身份。
7. Markdown、DOCX、PDF 必须使用同一章节和表格数据。

## 五、建议的实施顺序

1. 先完成任务 01：逐模块现状与来源对照。
2. 再完成任务 02：对已有产物做重复和矛盾审计。
3. 由用户确认报告开头、章节顺序、表格样式和格式关系。
4. 先选一个模块做 Report IR 试点，建议从 TIA 或 SCC 开始。
5. 用同一 IR 输出 Markdown/DOCX/PDF 并做视觉验收。
6. 再把公共能力推广到其他模块。
7. 最后删除重复模板映射和旧渲染路径。

## 六、需要用户确认的内容

请确认以下默认提案是否作为后续标准：

- 是否采用“结论先行”；
- 是否要求事实和判断严格分离；
- 是否要求一个 finding 只保留一个完整主展示位置；
- 是否采用 Report IR 作为 JSON/Markdown/DOCX/PDF 的唯一来源；
- 是否允许不同模块拥有不同专属章节；
- 是否把“用户声明”和“文件已验证”分开显示；
- 是否把“草案条件性结论”和“当前证据门禁结论”同时保留。

在这些内容确认前，不建议直接大规模修改模板或删除旧生成路径。
