# task071 — 前端开发期入口去重治理实施方案（可核验·无遗漏版）

> **日期**: 2026-08-15
> **关联 issue**: `status/issue/issue071_us14117测试案例按钮重复_20260815.md`
> **严重程度**: P2（开发期入口重复）
> **影响范围**: 仅开发者模式（`VITE_ENABLE_DEV_ACCEL=true`，即 `DEV_ACCEL_ENABLED=true`）下的前端 Wizard 入口。关闭开发者模式时 A/B/C 三类入口全部渲染为 `null`，生产环境不出现、不受影响。
> **涉及文件**: 仅 `frontend/src/components/workspace/ModuleRunPanel.tsx`（单文件改动）。

---

## 一、背景与根因

前端各 Wizard 在开发者模式下并存 **三类开发期入口**，且部分入口**语义重复**：

| 类别 | 位置 | 行为 | 数据来源 | 处置 |
|---|---|---|---|---|
| **A 类** | 顶部「🧪 测试案例」（11 处） | 打开案例选择器 → 回填表单 + 回填附件 → **手动再运行** | `dev-test-cases.ts`（`tc.formDefaults` + `tc.backendFilePaths`） | ✅ **保留** |
| **B 类** | 底部「一键体验/一键运行」（10 处） | 注入预设表单 + 预设附件 → **直接调后端执行**（跳过人工确认） | `dev-presets.ts`（`preset.formDefaults` + `preset.backendFilePaths`） | ❌ 删除 |
| **C 类** | us_14117 底部额外的「🧪 测试案例」（1 处） | 与 A 类**完全相同**，仅因放在底部运行条而多出 | `dev-test-cases.ts` | ❌ 删除 |

**根因**：A 类（顶部）与 C 类（us_14117 底部）是同一入口的重复渲染；B 类（底部「一键」）与「顶部选案例回填后再运行」的完整流程能力重叠，且 B 类「跳过确认直接跑」与 Wizard 的「人工确认」交互语义冲突。

---

## 二、数据流与依赖关系（本次改动的边界依据）

> 这是「没有任何漏洞」的关键：先厘清谁依赖谁，才能只删该删的、不误删不该删的。

### 2.1 三处数据源

1. `frontend/src/lib/dev-presets.ts`：10 个预设，每个含 `formDefaults` + `backendFilePaths`；导出 `DEV_ACCEL_ENABLED`、`getModuleDevPreset`、`getAssessmentDevPreset`。
2. `frontend/src/lib/dev-test-cases.ts`：28 个测试案例（`name` / `description` / `formDefaults` / `backendFilePaths`）；导出 `getTestCases`、`getDefaultTestCase`。
3. `benchmarks/cases/**/scenario.json`：24 个场景 JSON（+4 个内联），由 `getTestCases` 合并进案例列表。

### 2.2 `dev-presets.ts` 的全部消费方（**必须保留，不能删**）

| # | 消费方 | 用途 |
|---|---|---|
| 1 | `features/module-runner/model.ts:1217/1290/1363/1397/1501/1527/1570/1602`（`createDefaultXxxValues`） | 开发者模式下，**Wizard 表单初始值**注入 `preset.formDefaults`（挂载即生效） |
| 2 | `ModuleRunPanel.tsx:134-179`（`useState` 初始值） | `xxxDevFilePaths` 初始化为 `preset.backendFilePaths`，作为**正式运行路径的默认输入附件** |
| 3 | `ModuleRunPanel.tsx:238-293`（`useEffect` 回填） | 同上，重新同步 dev 附件路径 |
| 4 | `ModuleRunPanel.tsx:957-1090`（`runXxxDevPreset`） | B 类「一键」注入 + 直跑（本次删除） |
| 5 | `frontend/src/lib/dev-presets.test.ts`、`features/module-runner/payload-builders/payload-builders.test.ts` | 单元测试 |

> 结论：删除 B 类只让消费方 4 失效；消费方 1/2/3/5 仍活跃。因此 **`dev-presets.ts` 整体保留**，`ModuleRunPanel.tsx:14` 的 `DEV_ACCEL_ENABLED, getAssessmentDevPreset, getModuleDevPreset` import **保留**，`model.ts` / `payload-builders` **不改动**。

### 2.3 `dev-test-cases.ts` 的消费方（A 类依赖，保留）

| # | 消费方 | 用途 |
|---|---|---|
| 1 | `ModuleRunPanel.tsx:320`（`getTestCases(moduleKey)` → `devTestCases`） | A 类顶部按钮是否渲染、案例选择器内容 |
| 2 | `ModuleRunPanel.tsx:322-384`（`selectDevCase`） | 回填 `tc.formDefaults` + `tc.backendFilePaths` |

> 说明：`selectDevCase` **同时使用** `tc.formDefaults`（`:324-325`）与 `tc.backendFilePaths`（`:326-327`），即 A 类顶部「选案例」已具备「回填表单 + 回填附件」的完整能力，删除 B 类不会造成能力缺口。

---

## 三、目标

统一前端开发期快速测试入口为 **顶部「🧪 测试案例」（A 类）**，删除底部所有开发期入口（B 类 10 处 + C 类 1 处）。底部 `schema-actions-row` 最终只保留「上一步 / 下一步 / 正式运行主按钮」。

---

## 四、实施步骤（原子操作，避免中间态编译失败）

> 重要：`frontend/tsconfig.app.json` 开启 `noUnusedLocals: true` + `noUnusedParameters: true`。若先删按钮、后删函数，中间态会出现 10 个 unused `runXxxDevPreset` 导致 `tsc -b` 失败。因此 **每个模块的按钮与其 `runXxxDevPreset` 函数必须同步删除**，最终一次性通过类型检查。
>
> 行号均为改动前快照；执行时以块内容为锚点，自底向上删除以减少行号漂移。

### Step 1 — 删除 B 类 10 处底部「一键」按钮 + Step 2 同步删除对应函数

每个 B 类按钮均为 `{DEV_ACCEL_ENABLED ? (...)}` 包裹的 11 行块（`{DEV_ACCEL_ENABLED ? (` → `) : null}`），整块删除；其 `onClick={runXxxDevPreset}` 所指向的函数同步整段删除。

| # | 模块 | B 类按钮块起始行 | 文案 | 删除的 `runXxxDevPreset` 函数（行区间） |
|---|---|---|---|---|
| 1 | `cpra` | `:3119`（→`:3129`） | 一键运行 | `runCpraDevPreset` `:1071-1090` |
| 2 | `us_14117` | `:2927`（→`:2937`） | 一键运行 | `runUs14117DevPreset` `:1059-1069` |
| 3 | `tia` | `:2653` | 一键体验TIA | `runTiaDevPreset` `:1020-1030` |
| 4 | `dpia` | `:2518` | 一键体验DPIA | `runDpiaDevPreset` `:1008-1018` |
| 5 | `bcr` | `:2383` | 一键体验BCR | `runBcrDevPreset` `:996-1006` |
| 6 | `pipia` | `:2260` | 一键体验PIPIA | `runPipiaDevPreset` `:972-982` |
| 7 | `assessment` | `:2100` | 一键体验主流程 | `runAssessmentDevPreset` `:957-970` |
| 8 | `diagnosis` | `:1926` | 一键体验诊断 | `runDiagnosisDevPreset` `:1032-1043` |
| 9 | `eu_scc` | `:1741` | 一键体验SCC | `runEuSccDevPreset` `:984-994` |
| 10 | `review` | `:1559`（→`:1569`） | 一键体验 | `runDocumentReviewDevPreset` `:1045-1057` |

注意点：
- `review` 的 B 类按钮位于其确认头部 `doc-review-actions-row`（`:1558`）内，其余位于底部 `schema-actions-row`；删除时**不要误删 `onClick={execute}` 的正式主按钮**（如 `review` 的「执行审查」、`us_14117` 的「运行 EO 14117 评估」、`cpra` 的「生成CPRA合规全景报告」）。
- 被删的 10 个函数内部引用的 `buildXxxPayloadFrom`、`setXxxDevFilePaths`、`setXxxStepIndex` 等**均仍被正式运行路径 / `selectDevCase` 使用**，不会连带成死代码，只删函数体本身。

### Step 3 — 删除 C 类 1 处（us_14117 底部「🧪 测试案例」）

删除 `ModuleRunPanel.tsx:2938` 这一行：

```tsx
{DEV_ACCEL_ENABLED && devTestCases.length > 0 ? (<button type="button" className="pill-btn" onClick={() => setShowCasePicker(true)} style={{ marginLeft: 8 }}>🧪 测试案例</button>) : null}
```

`us_14117` 顶部 A 类入口（`:2849`，`style={{ marginBottom: 12 }}`）**保留**。

---

## 五、保留清单（明确不删、不改）

1. `frontend/src/lib/dev-presets.ts` 及其 10 个预设、`PRESET_MAP`、`DEFAULT_ASSESSMENT_PRESET_ID`、`DEV_ACCEL_ENABLED`、`getModuleDevPreset`、`getAssessmentDevPreset`。
2. `frontend/src/lib/dev-test-cases.ts` 及其 `DEV_TEST_CASES`、`getTestCases`、`getDefaultTestCase`。
3. `frontend/src/features/module-runner/model.ts`、`payload-builders/*`、`types.ts`。
4. `ModuleRunPanel.tsx:14` 的 `DEV_ACCEL_ENABLED, getAssessmentDevPreset, getModuleDevPreset` import。
5. `ModuleRunPanel.tsx:134-179 / 238-293` 的 `backendFilePaths` 初始化逻辑。
6. `ModuleRunPanel.tsx:320-384` 的 `devTestCases` / `selectDevCase` / `setShowCasePicker` 状态。
7. A 类 11 处顶部「🧪 测试案例」：`review:1334`、`eu_scc:1623`、`diagnosis:1775`、`assessment:1960`、`pipia:2134`、`bcr:2294`、`dpia:2417`、`tia:2552`、`cn_flow:2687`、`us_14117:2849`、`cpra:2943`。
8. 案例选择器弹窗 `ModuleRunPanel.tsx:3218-3239`（A 类目标交互）。
9. 所有 CSS 类名（`pill-btn` / `schema-actions-row` / `doc-review-actions-row` / `doc-review-head-action`）及对应样式，均被正式按钮共享，无需清理。

---

## 六、验证方案（可核验）

在 `frontend/` 目录执行：

### 6.1 静态核验（删除是否彻底）

```bash
# 1) 10 个 runXxxDevPreset 应零引用（定义+调用全部清除）
grep -n "runAssessmentDevPreset\|runPipiaDevPreset\|runEuSccDevPreset\|runBcrDevPreset\|runDpiaDevPreset\|runTiaDevPreset\|runDiagnosisDevPreset\|runDocumentReviewDevPreset\|runUs14117DevPreset\|runCpraDevPreset" src/components/workspace/ModuleRunPanel.tsx
# 期望：无输出

# 2) 底部「一键体验/一键运行」按钮文案应清零
grep -n "一键体验\|一键运行" src/components/workspace/ModuleRunPanel.tsx
# 期望：无输出

# 3) C 类（us_14117 底部 marginLeft:8 测试案例）应清除
grep -n "marginLeft: 8" src/components/workspace/ModuleRunPanel.tsx
# 期望：无输出

# 4) A 类顶部「测试案例」应仍存在（保留 11 处 marginBottom:12 + review 头部 1 处）
grep -cn "测试案例" src/components/workspace/ModuleRunPanel.tsx
# 期望：仍包含 11 个 A 类按钮 + 弹窗标题「选择测试案例」等，数量 > 0 且与 A 类一致
```

### 6.2 类型检查（无 unused / 无缺 import）

```bash
cd frontend && npx tsc -b          # 快速类型门禁（tsconfig 已 noEmit）
# 期望：退出码 0，无 noUnusedLocals / noUnusedParameters / 缺 import 报错
```

### 6.3 单元测试回归（证明 dev-presets / payload-builders / model 未被破坏）

```bash
cd frontend && npx vitest run src/lib/dev-presets.test.ts src/features/module-runner/payload-builders/payload-builders.test.ts src/features/module-runner/model.test.ts
# 期望：全部通过
```

### 6.4 E2E 兼容性（推理已确认，不需改动测试）

- 现有 e2e 对「测试案例」的点击均使用 `getByRole("button", { name: /测试案例/ }).first()`（`frontend/tests/e2e/module-main-paths.e2e.ts:115`、`resource-explorer.e2e.ts:66`）。
- `.first()` 命中页面**第一个**「测试案例」按钮 = A 类顶部按钮（DOM 中顶部先于底部）。删除 C 类（us_14117 底部第二个「测试案例」）后，`.first()` 仍命中保留的 A 类，不破坏测试。
- 全仓无任何测试引用「一键体验/一键运行」或 `runXxxDevPreset`，故 B 类删除无测试副作用。
- `playwright.config.ts:36` 以 `VITE_ENABLE_DEV_ACCEL=true` 启动 e2e，A 类按钮在 e2e 中可见，行为不变。

---

## 七、验收标准

1. Step 6.1 的 4 条 grep 结果符合期望（B/C 类清零、A 类保留、`runXxxDevPreset` 零引用）。
2. `npx tsc -b` 通过。
3. `vitest` 三个回归测试文件通过。
4. 开发者模式下：每个 Wizard 顶部仍有「🧪 测试案例」且可打开案例选择器；底部仅剩「上一步 / 下一步 / 正式运行主按钮」，无「一键体验/一键运行」、无 us_14117 底部重复「测试案例」。
5. 关闭开发者模式：行为与现状一致（所有开发期入口隐藏），生产构建 `npm run build` 通过。

---

## 八、回滚方案

单文件改动（仅 `ModuleRunPanel.tsx`）：

```bash
git checkout -- frontend/src/components/workspace/ModuleRunPanel.tsx
```

即可整体回滚。`dev-presets.ts` / `dev-test-cases.ts` / `model.ts` / `payload-builders` / 后端均未改动，无跨层影响。

---

## 九、风险与边界

| 风险 | 等级 | 缓解 |
|---|---|---|
| `review` 的 B 类在确认头部而非底部 `schema-actions-row`，误删正式「执行审查」主按钮 | 低 | 按 11 行块整体删除，锚点 `onClick={runDocumentReviewDevPreset}`；保留 `onClick={execute}` |
| 先删按钮后删函数导致 `noUnusedLocals` 中间态编译失败 | 低 | 按钮与函数同步删除（Step 1/2 合并），最终 `npx tsc -b` 门禁 |
| 行号快照在改动后漂移 | 低 | 以块内容 / 函数名为锚点定位，`tsc -b` 为最终校验 |

**边界（不在本 task 范围）**：
- 不删除 `dev-presets.ts`、`dev-test-cases.ts`、`model.ts`、`payload-builders`（均有其它活跃消费方）。
- 不动后端、不改 `DEV_ACCEL_ENABLED` 开关逻辑、不改案例数据内容。
- 若后续想进一步收敛 `dev-presets.ts`（formDefaults 与 `dev-test-cases.ts` 的 formDefaults 是否重叠），作为**独立后续 task** 评估，不在本次「去重 UI 入口」范围内。

---

## 十、子方案 B：接入 `seed-cases-v1/inputs` 取代现有前端「🧪 测试案例」

> 本子方案与「去重 UI 入口」（第一～九节）是**同一 task 下的两个可独立交付的子任务**：前者删重复入口，本子方案替换案例数据源。两者都只改前端，但本子方案依赖后端侧的数据补齐（Level B）作为前置。

### 10.1 目标

把 `benchmarks/datasets/seed-cases-v1/inputs/`（50 个种子案例的抽取记录）最终接入前端「🧪 测试案例」选择器，**取代当前 `dev-test-cases.ts` 里 28 个 scenario.json / 内联案例**，使开发者模式的测试案例与「黄金标准种子素材」同一源、可溯源。

### 10.2 现状盘点（已核验，硬事实）

| # | 事实 | 位置/证据 |
|---|---|---|
| 1 | 已有 Level A 抽取记录 **50 个** `*.input.json`，内容是**自由文本段落**（`input.paragraphs`），**不是**结构化模块请求 | `benchmarks/datasets/seed-cases-v1/inputs/` |
| 2 | `input.json` 已按 task072 落实 **input-only 契约**：只含 `input.paragraphs` + 身份溯源，**无输出/金标字段**（`output_measure` / `expected_status` / `expected_note` 已移除，**0 gold**） | 任一 `*.input.json` |
| 3 | Level B 适配器（`build_seed_case_requests.py`）已按 **v2.0 映射重算 + task074 忠实化 + 占位符主体名替换测试名**（2026-08-15）：**converted 9 / rejected 29 / pending_correction 12 / gap 0** | `benchmarks/datasets/seed-cases-v1/levelb-disposition.v1.json` |
| 4 | `requests/` 目录现有 **9 个** `*.request.json`（task02_case4 + task04/06/07/08 各 case1/2） | `benchmarks/datasets/seed-cases-v1/requests/` |
| 5 | 适配器**硬策略**：关键事实缺失/含占位符即 `rejected`，**禁止伪造**默认公司名、人数、国家、接收方、附件 | `scripts/build_seed_case_requests.py` 头部 docstring |
| 6 | task4/6/7/8 的任务名已按 **v2.0** 改对（文档审查/BCR/DPIA/TIA）；其 case3/4/5 共 **12 个**仍是 v1.0 旧内容，标 `pending_correction`，Level B 前必须隔离 | `status/issue/issue072`、`scripts/build_seed_case_inputs.py` |
| 7 | 模块身份裁决已按 v2.0 更新，映射如下 | 下表 10.2b |

**10.2b 模块身份裁决（v2.0 更新；task065 T03 旧裁决已被部分证伪）**

| task_no | declared_task_name | 前端模块 key | 后端 module_id | 状态 |
|---|---|---|---|---|
| 1 | 合规路径诊断 | `diagnosis` | `cn.transfer_diagnosis` | partial（可接入） |
| 2 | 安全评估路径 | `assessment` | `cn.security_assessment` | partial |
| 3 | 标准合同路径 | `pipia` | `cn.pipia` | partial |
| 4 | 文档审查 | `review` | `cn.document_review` | partial（case3/4/5 pending_correction） |
| 5 | SCC 审查 | `eu_scc` | `eu.scc_review` | partial |
| 6 | BCR 审查 | `bcr` | `eu.bcr_review` | partial（case3/4/5 pending_correction） |
| 7 | DPIA 草案生成 | `dpia` | `eu.dpia` | partial（case3/4/5 pending_correction） |
| 8 | TIA 草案生成 | `tia` | `eu.tia` | partial（case3/4/5 pending_correction） |
| 9 | 14117 行政令合规 | `us_14117` | `us.eo_14117` | partial |
| 10 | CPRA 合规 | `cpra` | `us.cpra` | partial |

**结论（v2.0 修正后）**：
- 10 个 task 全部有对应产品模块，**gap 归零**。
- 有效种子案例 = **29 个 rejected + 9 个 converted = 38 个有效**（其中 task02_case4 经占位符主体名替换测试名后由 rejected 转 converted）；task4/6/7/8 的 case3/4/5 共 **12 个 pending_correction**（v1.0 旧内容，Level B 前隔离）。
- 前端 11 个模块中仅 **`cn_flow`（us.eo_14117_flow_review，us_14117 的 flow_review 子路径）无独立种子**，其余 10 个模块有种子替代。

**10.2c 命名溯源（v2.0 权威结论：task04/06/07/08 的名字**起错了**，已改回）**

早期判断曾认为 task04/07/08 的名字没有起错（理由是 seed-cases-v1 忠实照抄 `resources/new` 的「种子案例及测试结果」夹）。该判断**已被 v2.0 证伪**：

- 权威来源 `benchmarks/数规通黄金标准与种子案例.zip`（v2.0）给出正确命名：**task4=文档审查、task6=BCR 审查、task7=DPIA 草案生成、task8=TIA 草案生成**。
- `resources/new` 的 v1.0 用了错误的 B 套命名（task4=豁免情形诊断、task6=TIA 审查、task7=GDPR 合规诊断、task8=BD ROD 判断），这是**种子创建者把任务名写错**，不是「天然无种子」。
- 已用 v2.0 内容覆盖 task4/6/7/8 的 case1/2（8 个 docx），并更新 `manifest.json` 哈希、`TASKS` 映射、`PENDING_CORRECTION_CASES` 隔离（12 个 case3/4/5 待修正）。详见 `status/issue/issue072`。

结论：review/bcr/dpia 三模块**不是「内容缺失」而是「旧命名错配」**，改名+覆盖后已回收；cn_flow 仍是 us_14117 的 flow_review 子路径，黄金标准种子无独立案例，维持「无种子」。

### 10.3 核心结论（为什么不能直接「改一行 import 就取代」）

1. `input.json` 是**自由文本抽取记录**，前端 Wizard 需要的是 `formDefaults`（表单字段值）+ `backendFilePaths`（附件路径），二者之间缺两层转换：
   - **Level B**：抽取记录 → 模块请求 schema（`requests/<case_id>.request.json`）——task074 落实后 **9 个成功**（task02_case4 + task04/06/07/08 各 case1/2），已按 task074 忠实化（DPIA 忠实提取 / TIA 走 `structured_input` / task04·06 附件派生），不再伪完整。
   - **Level C**：模块请求 → 前端 `DevTestCase`（`formDefaults` + `backendFilePaths`）——**尚不存在**，需新建。
2. Level B 的 `rejected`（task074 落实后 30 个）**并非全部只能人工补**：其中代码可消除项（解析 bug `_parse_count`/`_parse_ynu`、可派生附件 task04/06、TIA 走错路径、DPIA 伪完整反向 false）**已由 task074 消除**，从 36 降到 30；再经**占位符主体名替换测试名**（task02/03 的公司名/接收方名，仅替换不影响业务的标识名，业务事实不碰），task02_case4 转 converted，`rejected` 降至 **29**。剩余 29 个才是真正需数据 owner 补的「原文没写的结构化字段」；gold 签署属验收阶段，不阻塞 Level B。适配器始终**禁止伪造**默认值。
3. task4/6/7/8 的 case3/4/5 共 12 个仍是 v1.0 旧内容（`pending_correction`），在改回 v2.0 前不能进入 Level B；这不是「无产品能力」，而是「旧内容待修正」。
4. 种子案例 **无 expected.json（0 gold）**，替换后前端案例「能跑、不能对答案」，原有 `benchmarks/cases/**/expected.json` 的验收 oracle 对种子案例不适用。

### 10.4 决策点（须先拍板，否则方案不可落地）

| 决策 | 选项 | 建议 |
|---|---|---|
| D1 覆盖策略 | (a) 10 个有种子模块用种子案例替换（其中 task4/6/7/8 仅 case1/2 有效，case3/4/5 待修正），仅 `cn_flow` **保留旧 scenario.json 案例**（混合）；(b) 未覆盖模块**删除**前端案例（覆盖收缩） | **选 (a)**：不回退能力 |
| D2 rejected 缺失事实的补齐责任 | (a) 先跑 task074 代码可消除项，占位符主体名可替换测试名（不影响业务的标识名），剩余原文没写字段由数据 owner authoring，gold 签署归法域专家；(b) 代码放宽为「允许默认值」 | **选 (a)**：遵守 task065 §T03「禁止伪造」红线，但不再把 29 个全部归为专家（task074 已消除 6 个代码项；占位符主体名已替换测试名，再转 1 个 converted） |
| D3 无 gold 的验收口径 | (a) 前端仅验「能回填、能发起请求、请求 schema 通过」，不对答案；(b) 暂缓到 expected.json 签署后再接前端 | **选 (a)**，同时保留 0 gold「未签署」标记（seed-cases-v1 无 expected.json） |
| D4 数据落地形态 | (a) 先生成 `requests/*.request.json` + `frontend-cases/*.case.json`，前端**静态 import**；(b) 前端运行时 fetch JSON | **选 (a)**：与现有 scenario.json import 机制一致、可类型检查 |

### 10.5 分阶段实施步骤

#### Phase 1 — 数据补齐（Level B 按 v2.0 重算 + pending_correction 隔离 + rejected → converted）

> 前置：task072 已把 `TASKS` 映射改为 v2.0（task4=review、task6=bcr、task7=dpia、task8=tia）。

**已完成（2026-08-15，代码部分）**：

1. ✅ 重跑 `python3 scripts/build_seed_case_requests.py`，按 v2.0 映射 + task074 忠实化 + 占位符主体名替换测试名 重算 `blocking_missing_facts`：`converted 9 / rejected 29 / pending_correction 12 / gap 0`。
2. ✅ 隔离 12 个 `pending_correction`（task4/6/7/8 case3/4/5），不进入 request 路径。
3. ✅ 新增 `cn.document_review` / `eu.bcr_review` / `eu.dpia` 三个 adapter + schema loader；task074 落实后 `requests/` 现有 9 个（task02_case4 + task04/06/07/08 case1/2），已忠实转换（非伪完整）。
4. ✅ harness 测试同步 v2.0 + task074 忠实性测试（`test_seed_case_inputs.py` + `test_seed_case_requests.py`，94 passed）。

**待做（人类 authoring，禁止伪造）**：

5. task074 代码可消除项（Phase A 解析 bug；Phase B DPIA 忠实提取 / TIA 走 `structured_input` / task04·06 附件派生）**已落实**，重跑后得到**真实最小 rejected 集 = 30**；再对 task02/03 的**占位符主体名替换测试名**（仅替换不影响业务的标识名，业务事实不碰），task02_case4 转 converted，**真实最小 rejected 集 = 29**；剩余缺项由数据 owner authoring（仅剩「原文没写字段」）。
6. 补齐后重跑，验收：
   - `ls benchmarks/datasets/seed-cases-v1/requests/*.request.json | wc -l` == **38**（12 个 pending 隔离、cn_flow 无种子）。
   - `levelb-disposition.v1.json` 中 `converted + rejected + gap + pending_correction` 之和 == 50，且 `pending_correction == 12`。

**10.5a 各模块 blocking_missing_facts 汇总（task074 落实后现状，2026-08-15）**

> 下表是 task074（解析 bug + DPIA 忠实化 + TIA structured_input + task04/06 附件派生）落实后的现状；task4/6/7/8 已按 v2.0 新模块计算缺失事实。12 个 pending_correction（task4/6/7/8 case3/4/5）不在此表，单独隔离。

| 模块 | 缺失关键事实（task074 落实后实际，逐 case 不完全相同） | task074 结果 |
|---|---|---|
| `diagnosis`（task01） | `company_name`（全部）；`answers.q4_spi_count`（case1-4，原文「≥1万/大概」模糊） | case5 的 `q4_spi_count` 已消除（解析 bug） |
| `assessment`（task02） | `pii_count`（case1/2/3/5；case4 已 converted） | 占位符公司名已替换测试名 |
| `pipia`（task03） | `attachments`、`company_profile.company_uscc`（case4 另 `transfer_context.recipient_country_region`） | 占位符公司名/接收方名已替换测试名 |
| `review`（task04） | 无（case1/2 已 converted；case3/4/5 pending） | 附件派生（已消除） |
| `eu_scc`（task05） | `declared_module_type`、`project_name`、`scc_text` | — |
| `bcr`（task06） | 无（case1/2 已 converted；case3/4/5 pending） | 附件派生（已消除） |
| `dpia`（task07） | 无（case1/2 已 converted 且忠实；case3/4/5 pending） | 忠实提取（反向 false 已消除） |
| `tia`（task08） | 无（case1/2 已 converted 走 structured_input；case3/4/5 pending） | 走 `structured_input`（结论字段不再当 input，已消除） |
| `us_14117`（task09） | `project_name`、`recipient_entities` | — |
| `cpra`（task10） | `attachments`、`business_model`、`consumer_rights_process`、`data_lifecycle`、`notice_and_consent`、`opt_out_and_sale_sharing` | — |

#### Phase 2 — 新增 Level C 适配（request → 前端 DevTestCase）

1. 新建 `scripts/build_seed_frontend_cases.py`（或复用现有 `dev-test-cases.ts` 里的 builder），输入 `requests/*.request.json`，输出 `benchmarks/datasets/seed-cases-v1/frontend-cases/<case_id>.case.json`。
2. 复用 `frontend/src/lib/dev-test-cases.ts` 里已存在的 `sharedXxxFormDefaults` / 各模块 builder，把 `request` 映射为 `formDefaults` + `backendFilePaths`（与 scenario.json 案例同构），字段形状如下：
   ```ts
   { caseId, name, description, jurisdiction, formDefaults: Record<string, unknown>, backendFilePaths: string[] }
   ```
3. 后端 `module_id` → 前端 `moduleKey` 映射见 10.2b（如 `cn.transfer_diagnosis` → `diagnosis`）。
4. 验收：`ls benchmarks/datasets/seed-cases-v1/frontend-cases/*.case.json | wc -l` == **38**；每个 case.json 能被 `buildCasePayload(module, case)` 通过（沿用 `dev-test-cases.ts:263` 的构建器）。

#### Phase 3 — 前端接线（取代现有案例）

1. 新建 `frontend/src/lib/dev-seed-cases.ts`：静态 import 38 个 `frontend-cases/*.case.json`，组装 `SEED_DEV_TEST_CASES`（按 `moduleKey` 分组）。
2. 修改 `frontend/src/lib/dev-test-cases.ts` 的 `DEV_TEST_CASES`：10 个有种子模块改用 `SEED_DEV_TEST_CASES`（task4/6/7/8 仅 case1/2 有效）；仅 `cn_flow` 按 **D1=(a)** 保留原 scenario.json 案例。
3. 保持 `getTestCases(moduleKey)`、`selectDevCase`、案例选择器 UI、`buildCasePayload` 不变（只换数据源）。
4. 更新 `config/case_inventory.json` 的 `frontend_cases` 计数与来源标注。
5. 更新 `issue071` 附录 6.2/6.4（案例数、scenario.json 与 seed 映射口径）。

#### Phase 4 — 验证

1. 静态：`grep -n "scenario.json" frontend/src/lib/dev-test-cases.ts` 期望只剩 `cn_flow` 的 import；`grep -c "dev-seed-cases" ...` ≥1。
2. 类型：`cd frontend && npx tsc -b`。
3. 单测：`cd frontend && npx vitest run src/lib/dev-presets.test.ts src/features/module-runner/payload-builders/payload-builders.test.ts src/features/module-runner/model.test.ts`，并新增 `dev-seed-cases` 的契约测试。
4. 后端 harness：`python3 -m pytest backend/tests/harness/test_seed_case_inputs.py backend/tests/harness/test_seed_case_requests.py`（若存在则跑，确保 Level A/B 未回退）。

### 10.6 验收标准

> 当前进度（2026-08-15）：task074 代码项已产出 **9 个 request**（含占位符主体名替换测试名后转出的 task02_case4），剩余 **29 个 rejected** 待数据 owner authoring；下列 38 为补齐后的最终验收口径。

1. `requests/` 有 38 个 `*.request.json`；`frontend-cases/` 有 38 个 `*.case.json`。
2. `levelb-disposition.v1.json`：converted=38（均忠实转换，非伪完整）、rejected=0、gap=0、pending_correction=12（12 个隔离，不产 request）。
3. 前端 10 个有种子模块的案例选择器显示种子案例（来自 seed-cases-v1），仅 `cn_flow` 仍显示原案例（D1=a）。
4. 每个种子案例点击后能回填 `formDefaults` + `backendFilePaths`，并能发起请求（请求 schema 通过）。
5. 无 gold：每个种子案例 UI 需带「未签署/未对答案」标记（0 gold，源自 seed-cases-v1 无 expected.json），不误标为 gold。
6. `npx tsc -b` 通过；相关单测/harness 通过。

### 10.7 回滚

- Phase 3 前端接线仅影响 `dev-test-cases.ts`（+ 新增 `dev-seed-cases.ts`），`git checkout -- frontend/src/lib/dev-test-cases.ts` 即回滚到 28 个旧案例；删除新增 `dev-seed-cases.ts`。
- Phase 1/2 生成的数据文件（`requests/`、`frontend-cases/`）为**纯新增**，不覆盖旧数据，删除即回滚。

### 10.8 风险与边界

| 风险 | 等级 | 缓解 |
|---|---|---|
| rejected 事实补齐周期不可控（真实人工项经 task074 代码消除后为 30） | 高 | Phase 1 已跑 task074 代码项（36→30），剩余按模块分批人工 authoring |
| 12 个 pending_correction（task4/6/7/8 case3/4/5）旧内容待改回 | 高 | Level B 硬隔离，不得进入 request 路径；改回 v2.0 后再评估 |
| 种子案例无 gold，替换后失去 expected oracle | 中 | D3=(a) 前端只验「可跑、schema 通过」，0 gold 标记 |
| `cn_flow` 无独立种子替代 | 中 | D1=(a) 保留旧案例，覆盖不回退 |
| 数据补齐后与前端 builder 字段形状不一致 | 中 | Phase 2 复用 `sharedXxxFormDefaults`，逐模块契约测试兜底 |

**边界**：本子方案**不新建模块、不改 task4/6/7/8 的 case3/4/5 旧内容**（12 个 pending_correction 维持隔离）；**不签发 expected.json**（法律签署另行处理）；**不改后端路由/业务逻辑**（只新增数据 + 前端数据源）。

