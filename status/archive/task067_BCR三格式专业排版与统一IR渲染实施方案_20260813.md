# task067：BCR 网页、DOCX、PDF 专业排版与统一 IR 渲染实施方案

> 日期：2026-08-13  
> 状态：待审核，尚未执行本方案业务代码改动  
> 问题依据：[issue067_BCR报告生成预览与状态口径失真深度分析_20260813.md](./issue067_BCR报告生成预览与状态口径失真深度分析_20260813.md)  
> 试点模块：`bcr`  
> 样本任务：`4ab0776d9ddc49faba4d7bf89f30cd0a`  
> 范围：本地 BCR Report IR、Markdown、网页、DOCX、PDF、artifact preview、自动门禁和真实视觉验收  
> 非范围：法律结论重审、占位符内容治理、KPI/任务状态、task066、远程部署

## 一、目标和完成定义

本方案不是“调整 CSS”或“换一个 DOCX 模板”，而是完成一个可验证的 BCR 垂直切片：

```text
BCR 结构化结果（findings / citations / remediation）
→ 仓库现有 DocumentIR 的版本化扩展
→ DocumentCompiler 结构与排版门禁
→ Web / Markdown / DOCX / PDF 分别消费同一 IR
→ render_manifest 证明同源、完整和可打开
→ 自动结构检查 + 浏览器/DOCX/PDF 真实视觉验收
```

task067 只有同时满足以下条件才算完成：

1. BCR 正式用户产物不再从 Markdown table/list 字符串反推结构；
2. 同一运行只生成一份权威 IR，Web、Markdown、DOCX、PDF 均消费该 IR；
3. 网页报告不存在 DOCX 纯文本冒充忠实预览或同一正文重复展示；
4. DOCX 使用 Word 原生 heading、table、numbering、页眉页脚和分页属性；
5. PDF 不再使用 A4 竖版六列长文本表格，ID、风险等级和英文单词不逐字符断裂；
6. 多级建议条款的编号和从属关系在三种格式中保持一致；
7. `render_manifest.json` 记录同一 IR hash、renderer 版本、文件 hash 和结构计数；
8. 单元、集成、API、前端、浏览器、DOCX XML、PDF 文本层和逐页截图门禁全部通过；
9. BCR 切换后旧模板字符串路径不再生成用户可见产物；
10. 未连接远程、未部署。

## 二、范围边界

### 2.1 本方案纳入

- BCR 的结构化报告 adapter；
- 现有 `backend/common/reporting` 的必要版本升级；
- BCR 专业网页阅读版式；
- BCR Markdown、DOCX、PDF IR renderer；
- 结构、编号、分页和版式 profile；
- artifact API 的受控 IR 读取；
- render manifest 和跨格式一致性门禁；
- BCR 两条共享案例和问题样本的真实本地复验；
- 对复用公共 renderer/API 的模块运行回归，防止公共改动造成倒退。

### 2.2 本方案不纳入

- 判断某条 BCR 法律结论是否正确；
- 修复 `[Company Name]`、`[EU Member State]` 等内容占位符；
- 修复 finding 与建议的业务语义错配；
- 工作台告警数、证据数、风险 KPI；
- RUNNING/COMPLETED 状态口径；
- localStorage 容量治理；
- 一次性迁移全部 11 个模块到完整 Report IR；
- 远程部署和生产数据迁移。

内容问题可以作为排版压力样本，但不能在 task067 中顺手改写法律结论。

## 三、当前基线和环境前置条件

### 3.1 代码基线

| 能力 | 当前事实 | 代码依据 |
|---|---|---|
| Report IR | 已有 `DocumentIR/SectionIR` 和 5 类 block | `backend/common/reporting/schema/` |
| Compiler | 已有 ID、引用、去重、层级和残留 marker 门禁 | `backend/common/reporting/compiler/core.py` |
| BCR adapter | 当前只把 `BCRChapter.content` 按空行拆成 Paragraph/Claim | `backend/domains/eu/bcr_review/schema_first.py` |
| BCR 正式渲染 | 仍走 Markdown/DOCX 模板字符串和通用 PDF parser | `backend/domains/eu/bcr_review/service.py:806-830` |
| DOCX 模板 | 11 个段落、0 个表格，除 Title 外几乎全为 Normal | `resources/templates/eu/3.2_bcr_review_template_v0.docx` |
| DOCX renderer | 占位符字符串替换，不创建原生结构 | `backend/common/render/report.py:69-107` |
| PDF renderer | 固定 A4、40pt 边距、逐行解析 Markdown | `backend/common/render/artifacts.py:15-311` |
| Web preview | DOCX 先抽纯文本，再进入 Markdown renderer | `backend/api/v1/endpoints/artifacts.py:109-117`、`WorkspaceShell.tsx:988-1002` |
| IR artifact | 已生成 `document_ir_json`，资源树有意隐藏 | `frontend/src/features/resource-explorer/config.ts:42-57` |
| BCR 浏览器测试 | 已有真实上传、SSE、报告、引用闭环 | `frontend/tests/e2e/bcr-local-firstrun.e2e.ts` |

### 3.2 当前工具事实

本机已存在：

```text
uv
pdftotext
pdfinfo
pdffonts
pdftoppm
unzip / zipinfo
```

本机当前缺失：

```text
libreoffice / soffice
```

当前 BCR PDF 字体状态：

```text
Helvetica    embedded=no
STSong-Light embedded=no
```

仓库当前只有 PDF.js 的 Liberation Sans 字体，没有可复现的中文正文/标题字体资产。

因此有两个不可伪造的前置条件：

1. DOCX 自动视觉转换依赖 LibreOffice；未安装时只能完成 XML/打开性门禁，视觉验收必须标记 `BLOCKED_BY_TOOLING`，不得写通过；
2. PDF 中文字体必须选择可再分发且许可证清晰的字体资产并纳入版本管理或受控构建资源；继续依赖未嵌入的系统 CID 字体不能通过最终门禁。

## 四、关键架构决策

### 4.1 扩展现有 DocumentIR，不新建平行 Report IR

仓库已有：

- `DocumentIR`；
- `SectionIR`；
- `ParagraphBlock`；
- `ClaimBlock`；
- `ListBlock`；
- `TableBlock`；
- `WarningBlock`；
- `DocumentCompiler`。

task067 必须沿用该包，并与 [060task_03](./060task/060task_03_理想报告结构与统一渲染整改方案_20260812.md) 的 v4 契约方向一致。不得新增 `bcr_report_ir.py` 之类的第二套长期真相源。

当前 v3 block 无法表达 finding 主展示、多级法律条款和分页语义，因此需按明确主版本升级补齐 BCR 垂直切片。新增字段必须是完整 v4 契约的组成部分，不得使用 BCR 私有自由字典绕过 Schema。

### 4.2 业务结果和排版结构分离

```text
BCRFinding / BCRResult：业务判断
DocumentIR：跨格式展示语义和引用关系
RenderProfile：字体、页面、间距、表格和分页规则
Renderer：只负责把 IR 放入对应载体
```

Renderer 禁止：

- 重新判断风险；
- 从字符串识别 finding；
- 从 Markdown 猜标题或列表；
- 合并、删除或新增业务 finding；
- 改写 citation；
- 根据版面容量截断正文。

### 4.3 三格式同源，不要求像素相同

必须一致：

- 章节 ID 和顺序；
- finding ID、顺序和风险；
- 条款编号及父子关系；
- citation 集合；
- 正文段落集合；
- IR hash。

允许不同：

- 网页使用折叠、锚点和响应式布局；
- DOCX 使用 Word 原生表格和多级编号；
- PDF 使用摘要表 + finding 纵向分块和固定分页。

### 4.4 IR 是内部渲染数据源，不是普通下载项

`document_ir_json` 继续从用户文件树隐藏，但报告页可以通过已授权 API 按需读取。不能把完整 IR 写入 localStorage。

推荐 API 契约：

```text
ArtifactPreviewResponse
├─ render_mode: html | pdf | text | download | report_ir
├─ content: string                  # 仅文本类
└─ structured_content: object|null  # 仅 report_ir
```

后端必须依据数据库中登记的 `artifact_type=document_ir_json` 决定 `report_ir`，不能仅凭任意 `.json` 文件名自动解析为 IR。

### 4.5 DOCX 点击行为不冒充忠实预览

浏览器没有原生 Word 排版引擎，当前环境也没有 LibreOffice 服务。task067 不引入一个无法保证忠实度的客户端 DOCX 猜测器。

报告页行为应为：

```text
正文 tab：始终渲染 Report IR
PDF tab：使用同 IR 生成的 PDF canvas，作为固定版视觉预览
DOCX artifact：提供下载/打开；说明其正文已在当前页面展示
```

如果以后要求 Word 像素级浏览器预览，应单独建设受控 LibreOffice 转换服务，不属于本任务。

### 4.6 Feature flag 只控制切换，不制造双真相

建议增加独立切换：

```text
BCR_REPORT_IR_RENDERING_ENABLED=false
```

阶段行为：

1. `false`：旧路径仍是用户产物，新路径可在测试/影子目录生成比对；
2. 影子比对通过：本地将 flag 设为 `true`；
3. `true`：用户产物只由 IR renderer 生成，旧 renderer 不再写正式输出路径；
4. 回滚只切回 flag，不删除新 IR 和证据；
5. 完成本地验收后删除 BCR 旧分支，避免永久双路径。

现有 `schema_first_bcr_enabled` 只控制是否生成/编译 IR，语义不等于“用户产物已由 IR 渲染”，不能复用同一个 flag 混淆状态。

## 五、目标 Report IR 排版契约

### 5.1 必须补齐的公共对象

task067 应从完整 v4 方案中落地以下最小完整纵向切片，而不是“最小示例”：

```text
DocumentIR v4
├─ identity
├─ lifecycle
├─ metadata
├─ findings[]
├─ actions[]
├─ citations[] / CitationRegistry
├─ sections[]
├─ blocks[]
├─ render_contract
├─ diagnostics[]
└─ integrity
```

BCR 排版所需的强类型 block 至少包括：

| Block | 用途 | 关键字段 |
|---|---|---|
| `KEY_VALUE` | 报告元数据和评级摘要 | typed items、列数、label/value |
| `FINDING_SUMMARY` | 简短摘要表 | finding_refs、允许列集合 |
| `FINDING_DETAIL` | finding 唯一完整主展示 | finding_ref、字段显示顺序 |
| `FINDING_REFERENCE` | 其他章节引用 | finding_ref，不复制全文 |
| `CLAUSE_GROUP` | 建议条款及多级子条款 | clause nodes、编号风格 |
| `LIST` | 普通列表 | 强类型嵌套 item，不再仅 `list[str]` |
| `PARAGRAPH/CLAIM` | 正文和带引用主张 | text + refs |
| `WARNING` | 边界或降级提示 | severity + refs |
| `PAGE_BREAK` | 明确分页点 | reason |
| `CITATION_NOTE` | 引用附录 | citation_refs |

### 5.2 多级条款结构

必须使用递归结构表达，而不是手写 `1.`、`(a)`：

```text
ClauseNode
├─ node_id
├─ label                 # 可选显示标签
├─ text
├─ numbering_style       # decimal / lower_alpha / lower_roman / none
├─ citation_refs[]
└─ children[]
```

强制约束：

- `node_id` 文档内唯一；
- 最大层级明确限制，例如 4 级；
- label 由 renderer 按 numbering style 计算，不允许在 text 中重复手写；
- children 顺序稳定；
- 三个 renderer 对同一节点产生相同的逻辑编号；
- 未知 numbering style 编译失败，不降级成普通段落。

### 5.3 finding 展示契约

详细 finding 不再用六列表格。目标结构：

```text
Finding header
  编号 | 风险 | 标题
Finding body
  现状
  证据/定位
  法律依据
  整改建议
  建议条款（如有）
```

摘要区域可以使用短表：

```text
编号 | 风险 | 主题 | 状态
```

强制约束：

- 每个 finding 恰好一个 `FINDING_DETAIL`；
- 摘要只引用 finding，不复制长正文；
- 不在一张表中同时放置长“现状/建议/法律依据”；
- finding 的字段缺失时省略对应行，不用空列占位；
- renderer 不改变 finding 顺序。

### 5.4 RenderProfile

新增公共、版本化、可测试的版式 profile，建议位置：

```text
backend/common/reporting/render_profiles/legal_report_a4_v1.py
或 config/report_render_profiles/legal-report-a4-v1.json
```

建议冻结值：

```yaml
profile_id: legal-report-a4-v1
page:
  size: A4
  margin_top_mm: 20
  margin_right_mm: 22
  margin_bottom_mm: 20
  margin_left_mm: 22
typography:
  body_pt: 10.5
  body_leading: 1.45
  heading_1_pt: 16
  heading_2_pt: 13.5
  heading_3_pt: 11.5
  table_pt: 9
paragraph:
  first_line_indent: 0
  space_after_pt: 6
table:
  repeat_header: true
  allow_long_text_columns: false
pagination:
  keep_heading_with_next: true
  keep_finding_header_with_first_field: true
  page_number: true
```

字体资产必须另附：来源、版本、许可证、SHA-256、覆盖字符集和 PDF 嵌入结果。不得只写字体名称而依赖某台机器已安装。

## 六、三种载体的目标版式

### 6.1 网页版

页面结构：

```text
报告标题 + 元数据
综合评级摘要
章节导航 / 风险筛选
finding 摘要
finding 详情列表
整改建议和多级条款
引用附录
导出文件区（MD / DOCX / PDF / ZIP）
```

明确要求：

- 正文内部容器最大可读宽度建议 `880-960px`，居中但不做浮动大卡片；
- finding 是重复工作单元，可使用 1px 分隔和标题行，不嵌套卡片；
- 正文不默认 `text-align: justify`，中英文混排使用自然左对齐；
- 摘要表可以横向滚动，详细 finding 不用六列长表；
- 风险使用图标/文本标签，但不只靠颜色；
- 三级列表有稳定缩进和编号；
- 移动端把摘要表降级为 finding 摘要列表，不缩成不可读小字；
- 报告正文与 artifact 预览只保留一个主展示；
- DOCX 不进入 Markdown renderer。

### 6.2 DOCX 版

页面和样式：

- A4；
- 上下 20mm、左右 22mm；
- 标题、Heading 1/2/3、Body、Caption、Table Header、Finding Title、Risk Label、Citation Note 使用命名 Word style；
- 页眉包含短报告名/草案状态，页脚包含页码；
- 标题与首段 `keep_with_next`；
- finding header 与首个字段保持同页；
- 摘要表使用 Word 原生 table，表头跨页重复；
- 详细 finding 使用段落结构或两列 key/value 表，不使用六列长表；
- 建议条款使用 Word 原生 numbering definitions；
- 不通过空格、Tab 或手写数字模拟缩进；
- 文件必须能被 `python-docx` 重新打开，OOXML ZIP 完整。

旧 `3.2_bcr_review_template_v0.docx` 不再承载正文内容。可保留为迁移参考，或重构为只提供封面/section/style 的母版；正文必须由 IR renderer 创建。

### 6.3 PDF 版

- 直接从 IR 构造 ReportLab flowables，不先生成 Markdown 再解析；
- 使用 `PageTemplate`/页眉页脚/页码；
- 标题、finding header 和首段使用 `KeepWithNext` 或等价策略；
- 摘要表最多 4 个短列；
- 详细 finding 纵向分块；
- 多级条款递归生成嵌套 `ListFlowable` 或显式编号段落；
- 中文字体显式注册并嵌入；
- 长英文禁止逐字符断裂；
- 不以缩小到 7-8pt 的方式保住六列表格；
- PDF 文本可复制、可检索；
- 页面 raster 后无裁切、重叠、空白页和孤立标题。

### 6.4 Markdown 版

Markdown 仍是用户产物之一，但只作为 IR renderer 输出：

- 标题由 SectionIR 生成；
- finding 摘要使用短表；
- finding 详情使用稳定 heading/definition layout；
- ClauseNode 递归输出正确缩进列表；
- renderer 不读取旧模板中的业务正文；
- Markdown 仅作为一种产物，不再作为 DOCX/PDF 的中间真相源。

## 七、实施依赖和并行计划

```text
T00 基线与工具冻结
  ↓
T01 IR v4 排版契约与迁移边界
  ↓
T02 BCR 结构化 adapter + compiler 门禁
  ├──────────────┬──────────────┬──────────────┬──────────────┐
  ↓              ↓              ↓              ↓
T03 Markdown    T04 Web/API    T05 DOCX       T06 PDF
  └──────────────┴──────────────┴──────────────┴──────────────┘
                         ↓
                 T07 render manifest/一致性
                         ↓
                 T08 自动排版门禁
                         ↓
                 T09 影子渲染与本地切换
                         ↓
                 T10 真实视觉和浏览器验收
                         ↓
                 T11 公共回归与旧路径退出
```

并行原则：

- T03/T04/T05/T06 只有在 T01/T02 的 IR 和编号契约冻结后才能并行；
- 四个 renderer 使用同一 fixture 和同一 IR hash；
- 任何 renderer 不得临时新增私有字段；
- T05 DOCX 和 T06 PDF 可并行，但字体/profile 由 T00/T01 单一裁决；
- 最终一致性、真实视觉和切换必须串行收口。

## 八、具体实施任务

### T00：冻结问题样本、工具和字体基线

**目标：** 建立修复前可重复失败信号，避免“看起来更好”但无法比较。

**涉及文件：**

- 新增 `backend/common/reporting/tests/fixtures/bcr_layout_regression/`
- 新增 `scripts/check_report_layout_environment.py`
- 新增 `status/check/task067/baseline/`

**执行：**

1. 保存样本任务对应的 IR、Markdown、DOCX、PDF 和输入 fixture 的 SHA-256；
2. 记录 DOCX 段落、表格、style、section、numbering 定义；
3. 保存 `pdfinfo`、`pdffonts`、`pdftotext -layout` 输出；
4. `pdftoppm` 将旧 PDF 逐页 raster 到 baseline 目录；
5. 固化以下失败模式：`Word tables=0`、Markdown pipe residue、`HIG\nH`、`ME\nDIU\nM`、`BCR\n-C-1\n.1`、编号重置；
6. 盘点 CJK 字体资产、许可证和 hash；
7. 检查 LibreOffice；缺失时写明本机视觉验收阻断，不静默跳过。

**验收：**

- [ ] fixture 与实际样本 hash 可追溯；
- [ ] 至少一个自动检查能在旧产物上稳定失败；
- [ ] 工具缺失返回非零或明确 `BLOCKED_BY_TOOLING`；
- [ ] 不提交用户敏感正文；fixture 必须使用已批准测试材料或脱敏复制；
- [ ] 字体选择有许可证和 hash。

**依赖：** 无。**可并行：** 工具盘点、DOCX 基线、PDF 基线可并行。

### T01：升级现有 DocumentIR 排版契约

**目标：** 让 IR 能明确表达 finding、引用、多级条款和渲染要求，不再依赖 Markdown 猜测。

**涉及文件：**

- `backend/common/reporting/schema/document.py`
- `backend/common/reporting/schema/blocks.py`
- `backend/common/reporting/schema/diagnostics.py`
- 新增或扩展 `backend/common/reporting/schema/findings.py`
- 新增 `backend/common/reporting/schema/rendering.py`
- `backend/common/reporting/__init__.py`
- `backend/common/reporting/tests/test_schema.py`
- `backend/common/reporting/tests/test_compiler.py`
- BCR golden fixture

**实现要求：**

1. 明确 schema 主版本升级和兼容 reader；
2. 加入强类型 finding/action/render contract/integrity；
3. 加入 `FINDING_SUMMARY/FINDING_DETAIL/CLAUSE_GROUP/KEY_VALUE/PAGE_BREAK` 等 block；
4. `ListBlock` 升级为结构化嵌套 item 或由 `ClauseGroup` 独立承担法律编号；
5. 所有类型 `extra=forbid`；
6. renderer 必须穷尽 block 联合类型；
7. v3 adapter 只能作为迁移输入，记录 `migrated_from_schema` 和 diagnostics；
8. 不允许一个长期并存的 BCR 私有 IR。

**编译门禁：**

- finding ID、block ID、clause node ID 唯一；
- 每个 finding 恰好一个主展示；
- finding summary 只能引用存在 finding；
- clause 最大深度、numbering style 和 parent/child 合法；
- section level 不跳级；
- block 文本不含 Markdown 结构标记；
- render contract profile 必须存在；
- 未知 block/profile fatal。

**验收：**

- [ ] v4 fixture 可序列化并通过 compiler；
- [ ] 重复主 finding、断裂引用、非法嵌套、未知 block 的 mutation tests 均失败；
- [ ] 现有 v3 fixture 有明确迁移结果或明确拒绝；
- [ ] `060task_03` 的契约方向未被另一个 schema 破坏。

**依赖：** T00。**可并行：** Schema 模型与 mutation fixture 可并行，最终契约串行冻结。

### T02：重写 BCR adapter，直接从结构化业务结果构建 IR

**目标：** 不再把 `BCRChapter.content` 或 Markdown table 作为 IR 的主要来源。

**涉及文件：**

- `backend/domains/eu/bcr_review/schema_first.py`
- `backend/domains/eu/bcr_review/schema.py`（只增加必要的排版引用字段，不改法律结论）
- `backend/domains/eu/bcr_review/service.py`
- `backend/domains/eu/bcr_review/bcr_report_renderer.py`（降级为 adapter/helper 或退出）
- `backend/domains/eu/bcr_review/tests/test_schema_first_adapter.py`
- `backend/domains/eu/bcr_review/tests/fixtures/bcr_document_ir.golden.json`

**输入：**

- `BCRTypeClassification`；
- `rating` 和 score；
- `BCRFinding[]`；
- missing requirements；
- citation registry；
- review metadata；
- remediation 的结构化 clause 信息。

**IR 章节建议：**

1. 报告摘要；
2. BCR 类型与审查范围；
3. 风险与 finding 摘要；
4. 详细 finding；
5. 整改优先级；
6. 建议条款；
7. 法规与引用；
8. 审查边界。

**实现要求：**

1. 18 条 finding 直接转成 18 个 FindingRecord；
2. 每条 finding 生成一个 `FINDING_DETAIL`，摘要只引用；
3. `suggested_revision` 在进入 adapter 前结构化为 ClauseNode 或明确 Paragraph，不用 regex 在 renderer 中解析；
4. citation 保留 ID，不把 `[1]` 写进 block text；
5. 旧 `chapters` 可从 IR 派生给 API 兼容消费者，但不能反向作为正式 renderer 输入；
6. IR 文件在任何用户格式渲染前通过 compiler；
7. compiler error/fatal 时不得生成“成功”的正式 DOCX/PDF。

**验收：**

- [ ] finding 数和顺序与 BCRResult 完全一致；
- [ ] 每个 finding 主展示次数为 1；
- [ ] IR 中不存在 Markdown table separator；
- [ ] 多级条款有稳定 node ID 和编号关系；
- [ ] 同一固定输入生成 byte-stable canonical IR（时间字段冻结后）；
- [ ] 旧问题样本可稳定生成 v4 IR。

**依赖：** T01。**可并行：** adapter 与 golden fixture 可并行，必须共同收口。

### T03：实现 IR → Markdown renderer

**目标：** 保留 Markdown 用户产物，但停止使用旧 BCR Markdown 模板作为业务正文源。

**涉及文件：**

- 新增 `backend/common/reporting/renderers/markdown.py`
- `backend/domains/eu/bcr_review/service.py`
- `backend/common/reporting/tests/test_markdown_renderer.py`

**实现要求：**

- 按 SectionIR 输出 heading；
- finding summary 只输出短表；
- finding detail 使用字段分块；
- ClauseNode 递归输出正确缩进；
- citation 由 registry 的首次使用编号统一生成；
- 未知 block fatal；
- 输出禁止残留 `{{...}}`；
- renderer 只读 IR。

**验收：**

- [ ] 章节/finding/clause/citation 集合与 IR 相等；
- [ ] 不出现六列长 finding 表；
- [ ] 嵌套列表经 CommonMark parser 后层级正确；
- [ ] Markdown lint 通过；
- [ ] 同一 IR 重复渲染 hash 稳定。

**依赖：** T02。**可并行：** 与 T04-T06 并行。

### T04：实现受控 IR API 和网页专业阅读版式

**目标：** 报告页直接渲染结构化 IR，不再以 DOCX 抽取文本作为主正文。

**涉及文件：**

- `backend/schemas/common.py`
- `backend/api/v1/endpoints/artifacts.py`
- `backend/api/v1/tests/test_artifacts_access.py`
- `frontend/src/api/artifacts.ts`
- `frontend/src/api/generated/openapi.d.ts`
- 新增 `frontend/src/components/report/ReportDocumentView.tsx`
- 新增 `frontend/src/components/report/FindingView.tsx`
- 新增 `frontend/src/components/report/ClauseTree.tsx`
- `frontend/src/components/workspace/WorkspaceShell.tsx`
- `frontend/src/styles/app/workspace.css`（或拆为 report.css，遵循现有样式组织）
- 对应 Vitest 测试

**后端要求：**

1. artifact access 查询返回实际登记记录，而不是只返回 boolean；
2. 只有 `artifact_type=document_ir_json` 且 Pydantic 校验通过时返回 `report_ir`；
3. 普通 JSON 仍走 JSON/text preview；
4. 越权、路径越界、损坏 IR、未知版本有明确状态码；
5. `structured_content` 不与 `content` 混用；
6. 生成 OpenAPI 类型。

**前端要求：**

1. 报告数据源优先级：有效 IR > response 兼容结构 > Markdown artifact；DOCX 不再作为正文 fallback；
2. `document_ir_json` 不显示为普通下载项；
3. 删除同一 artifact 同时进入正文和导出预览的双重链路；
4. `ReportDocumentView` 对 block 联合类型穷尽处理；
5. 正文可读宽度、左对齐、多级编号、finding 分块、移动端降级；
6. PDF 独立 tab 保留；DOCX 只提供打开/下载动作；
7. 不把 IR 放入 AppState/localStorage；组件按需请求并在内存中持有。

**验收：**

- [ ] 所有者可读取 IR，其他用户 403；
- [ ] 任意 JSON 不能冒充 report IR；
- [ ] 损坏 IR 显示可恢复错误，不白屏；
- [ ] 页面只有一个报告正文主展示；
- [ ] DOCX 不进入 Markdown renderer；
- [ ] 1440、1280、390 宽度无页面级横向滚动；
- [ ] 风险信息不只依赖颜色；
- [ ] 长英文和中英文混排不溢出。

**依赖：** T02。**可并行：** API、React renderer、CSS 和 component tests 可并行，契约合并需串行。

### T05：实现 IR → DOCX 原生 renderer

**目标：** 生成可以继续编辑、结构可检验的 Word 报告。

**涉及文件：**

- 新增 `backend/common/reporting/renderers/docx.py`
- 新增 `backend/common/reporting/render_profiles/`
- `backend/domains/eu/bcr_review/service.py`
- `resources/templates/eu/3.2_bcr_review_template_v0.docx`（重构为 style master 或退出正式正文链）
- 新增 DOCX renderer/OOXML tests

**实现要求：**

1. A4 和统一 margin；
2. 创建/校验命名 styles；
3. 使用 heading outline level；
4. 创建报告元数据表和 finding 摘要原生表；
5. finding detail 使用纵向结构，不使用六列长表；
6. ClauseNode 转 Word 原生多级 numbering；
7. table header 设置 repeat；
8. heading/finding title 设置 keep-with-next；
9. 页眉页脚和动态页码；
10. citation note 使用统一 style；
11. 不把 Markdown 语法写入段落；
12. 未知 block/profile 构建失败。

**机器验收：**

- [ ] OOXML ZIP 完整；
- [ ] `python-docx` 可重新打开；
- [ ] Heading 1/2/3 顺序正确；
- [ ] 原生 table 数符合设计；
- [ ] `word/numbering.xml` 存在预期编号定义；
- [ ] `w:keepNext`、页码 field、页眉页脚关系存在；
- [ ] 普通段落不存在 `| --- |`；
- [ ] finding 和 clause 计数与 IR 一致。

**视觉验收：**

```text
DOCX → LibreOffice headless → PDF → pdftoppm PNG
```

若 LibreOffice 缺失，该项不得标 PASS。可以先完成结构门禁，但最终 checkpoint 保持未完成。

**依赖：** T00 字体/工具、T02 IR。**可并行：** 与 T03/T04/T06 并行。

### T06：实现 IR → PDF 固定版 renderer

**目标：** 直接从 IR 生成可交付 PDF，不再经过 Markdown parser。

**涉及文件：**

- 新增 `backend/common/reporting/renderers/pdf.py`
- `backend/common/reporting/render_profiles/`
- 字体资产及许可证清单
- `backend/domains/eu/bcr_review/service.py`
- 新增 PDF renderer/layout tests

**实现要求：**

1. 显式注册并嵌入 CJK/Latin 字体；
2. 使用 `BaseDocTemplate/PageTemplate` 或等价实现页眉页脚和页码；
3. 摘要表最多 4 个短列；
4. finding detail 使用纵向 flowables；
5. ClauseNode 递归渲染多级编号；
6. heading 与首段保持；
7. finding header 与首字段保持；
8. 长 finding 可跨页但不丢身份；
9. citation 附录可复制、可检索；
10. 未知 block/profile fatal；
11. 不调用 `_markdown_to_flowables()` 处理正式 BCR 正文。

**机器验收：**

- [ ] `%PDF`、页数大于 0；
- [ ] `pdffonts` 显示目标字体 `emb=yes`；
- [ ] `pdftotext -layout` 可提取所有 finding ID；
- [ ] 不出现 `HIG\nH`、`ME\nDIU\nM`、`BCR\n-C-1\n.1`；
- [ ] 不残留 Markdown table separator；
- [ ] finding/clause/citation 计数与 IR 一致；
- [ ] raster 每页非空，无内容越过页面边界。

**视觉验收：**

- 标题层级；
- 行宽和行距；
- finding 间距；
- 多级编号；
- 页眉页脚和页码；
- 跨页 finding；
- 无孤立标题、裁切、重叠和空白页。

**依赖：** T00 字体、T02 IR。**可并行：** 与 T03-T05 并行。

### T07：生成 render manifest 和跨格式等价证明

**目标：** 机器证明四种输出来自同一 IR，而不是人工肉眼猜一致。

**新增产物：**

```text
outputs/bcr/<task_id>/outputs/
├─ document_ir.json
├─ report.md
├─ report.docx
├─ report.pdf
├─ citation_map.json
└─ render_manifest.json
```

**manifest 至少记录：**

- schema/IR/compiler/profile/renderer 版本；
- canonical IR SHA-256；
- 每个 artifact path、size、SHA-256、MIME；
- section IDs；
- finding IDs；
- clause node IDs 和逻辑编号；
- citation IDs/数量；
- DOCX 段落、heading、table、numbering 统计；
- PDF page count、font embedding、text extraction 状态；
- 每个 gate 的 status 和 diagnostics。

**一致性规则：**

- 每个格式的 section/finding/clause/citation 集合必须与 IR 相等；
- 顺序必须一致；
- 任何缺段、重复段或编号归属变化为 error；
- renderer 不得修改 IR hash；
- artifact 写完后再原子写 manifest；
- 任一 required artifact 失败则整个 render 状态非 success。

**验收：**

- [ ] 篡改任一产物后 hash 门禁失败；
- [ ] 删除 finding 或改变顺序后等价门禁失败；
- [ ] 同一输入重跑 manifest 的非时间字段稳定；
- [ ] manifest 不含 token、上传正文或敏感配置。

**依赖：** T03-T06。**可并行：** manifest schema 可提前设计，最终计算必须等待所有 renderer。

### T08：建立自动排版回归门禁

**目标：** 把 issue067 的截图问题转成可重复失败的测试，而不是以后继续靠人工发现。

**建议新增：**

- `backend/common/reporting/tests/test_layout_contract.py`
- `backend/common/reporting/tests/test_docx_renderer.py`
- `backend/common/reporting/tests/test_pdf_renderer_ir.py`
- `backend/domains/eu/bcr_review/tests/test_ir_rendering.py`
- `backend/api/v1/tests/test_artifact_report_ir.py`
- `frontend/src/components/report/*.test.tsx`
- `frontend/tests/e2e/bcr-report-layout.e2e.ts`
- `scripts/check_report_artifacts.py`

**必须覆盖：**

1. 18+ finding 压力样本；
2. 中英文长标题；
3. 超长英文单词/法规名；
4. 三级以上条款编号；
5. finding 跨页；
6. 缺可选字段；
7. 空 finding 集合；
8. 未知 block/profile；
9. 损坏 IR；
10. DOCX/PDF/Markdown 集合等价；
11. Web 移动端降级；
12. artifact 越权。

**禁止的弱断言：**

- 只检查文件存在；
- 只检查大于 0 字节；
- 只检查 PDF 以 `%PDF` 开头；
- 只检查 DOCX 是 ZIP；
- 只检查页面没有抛异常；
- 只保存截图但不断言关键 DOM/尺寸。

**验收：**

- [ ] 在旧产物或注入缺陷 fixture 上测试失败；
- [ ] 新 renderer 上测试通过；
- [ ] mutation tests 能拦截编号重置、表格退化、重复正文和异常断词；
- [ ] 测试不依赖真实模型网络调用。

**依赖：** T07。测试设计可与 renderer 并行，最终通过需等待 T03-T07。

### T09：影子渲染、差异审计和本地切换

**目标：** 在不立即替换用户产物的情况下证明新路径结构完整，然后只切换 BCR。

**阶段 A：影子渲染**

- 同一 BCRResult 同时生成 legacy 和 IR 版本到隔离目录；
- 不登记影子文件为用户 artifact；
- 比较 section/finding/citation 集合；
- 内容差异只登记，不在 task067 中改法律结论；
- 版式差异按本方案门禁评价。

**阶段 B：本地切换**

- `BCR_REPORT_IR_RENDERING_ENABLED=true`；
- 正式 output_files 指向 IR renderer 文件；
- legacy 文件不写正式 outputs；
- API、artifact registry、ZIP 都引用新产物；
- `document_ir_json` 和 `render_manifest_json` 登记为内部 artifact。

**回滚：**

- flag 切回 false；
- 不删除已生成 IR/manifest；
- 不修改历史 artifact；
- 保存切换原因和失败证据。

**验收：**

- [ ] 影子运行不改变用户 artifact 数量；
- [ ] 本地切换后所有正式文件来自同一 IR；
- [ ] ZIP 不混入 legacy 与新版本；
- [ ] 回滚不破坏已完成任务；
- [ ] 切换状态在 run/render manifest 可见。

**依赖：** T08。**可并行：** 无，属于切换门禁。

### T10：真实浏览器、DOCX 和 PDF 视觉验收

**目标：** 用真实用户路径证明报告可读，不以单元测试替代视觉结果。

**固定案例：**

- `benchmarks/cases/bcr/bcr_controller_basic/`；
- `benchmarks/cases/bcr/bcr_high_risk_health/`；
- issue067 的 18 finding 排版压力 fixture。

**浏览器 viewport：**

```text
1440×900
1280×800
390×844
```

**浏览器场景：**

1. 打开报告正文；
2. 检查章节导航、finding 摘要和详情；
3. 展开多级建议条款；
4. 切换 PDF tab；
5. 下载 DOCX/PDF；
6. 刷新后重新打开；
7. 检查页面无重复正文；
8. 检查无页面级横向滚动；
9. 检查 DOCX 未被标成 Markdown 忠实预览；
10. 检查控制台和 network。

**DOCX/PDF 视觉：**

```text
DOCX → LibreOffice headless → PDF → pdftoppm PNG
PDF  → pdftoppm PNG
```

逐页检查：

- 标题层级；
- 正文宽度和留白；
- finding 边界；
- 多级编号；
- 跨页关系；
- 表格宽度；
- 页眉页脚；
- 页码；
- 字体；
- 裁切、重叠、空白页和孤立标题。

**证据目录：**

```text
status/check/task067/
├─ environment.json
├─ render_manifest.json
├─ structural_checks.json
├─ web/
│  ├─ 1440_report.png
│  ├─ 1280_report.png
│  ├─ 390_report.png
│  ├─ pdf_tab.png
│  ├─ browser_console.json
│  └─ browser_network.json
├─ docx/
│  ├─ structure.json
│  └─ pages/*.png
└─ pdf/
   ├─ pdfinfo.txt
   ├─ pdffonts.txt
   ├─ text_layout.txt
   └─ pages/*.png
```

**验收：**

- [ ] 三 viewport 通过；
- [ ] 两个共享案例 + 压力 fixture 通过；
- [ ] DOCX 与 PDF 逐页检查完成；
- [ ] 无未捕获前端错误；
- [ ] 截图与 JSON 记录能关联 task ID、IR hash、artifact hash；
- [ ] LibreOffice 缺失时不得关闭 task067。

**依赖：** T09。**可并行：** 三 viewport、DOCX、PDF 可并行执行，结论统一收口。

### T11：公共回归、BCR 旧路径退出和验收归档

**目标：** 确认公共 schema/API/render 改动没有破坏其他模块，并结束 BCR 双路径。

**回归范围：**

- `backend/common/reporting` 全量；
- `backend/common/render` 全量；
- BCR service/router/API；
- artifact access/preview；
- 前端 WorkspaceShell、resource explorer、PdfViewer；
- 所有直接调用被修改公共 API/renderer 的模块；
- 前端 test/build；
- BCR 浏览器 E2E。

**旧路径退出：**

- 删除 BCR 正式路径对 `_build_markdown_table()` 的依赖；
- 删除 BCR 正式路径对 `render_docx_template()` 和 `PdfRenderer.from_template()` 的依赖；
- 旧模板标记 archive/reference 或删除，必须有迁移说明；
- 删除临时影子 flag 分支或明确设定短期移除日期；
- 不修改其他模块仍在使用的公共 legacy 函数，除非其全量回归已覆盖。

**验收：**

- [ ] BCR 用户产物只有一条正式生成路径；
- [ ] 旧 BCR 输出路径扫描为 0；
- [ ] 公共回归全部通过；
- [ ] `git diff --check` 通过；
- [ ] 证据不含 token、`.env` 或真实用户上传正文；
- [ ] 更新 issue067/task067 状态和 `status/check/task067/` 验收报告；
- [ ] 未连接远程、未部署。

**依赖：** T10。**可并行：** 无，必须最后收口。

## 九、验证命令

实际文件名可随实现落地调整，但不得减少验证层级。

### 9.1 后端 Schema、compiler 和 renderer

```bash
uv run --frozen pytest -q \
  backend/common/reporting/tests \
  backend/common/render/tests \
  backend/domains/eu/bcr_review/tests
```

### 9.2 Artifact API

```bash
uv run --frozen pytest -q \
  backend/api/v1/tests/test_artifacts_access.py \
  backend/api/v1/tests/test_me_scope.py
```

### 9.3 排版结构检查

```bash
uv run --frozen python scripts/check_report_layout_environment.py
uv run --frozen python scripts/check_report_artifacts.py \
  --ir outputs/bcr/<task_id>/outputs/document_ir.json \
  --docx outputs/bcr/<task_id>/outputs/report.docx \
  --pdf outputs/bcr/<task_id>/outputs/report.pdf \
  --manifest outputs/bcr/<task_id>/outputs/render_manifest.json
```

### 9.4 PDF 外部工具

```bash
pdfinfo outputs/bcr/<task_id>/outputs/report.pdf
pdffonts outputs/bcr/<task_id>/outputs/report.pdf
pdftotext -layout outputs/bcr/<task_id>/outputs/report.pdf status/check/task067/pdf/text_layout.txt
pdftoppm -png -r 144 outputs/bcr/<task_id>/outputs/report.pdf status/check/task067/pdf/pages/page
```

### 9.5 DOCX 外部工具

```bash
unzip -t outputs/bcr/<task_id>/outputs/report.docx
libreoffice --headless --convert-to pdf \
  --outdir status/check/task067/docx \
  outputs/bcr/<task_id>/outputs/report.docx
pdftoppm -png -r 144 status/check/task067/docx/report.pdf status/check/task067/docx/pages/page
```

当前本机缺少 LibreOffice，该命令是最终环境前置条件，不是已通过事实。

### 9.6 前端

```bash
cd frontend
npm test -- --run
npm run build
npm run test:e2e -- bcr-report-layout.e2e.ts
```

### 9.7 最终检查

```bash
git diff --check
rg -n "_build_markdown_table|render_docx_template|from_template" backend/domains/eu/bcr_review
rg -n "document_ir|report_ir" frontend/src backend/api/v1
```

扫描结果必须由人工区分测试、archive 和正式运行路径，不能仅凭字符串出现就判失败。

## 十、阶段门禁

### Checkpoint A：契约冻结

- [ ] T00-T02 完成；
- [ ] v4 排版契约和 v3 迁移边界清晰；
- [ ] BCR fixture 生成唯一 finding/clause 结构；
- [ ] compiler mutation tests 通过。

Checkpoint A 未通过，禁止并行 renderer 进入正式实现。

### Checkpoint B：四 renderer 完成

- [ ] T03-T06 完成；
- [ ] 四种格式只读取同一 IR；
- [ ] DOCX/PDF 不再解析 Markdown；
- [ ] Web 不再解析 DOCX 文本作为正文；
- [ ] 字体和工具条件明确。

### Checkpoint C：自动门禁和本地切换

- [ ] T07-T09 完成；
- [ ] render manifest 证明同源；
- [ ] 旧故障 fixture 失败、新产物通过；
- [ ] 本地 flag 切换和回滚可验证。

### Checkpoint D：真实视觉与收尾

- [ ] T10-T11 完成；
- [ ] Web/DOCX/PDF 视觉证据齐全；
- [ ] LibreOffice 和字体门禁真实通过；
- [ ] BCR 旧正式路径退出；
- [ ] 公共回归通过；
- [ ] 未远程部署。

## 十一、失败条件和禁止操作

出现以下任一情况，不得把 task067 标记完成：

1. Web、DOCX 或 PDF 仍有一个格式从 Markdown 字符串猜结构；
2. DOCX 仍为 0 原生表格且 detailed findings 仍在单一普通段落；
3. PDF 仍出现 ID/风险逐字符断裂；
4. 多级编号在任一格式重置、丢级或改变从属；
5. 只靠截图人工判断，没有结构/文本层门禁；
6. 只检查文件存在和非零；
7. 使用未嵌入或许可证不明的中文字体，却声称 PDF 可复现；
8. LibreOffice 缺失却声称 DOCX 视觉通过；
9. 为缩短页数将正文或表格缩到不可读字号；
10. 用横向 A4 六列表格掩盖结构问题，而没有 finding 详情版式；
11. 将完整 IR 写入 localStorage；
12. 把内部 `document_ir_json` 暴露为普通用户下载项；
13. BCR 新旧路径同时对用户可见；
14. 为排版修复改写法律结论或 Gold；
15. 使用 `npm audit fix --force`、远程部署或未授权外部写入。

## 十二、风险和控制措施

| 风险 | 控制措施 |
|---|---|
| IR v4 改动影响其他 schema-first 模块 | v3 兼容 reader、BCR feature flag、公共 reporting 全量回归 |
| renderer 各自扩展私有字段 | 联合类型穷尽检查，Checkpoint A 后冻结契约 |
| 字体文件体积和许可证 | 记录来源/许可证/hash，选择可再分发字体，禁止系统字体隐式依赖 |
| DOCX 在 Word 与 LibreOffice 中差异 | OOXML 结构门禁 + LibreOffice 转换 + 至少一次 Word 人工抽查 |
| PDF 视觉测试过度依赖像素快照 | 结构、文本层、像素边界和人工视觉四层组合，不做单一全页像素精确匹配 |
| BCR 切换导致历史任务打不开 | artifact 按历史 path 保留，reader 兼容旧产物，新旧任务分别测试 |
| Web 为适配 IR 增加 localStorage 压力 | IR 仅按需请求并保存在组件内存，不进入 AppStore persistence |
| 公共 artifact API 越权 | 以登记 artifact type + owner access 双重验证，双用户 API 测试 |
| 方案与 task065/060task_03 冲突 | task067 作为 BCR 垂直切片，字段并入统一 v4 契约，不建立平行架构 |

## 十三、最终交付物

完成后至少应存在：

```text
代码：
  DocumentIR v4 排版纵向切片
  BCR structured adapter
  Markdown/Web/DOCX/PDF IR renderer
  RenderProfile
  artifact report_ir API
  render manifest

测试：
  schema/compiler mutation tests
  BCR adapter golden tests
  Markdown/DOCX/PDF renderer tests
  artifact 权限测试
  React component tests
  Playwright 多 viewport 测试

证据：
  status/check/task067/environment.json
  status/check/task067/render_manifest.json
  status/check/task067/structural_checks.json
  status/check/task067/web/*
  status/check/task067/docx/*
  status/check/task067/pdf/*
  status/check/task067/task067_验收报告.md
```

## 十四、当前执行状态

```text
issue067 根因分析：完成
task067 实施方案：已编写，待审核
T00（基线与工具冻结）：✅ 已完成（见 §十五 落实记录）
T01（升级 DocumentIR 排版契约）：✅ 已完成（见 §十六 落实记录）
T02（重写 BCR adapter 直出结构化 IR）：✅ 已完成（见 §十七 落实记录）
T03（IR → Markdown renderer）：✅ 已完成（见 §十八 落实记录）
T04（受控 IR API + 网页专业阅读版式）：✅ 已完成（见 §十九 落实记录）
T05（IR → DOCX 原生 renderer）：✅ 已完成（见 §二十 落实记录）
T06（IR → PDF 定版 renderer）：✅ 已完成（见 §二十一 落实记录）
T07（render manifest + 跨格式等价证明）：✅ 已完成（见 §二十二 落实记录）
T08（自动排版回归门禁）：✅ 已完成（见 §二十三 落实记录）
T09（影子渲染、差异审计和本地切换）：✅ 已完成（见 §二十四 落实记录）
T10（真实浏览器、DOCX 和 PDF 视觉验收）：✅ 已完成（见 §二十五 落实记录，DOCX 视觉 `BLOCKED_BY_TOOLING`、PDF 中文字体 `BLOCKED_BY_FONT` 如实记录）
T11（公共回归、BCR 旧路径退出和验收归档）：⚠ 公共回归完成，旧路径退出 `BLOCKED`（见 §二十六 落实记录，等 LibreOffice + CJK 字体解除后收尾）
收尾：✅ 已完成（2026-08-10）——影子 flag 已显式设定短期移除日期 2026-09-01（§T11「或明确设定短期移除日期」替代项），详见 §二十七
业务代码改动：T01 已升级 reporting schema/compiler/migration，并再生 9 个模块的 document_ir golden；T02 已把 BCR adapter 从 Markdown 模板反推改为结构化 `BCRFinding[]` 直出 v4 IR，编译门禁 fail-closed；T03 已新增只读 IR 的 `MarkdownRenderer`（12 类 block 穷尽 + 未知 block fatal）；T04 已新增受控 `report_ir` API（`artifact_type=document_ir_json` 且 Pydantic 校验通过才返回）与网页 `ReportDocumentView`（block 联合穷尽、风险文本徽标、clause 多级编号、响应式）；T05 已新增只读 IR 的 `DocxRenderer`（A4/统一边距、命名 style、Heading 大纲级、原生表格+表头 repeat、finding 纵向分块、ClauseNode 原生多级编号 `word/numbering.xml`、keepNext、页眉页脚+动态 PAGE 字段、citation 统一 style、未知 block/profile fail-closed + ZIP 时间戳定版化 `_normalize_zip_timestamps`）+ `render_profiles/`；T06 已新增只读 IR 的 `PdfRenderer`（`BaseDocTemplate`+`PageTemplate`+`onPage` 运行页眉/动态页码、`ParagraphStyle keepWithNext`、finding 摘要短表 repeatRows、finding 详情纵向字段块、ClauseNode 递归多级编号、citation 附录可复制段落、`rl_config.invariant` 确定性输出、字体显式注册、未知 block/profile fail-closed）+ `render_profiles/` 字体盘点（`FontAsset`/`discover_font_assets`/`get_cjk_font`/`get_latin_font`）；T07 已新增 `render_manifest.py`（`RenderManifest` Pydantic 契约 + `build_render_manifest` 全量渲染与原子写 manifest + `verify_render_manifest` 磁盘重校验），记录 canonical IR SHA-256 / 每产物 path+size+SHA-256+MIME / section/finding/clause node id+逻辑编号/citation ID / DOCX 段落+标题+表+编号统计 / PDF 页数+字体嵌入+文本提取 / 各 gate 状态与诊断，并做跨格式 section/finding/clause/citation 等价证明；T08 已新增 `layout_gate.py`（可复用排版契约 verifier：异常断词/六列表格退化/Markdown 分隔残留/编号重置/重复正文）+ `test_layout_contract.py`（14 项对抗样本 + 变异测试）+ `scripts/check_report_artifacts.py`（渲染并跑全部门禁、产出 `structural_checks.json` 证据）；BCR 用户产物仍走旧模板字符串，切换留待 T09（影子渲染 + 本地 flag）；T09 已新增 `shadow_render.py`（`render_ir_artifacts`/`audit_ir_vs_legacy`/`write_audit_json` + `CollectionAudit` 契约）+ `settings.bcr_report_ir_rendering_enabled` 本地开关 + service 接线（阶段 A 影子渲染到 `shadow_ir/` 不登记、阶段 B 切换后正式 `output_files` 指向 IR renderer + `render_manifest.json` 登记、legacy 停放到 `shadow_legacy/`、回滚不删除已生成 IR/manifest）
自动测试：reporting 111 通过（含 v4 compiler gate、migration、markdown renderer 10、docx renderer 16、pdf renderer 15、render_manifest 11、layout_contract 14、visual_html_contract 5）；BCR 39 通过（含 T09 影子/切换/回滚 7 项）；reporting+citation+contracts+BCR+artifacts 合并 356 通过；方案 §9.1 reporting+render+BCR 176 通过、§9.2 artifact+contracts+citation 206 通过；后端全量 1132 通过 / 3 失败（均 cn/pipia、eu/scc_review 无关）；前端 report/css 9 通过、全量 187 通过 / 2 失败（无关）、`npm run build` 通过
浏览器/DOCX/PDF 新验收：DOCX 视觉验收被 LibreOffice 缺失阻断（`BLOCKED_BY_TOOLING`，结构门禁已在 T00 脚本 `_check_docx` 实测 pass：4 原生表 / 11 标题 / 页眉页脚 / 10 编号定义）；PDF 字体嵌入门禁被「无可再分发 CJK 字体」阻断（`BLOCKED_BY_FONT`，见 §二十一 21.3）：拉丁字体 Liberation Sans `emb=yes`，CJK 退化为 `STSong-Light` CID `emb=no`
远程部署：未执行
```

审核通过后的正确起点是 T00：先冻结问题样本、字体和工具条件，再冻结 IR 契约。不得直接从 CSS、DOCX 模板或 PDF 列宽开始零散修改。

---

## 十五、T00 落实记录（2026-08-13）

### 15.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| 环境/结构门禁脚本 | `scripts/check_report_layout_environment.py` | ① 默认盘点工具与 CJK 字体，LibreOffice 缺失时输出 `BLOCKED_BY_TOOLING` 并返回非零（exit 2）；② `--check-docx/--check-pdf/--check-md` 断言 DOCX 原生结构、PDF 字体嵌入与断词、Markdown 六列 finding 表 |
| 基线证据 | `status/check/task067/baseline/` | SHA-256、DOCX 结构、`pdfinfo`/`pdffonts`/`pdftotext -layout`、旧 PDF 逐页 raster（5 页 PNG） |
| 基线清单 | `status/check/task067/baseline/baseline_manifest.json` | 样本任务与输入 fixture 的 hash 可追溯 |
| 环境快照 | `status/check/task067/environment.json` | 工具清单 + 字体盘点（由脚本写入） |
| 回归夹具 | `backend/common/reporting/tests/fixtures/bcr_layout_regression/` | 冻结失败模式签名（无用户正文），供 T08 复用 |

### 15.2 冻结的失败模式（旧产物可稳定失败）

| 门禁 | 旧产物实测 | 说明 |
|---|---|---|
| DOCX | `Word tables=0`、`headings=0`、无页眉页脚、`\| --- \|` 残留 | 11 段落全为 Normal，finding 详情是单一普通段落 |
| PDF | `fonts embedded=0/3`；`MEDIUM` 断为 `ME/DIU/M`；finding ID 断为 `-C-1`/`.1` | Helvetica / STSong-Light / ZapfDingbats 全部 `emb=no` |
| Markdown | 六列 finding 详情表（`检查项/主题/风险/现状/整改建议/法律依据`） | 20 行 pipe 表，是 PDF 断词与 pipe 残留的根源 |

### 15.3 环境结论（不可伪造）

- 工具：`uv/pdftotext/pdfinfo/pdffonts/pdftoppm/unzip/zipinfo` 已具备；**`libreoffice/soffice` 缺失** → DOCX 视觉验收只能标 `BLOCKED_BY_TOOLING`，不得写 PASS。
- 字体：仓库**无**可再分发的中文（CJK）字体资产，仅有 PDF.js 的 Latin-only Liberation Sans；当前 PDF 依赖未嵌入的系统 CID 字体（STSong-Light）。PDF 门禁在加入带许可证/ hash 的 CJK 字体前不能通过。

### 15.4 T00 验收对照

- [x] fixture 与实际样本 hash 可追溯（`baseline_manifest.json`）
- [x] 至少一个自动检查能在旧产物上稳定失败（DOCX/PDF/Markdown 三门禁均 fail）
- [x] 工具缺失返回非零并明确 `BLOCKED_BY_TOOLING`（exit 2 + stderr 提示）
- [x] 不提交用户敏感正文（输入为已批准的 `bcr_c_globaltech.docx` 测试夹具；回归夹具仅存结构签名）
- [x] 字体选择有许可证和 hash（如实记录“无 CJK 字体”缺口，列为 T05/T06 前置条件）

---

## 十六、T01 落实记录（2026-08-13）

### 16.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| finding/action/clause 模型 | `backend/common/reporting/schema/findings.py` | `FindingRecord`/`ActionRecord`/`ClauseNode`（递归多级编号），`extra=forbid` |
| render contract | `backend/common/reporting/schema/rendering.py` | `RenderContract`（profile、required/optional artifacts、locale、TOC/页码/引用附录、外部可见性策略） |
| v4 block 联合 | `backend/common/reporting/schema/blocks.py` | 12 个 block（v3 五类 + `FINDING_SUMMARY`/`FINDING_DETAIL`/`FINDING_REFERENCE`/`CLAUSE_GROUP`/`KEY_VALUE`/`PAGE_BREAK`/`CITATION_NOTE`）；`ListBlock` 升级为结构化嵌套 `ListItem`；`type` 为唯一判别器 |
| v4 DocumentIR | `backend/common/reporting/schema/document.py` | `schema_version="4.0"`；保留 v3 构造面（`document_id`/`report_type`/内联 `SectionIR.blocks` 必填），v4 字段（identity/lifecycle/findings/actions/citations/render_contract/integrity/migrated_from_schema）为可选 |
| v4 编译门禁 | `backend/common/reporting/compiler/core.py` | v3 六 pass 保留 + `FindingValidationPass`/`ClauseStructureValidationPass`/`ActionValidationPass`/`RenderContractValidationPass`；fatal 时 fail-closed |
| v3→v4 迁移 reader | `backend/common/reporting/migration.py` | `migrate_v3_document()`；非 v3.0 显式 `ValueError`；内联 block 原样保留，记录 `migrated_from_schema="3.0"` |
| 顶层再导出 | `backend/common/reporting/__init__.py` | 41 个符号，含 `migrate_v3_document` 与 `CompileResult`/`PassResult` |
| v4 gate 测试 | `backend/common/reporting/tests/test_compiler_v4.py` | 12 项：唯一性、主展示==1、断裂引用、clause 深度/重复 node、未知 profile fatal、未知 block 拒绝、citation note |
| 迁移测试 | `backend/common/reporting/tests/test_migration.py` | 5 项：v3 迁移、JSON 字符串输入、非 v3 拒绝、v4 拒绝、非 dict 拒绝 |
| 9 模块 golden 再生成 | `backend/domains/{cn,eu,us}/**/tests/**/*_document_ir.golden.json`（review/pipia/assessment/dpia/scc/tia/cpra/eo14117/bcr） | `schema_version` 3.0→4.0 且新增可选 v4 字段（identity/lifecycle/findings/actions/citations/render_contract/integrity/migrated_from_schema）后，由现有 v3 风格 adapter 输出的 v4 快照；BCR 的该快照是过渡态，T02 重写 adapter 后再次冻结 |

### 16.2 关键设计决策

- **向后兼容而非破坏**：`DocumentIR` 保留 v3 必填 `document_id`/`report_type` 与内联 `SectionIR.blocks`，避免破坏 9 个既有 `schema_first.py` adapter 与跨模块契约测试 `test_schema_first_contracts.py`（34 项全过）。
- **单判别器 + `extra=forbid`**：`Block` 用 `type` 判别，所有模型 `extra="forbid"`，未知 block/profile 在解析/编译期即 fatal。
- **主展示次数由编译期推导**：`FindingRecord.primary_display_count` 不写入业务，由 `FindingValidationPass` 统计 `FINDING_DETAIL` 引用数并断言 `==1`。
- **clause 编号在 schema 表达、不在 text**：`ClauseNode.numbering_style` + `children` 表达多级编号，禁止手写 `1.`/`(a)`。
- **v3→v4 只迁移不并轨**：迁移 reader 只接受显式 v3.0 输入；新 BCR adapter 将在 T02 直接产出 v4 IR，不再长期并存私有 IR。

### 16.3 编译门禁对照（§T01）

| 门禁 | 实现 |
|---|---|
| finding/block/clause node ID 唯一 | `FINDING_ID_DUPLICATE` / `BLOCK_ID_DUPLICATE` / `CLAUSE_NODE_ID_DUPLICATE` |
| 每个 finding 恰好一个主展示 | `FINDING_PRIMARY_DISPLAY_COUNT`（==1，否则 error） |
| finding summary 只引用存在 finding | `FINDING_REF_NOT_REGISTERED` |
| clause 最大深度、numbering style、parent/child 合法 | `CLAUSE_DEPTH_EXCEEDED`（>4 报错）+ schema Literal 约束 |
| section level 不跳级 | `SECTION_LEVEL_SKIP`（v3 保留） |
| block 文本不含 Markdown 结构标记 | schema `field_validator`（`#`/`**`/`__`/反引号/`[N]`/`{{CIT-*}}` 拒绝） |
| render contract profile 必须存在 | `RENDER_PROFILE_UNKNOWN`（fatal） |
| 未知 block/profile fatal | 判别联合解析拒绝 + `RENDER_PROFILE_UNKNOWN` fatal |

### 16.4 T01 验收对照

- [x] v4 fixture 可序列化并通过 compiler（`test_compiler_v4.py::test_v4_fixture_compiles_clean_and_records_primary_display_count`）
- [x] 重复主 finding、断裂引用、非法嵌套、未知 block 的 mutation tests 均失败（`test_compiler_v4.py` 12 项）
- [x] 现有 v3 fixture 有明确迁移结果或明确拒绝（`test_migration.py` 5 项）
- [x] `060task_03` 契约方向未被破坏（`test_schema_first_contracts.py` 34 项全过）

### 16.5 验证命令与结果

```bash
uv run --frozen pytest -q backend/common/reporting backend/tests/contracts backend/domains \
  -k "schema_first or reporting or citation or compiler"
# 193 passed
```

- 9 个 `schema_first.py` adapter 全部 import 通过；`DocumentIR` 构造面未破坏。
- `ListBlock(items: list[ListItem])` 升级对既有 adapter 无影响（无 adapter 构造 reporting `ListBlock`；`backend/common/render/report_model.ListBlock` 是独立渲染模型，不受影响）。
- 9 个模块的 `*_document_ir.golden.json` 已从 v3 快照再生为 v4 快照（`schema_version` 3.0→4.0 + 新增可选 v4 字段）。
- 本次未改 BCR adapter、service 或任何渲染路径；T02 起才把 BCR 从 Markdown 模板源切到 IR 直出。
- `backend/common/knowledge` 有一个与本任务无关的既有失败（`test_non_quarantined_sources_remain_citable`，`JP-GUIDE-006` review_status 数据问题），不属于 task067。

---

## 十七、T02 落实记录（2026-08-13）

### 17.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| 结构化 BCR adapter | `backend/domains/eu/bcr_review/schema_first.py` | `build_bcr_document_ir(*, task_id, company_name, type_class, rating, score, findings, missing, citation_registry, metadata, model, generated_at)` 直出 v4 IR；8 个 section（`bcr.s1`..`bcr.s8`）；每条 finding → 1 个 `FindingRecord` + 恰好 1 个 `FINDING_DETAIL`；摘要只引用；citation 保留稳定 ID；`suggested_revision` 结构化为 `ClauseNode`（编号进 schema，不进 text） |
| 旧 chapters 降级 shim | `backend/domains/eu/bcr_review/schema_first.py::build_bcr_document_ir_from_chapters` | 仅供 form-driven 路径（T07 退出）的废弃 reader，不喂 document-driven renderer |
| finding 引用字段 | `backend/domains/eu/bcr_review/schema.py::BCRFinding.citation_refs` | 稳定 citation identity 绑定到 finding，不再把 `[N]` 写进 `legal_basis` |
| service 接线 | `backend/domains/eu/bcr_review/service.py` | form-driven `_render` → legacy shim；document-driven `_render_document_driven` → 新 adapter + `DocumentCompiler` fail-closed（`status != "success"` 即 `raise ValueError`，不产出成功 DOCX/PDF）；`_annotate_finding_footnotes` 存 `citation_refs` 而非内嵌 `[N]` |
| BCR golden 冻结 | `backend/domains/eu/bcr_review/tests/fixtures/bcr_document_ir.golden.json` | 由 findings-based v4 adapter 再生成的稳定快照 |
| adapter 测试 | `backend/domains/eu/bcr_review/tests/test_schema_first_adapter.py` | 8 项：golden 快照、finding 数/序、主展示==1、无 Markdown table separator、citation 留 ID 不写 `[N]`、多级 clause node ID/numbering、byte-stable、未知 citation 拒绝 |
| renderer 测试 | `backend/domains/eu/bcr_review/tests/test_schema_first_renderer.py` | 5 项：findings-based IR 编译通过、citation identity 在 finding 上、block 数不变量、未知 citation 阻断、citation map/finding/pool identity 一致 |
| 跨模块契约 | `backend/tests/contracts/test_schema_first_contracts.py::_bcr_case` | 改用 `build_bcr_document_ir_from_chapters`（legacy shim），其余 7 个 adapter 契约不破坏 |

### 17.2 关键设计决策

- **单一权威 IR 源**：document-driven renderer 只以 `build_bcr_document_ir` 产出的 v4 IR 为输入；`sections`/`chapters` 仅保留给 `BCRResult` API 兼容消费者与旧模板映射，不反向作为正式 renderer 的 IR 来源。
- **citation 身份下沉到 finding**：`BCRFinding.citation_refs` 保存稳定 citation_id；adapter 同时读 `citation_refs` 与 `legal_basis` 中的脚注编号（经 registry footnote map 解析），两种形状归一到同一 ID。
- **fail-closed**：`DocumentCompiler().compile(...).status != "success"`（含 error/fatal）即抛异常，阻断 DOCX/PDF/MD 产出，满足「compiler error/fatal 不得生成成功产物」。
- **建议条款结构化**：`suggested_revision` 若已是 `(a)...\n(b)...` 编号行，拆为 `ClauseNode` 的 `lower_alpha` children，node_id 稳定（`clause.<finding_id>.<n>`）；否则为单节点。

### 17.3 T02 验收对照

- [x] finding 数和顺序与 BCRResult 完全一致（`test_finding_count_and_order_match_input`；document-driven 路径 `findings=deduped` 直传，顺序不重排）
- [x] 每个 finding 主展示次数为 1（`test_each_finding_has_exactly_one_primary_display`；`FindingValidationPass` 断言 `primary_display_count == 1`）
- [x] IR 中不存在 Markdown table separator（`test_ir_has_no_markdown_table_separator`）
- [x] 多级条款有稳定 node ID 和编号关系（`test_suggested_revision_becomes_multi_level_clause`）
- [x] 同一固定输入生成 byte-stable canonical IR（`test_byte_stable_for_fixed_input`，时间字段冻结）
- [x] 旧问题样本可稳定生成 v4 IR（`test_bcr_document_review_binds_only_supported_citations` / `test_bcr_document_upload_path_exposes_document_ir`，基于 `case_02_healthdata_bcr_c_draft.docx` 产出 findings-based v4 IR）

### 17.4 验证命令与结果

```bash
uv run --frozen pytest -q backend/domains/eu/bcr_review/tests
# 28 passed

uv run --frozen pytest -q backend/domains/eu/bcr_review/tests \
  backend/tests/contracts/test_schema_first_contracts.py \
  backend/common/reporting/tests backend/common/citation/tests
# 259 passed
```

- 跨模块契约 `test_schema_first_contracts.py` 34 项全过（BCR case 走 legacy shim，其余 7 个 adapter 契约未动）。
- 唯一与本任务无关的既有失败仍在 `backend/common/knowledge`（`JP-GUIDE-006` 数据问题），不属于 task067。
- 本阶段未改 Markdown/DOCX/PDF 渲染路径（T03-T06）；旧模板字符串路径仍在产出用户产物，留待 T07 退出。

---

## 十八、T03 落实记录（2026-08-13）

### 18.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| IR → Markdown renderer | `backend/common/reporting/renderers/markdown.py` | `MarkdownRenderer.render(document, registry=None)` + `render_markdown()`；只读 `DocumentIR`，输出纯结构化 Markdown（heading/table/list/blockquote/paragraph），不含 `**`/`__` 强调、不残留 `{{...}}` |
| 12 类 block 穷尽 | 同上 `_render_block` | paragraph/claim/list/table/warning/key_value/finding_summary/finding_detail/finding_reference/clause_group/page_break/citation_note 全处理；未知 block 抛 `RenderError`（fail-closed） |
| 首用编号 | 同上 `_assign_citation_numbers` | 按与 compiler 相同的显示序（section→block→clause→finding）幂等分配 footnote number；inline `[N]` 与 citation appendix 统一由 registry 生成 |
| 多级条款 | 同上 `_render_clause` | `decimal`/`lower_alpha`/`lower_roman`/`none` 显式编号 + 递归缩进，不依赖 Markdown 列表自动重编号，保留法律编号身份 |
| 顶层再导出 | `backend/common/reporting/__init__.py` | 新增 `MarkdownRenderer`/`RenderError`/`render_markdown`；`backend/common/reporting/renderers/__init__.py` 新建 |
| renderer 单测 | `backend/common/reporting/tests/test_markdown_renderer.py` | 10 项：集合相等、无六列表、嵌套列表缩进、lint、hash 稳定、compiler render gate 集成、claim 脚注、finding 字段分块、未知 block 拒绝、缺 finding ref 拒绝 |
| BCR 集成 | `backend/domains/eu/bcr_review/tests/test_schema_first_renderer.py::test_bcr_ir_renders_to_markdown_without_six_column_table` | 真实 BCR v4 IR 经 MarkdownRenderer 渲染，8 section / 3 finding 齐全、无六列表、无 `{{`/强调残留 |

### 18.2 关键设计决策

- **只读 IR、不碰模板**：renderer 唯一输入是 `DocumentIR` + citation registry；不 import、不读取 BCR 模板或 `chapter.content`。
- **强调标记禁用**：compiler 的 `RenderValidationPass` 把 `**`/`__` 判为 error，故 renderer 用 blockquote（`>`）表达 warning/reference，用纯文本标签表达字段分块，避免产物再被 render gate 拦下。
- **编号不入 text**：clause 编号（`1.`/`(a)`/`i.`）由 renderer 从 `numbering_style`+兄弟序推导，与 T01「编号在 schema 表达、不在 text」一致。
- **首用编号幂等**：`assign_footnote_number` 幂等，renderer 与 compiler 使用同一遍历序，因此无论是否先跑 compiler，`[N]` 编号都一致。
- **service 接线延后**：T03 只交付 renderer 与测试；把 BCR 用户产物切到 IR renderer 属 T09（影子渲染 + `BCR_REPORT_IR_RENDERING_ENABLED` 本地切换），T07 的 render manifest 也会消费本 renderer。

### 18.3 T03 验收对照

- [x] 章节/finding/clause/citation 集合与 IR 相等（`test_section_finding_clause_citation_sets_match_ir` + BCR 集成）
- [x] 不出现六列长 finding 表（`test_no_six_column_finding_table`，逐表断言列数 < 6 且无旧表头）
- [x] 嵌套列表层级正确（`test_nested_list_indentation_is_structural`，4 空格 CommonMark 安全缩进；仓库无 CommonMark 依赖，直接断言结构缩进）
- [x] Markdown lint 通过（`test_markdown_lint_passes`：无 `{{`/`}}`、无 `**`/`__`、无行尾空白、无 tab、heading ≤ 6）
- [x] 同一 IR 重复渲染 hash 稳定（`test_render_is_hash_stable`）

### 18.4 验证命令与结果

```bash
uv run --frozen pytest -q backend/common/reporting/tests
# 50 passed（含 markdown renderer 10 项）

uv run --frozen pytest -q backend/common/reporting/tests backend/common/citation/tests \
  backend/tests/contracts/test_schema_first_contracts.py \
  backend/domains/eu/bcr_review/tests
# 270 passed
```

- 新增 `backend/common/reporting/renderers/markdown.py`（372 行）与 `tests/test_markdown_renderer.py`（279 行）。
- 唯一与本任务无关的既有失败仍在 `backend/common/knowledge`（`JP-GUIDE-006` 数据问题），不属于 task067。
- 未改 DOCX/PDF 渲染（T05/T06）、未改 service 用户产物路径（T09）、未做远程连接/部署。

## 十九、T04 落实记录（2026-08-13）

### 19.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| 受控 report IR API | `backend/api/v1/endpoints/artifacts.py` | `_resolve_artifact_access` 返回实际 `artifact_type`（越权 403）；仅当 `artifact_type == "document_ir_json"` 且 Pydantic 校验通过时返回 `render_mode="report_ir"` + `report_ir`（`content=""`） |
| IR 解析与版本门禁 | 同上 `_parse_document_ir` | 解析 JSON（非对象/损坏 → 422）；`schema_version=4.0` 走 `DocumentIR.model_validate`，`3.0` 走 `migrate_v3_document`，其余版本 422 `Unknown DocumentIR schema version` |
| 响应模型 | `backend/schemas/common.py` | `ArtifactPreviewResponse.report_ir: dict[str, Any] \| None`（可选，普通 JSON/text 仍返回 `content`） |
| API 单测 | `backend/api/v1/tests/test_artifact_report_ir.py` | 6 项：所有者读 IR（`content==""`）、其他用户 403、任意 JSON 不冒充 IR、损坏 JSON 422、未知版本 422、v3 迁移为 v4（`migrated_from_schema="3.0"`） |
| TS 类型镜像 | `frontend/src/lib/document-ir.ts` | v4 `Block`/`FindingRecord`/`ClauseNode`/`DocumentIR` 全量 TS 镜像 + `isDocumentIR` 运行时守卫（`schema_version` 4.0/3.0、`document_id` 字符串、`sections`/`findings` 数组） |
| 受控读取 hook | `frontend/src/lib/use-report-ir.ts` | 在 app-store 找 `document_ir_json`，按需 fetch，`isDocumentIR` 校验后仅在组件内存持有（不落 localStorage）；状态 idle/loading/ready/error/unavailable |
| 网页阅读组件 | `frontend/src/components/report/ReportDocumentView.tsx` | `BlockNode` 对 12 类 block 穷尽 switch（未知 block 渲染 `<p class="ir-unknown-block">未知内容块…` 不 throw）；`SectionView` 用 `ordinal title` 标题 |
| finding 分块 | `frontend/src/components/report/FindingView.tsx` | `<article class="finding-card">` + `finding-fields` 字段分块（现状/法规依据/整改建议/建议修改文本）+ 文本风险徽标（高风险/中风险/低风险，不依赖颜色） |
| 条款编号 | `frontend/src/components/report/ClauseTree.tsx` | `numberToken` 递归渲染 `decimal`/`lower_alpha`/`lower_roman`/`none`（`undefined` 视作 `decimal` 默认，与 schema 一致） |
| 阅读样式 | `frontend/src/styles/app/report.css` | `.report-document` 最大宽 62rem、`word-break: break-word`、`overflow-wrap: anywhere`；`.finding-card` 文本徽标；`.clause-tree`；`@media (max-width:720px)` 单列降级 |
| 报告页接线 | `frontend/src/pages/ReportCenterPage.tsx` | 正文优先级：`reportIr.status === "ready"` → `<ReportDocumentView>`；否则 error（`role="alert"` + i18n 文案）+ chapters 兼容回退；DOCX 不进入正文 |
| OpenAPI 类型 | `frontend/src/api/generated/openapi.d.ts` | 再生含 `report_ir?: { [key: string]: unknown } \| null` |

### 19.2 关键设计决策

- **受控入口，fail-closed**：只有 `document_ir_json` 且通过 `DocumentIR`/`migrate_v3_document` 校验才进 IR 渲染；任意 JSON 仍走 text preview，损坏/未知版本 422，杜绝把普通 JSON 冒充结构化 IR。
- **正文单一来源**：网页正文优先级为 `有效 IR > response 兼容 chapters`；DOCX 只保留打开/下载动作，绝不进正文（`INTERNAL_ARTIFACT_PATTERNS` 已含 `document_ir`，故 `document_ir_json` 不显示为普通下载项）。
- **内存持有、不持久化**：IR 只由 `useReportIr` 按需 fetch 并在组件内存持有，不写入 AppState/localStorage，避免把结构化报告塞进全局状态。
- **风险不只靠颜色**：finding 风险用文本徽标（高风险/中风险/低风险）表达，CSS 颜色只是辅助，满足可访问性。
- **DOCX 视觉验收不伪造**：LibreOffice/soffice 缺失 → DOCX 视觉验收保持 `BLOCKED_BY_TOOLING`；本阶段不执行浏览器/DOCX/PDF 视觉验收，不做远程部署。

### 19.3 T04 验收对照

- [x] 所有者可读取 IR，其他用户 403（`test_owner_reads_document_ir`、`test_other_user_403`）
- [x] 任意 JSON 不能冒充 report IR（`test_arbitrary_json_is_not_report_ir`，`render_mode="text"`）
- [x] 损坏 IR 显示可恢复错误，不白屏（后端 422 + 前端 `role="alert"` + chapters 回退；`test_corrupted_ir_returns_422`）
- [x] 页面只有一个报告正文主展示（`ReportCenterPage` 只在 `reportIr.status==="ready"` 渲染 `ReportDocumentView`，else 分支为兼容回退，无双正文）
- [x] DOCX 不进入 Markdown renderer（正文链路不 import DOCX 抽取；`useReportIr` 只找 `document_ir_json`）
- [x] 风险信息不只依赖颜色（文本风险徽标 `test_risk_is_text_not_color`）
- [ ] 1440、1280、390 宽度无页面级横向滚动 —— 未执行（需浏览器验收，留待 T10；CSS 已做 62rem 宽 + `overflow-wrap: anywhere` + 移动端单列降级）
- [ ] 长英文和中英文混排不溢出 —— 未执行（同上，留待 T10 视觉验收）

### 19.4 验证命令与结果

```bash
uv run --frozen pytest -q backend/api/v1/tests/test_artifact_report_ir.py
# 6 passed

uv run --frozen pytest -q backend/common/reporting/tests backend/common/citation/tests \
  backend/tests/contracts/test_schema_first_contracts.py \
  backend/domains/eu/bcr_review/tests \
  backend/api/v1/tests/test_artifacts_access.py \
  backend/api/v1/tests/test_artifact_report_ir.py
# 279 passed

cd frontend && npx vitest run src/components/report/ReportDocumentView.test.tsx tests/architecture/css-structure.test.ts
# 9 passed（report 组件 7 + css 结构 2）
```

- 前端 `ReportDocumentView.test.tsx` 7 项全过（含 `getAllByText("现状")` 修正：两个 finding 均含"现状"字段标签，改用 `getAllByText`）。
- `tests/architecture/css-structure.test.ts` 同步更新 `EXPECTED_IMPORTS`/`STYLE_SEGMENTS`，新增 `report.css`。
- 未改 DOCX/PDF 渲染（T05/T06）、未改 service 用户产物路径（T09）、未做远程连接/部署、未写入用户正文/敏感内容。
- 与本任务无关的既有前端失败仍在 `src/lib/dev-test-cases.test.ts`（PIPIA fixture 直/弯引号文件名不一致）与 `tests/contract/dev-case-fixtures.test.ts`，不属于 task067。

## 二十、T05 落实记录（2026-08-13）

### 20.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| IR → DOCX 原生 renderer | `backend/common/reporting/renderers/docx.py` | `DocxRenderer.render(document, registry=None) -> bytes` + `render_to_file` + `render_docx`/`render_docx_to_file`；只读 `DocumentIR`，产出可继续编辑、结构可检验的 Word 报告 |
| render profiles | `backend/common/reporting/render_profiles/__init__.py` | `RenderProfile`（A4 210×297、统一边距、正文/标题 CJK 字体、基础字号、命名 style id）+ `get_profile`（未知 profile 抛 `RenderProfileError`）+ `KNOWN_PROFILE_IDS`；纯数据无 `python-docx` 依赖 |
| 12 类 block 穷尽 | 同上 `_render_block` | paragraph/claim/list/table/warning/key_value/finding_summary/finding_detail/finding_reference/clause_group/page_break/citation_note 全处理；未知 block 抛 `DocxRenderError` |
| A4 + 统一 margin | 同上 `_configure_page` | `section.page_width/height` + `top/bottom/left/right_margin` 全部来自 profile |
| 命名 style | 同上 `_ensure_styles` | 创建/校验 `CitationNote`/`FieldLabel`/`Warning` 三个段落 style（`doc.styles.add_style`） |
| Heading 大纲级 | 同上 `_add_heading` | `add_heading(level=1..6)`（默认模板 Heading 1..6 已带 `w:outlineLvl`）+ `keep_with_next=True` |
| 原生表格 + 表头 repeat | 同上 `_render_table`/`_set_table_header_repeat` | 元数据表、finding 摘要表、table/key_value block 均为原生 `w:tbl`；首行 `w:tblHeader` repeat |
| finding 纵向分块 | 同上 `_render_finding_detail`/`_add_field_block` | 标题（`finding_id title`）+ 现状/法规依据/整改建议/建议修改文本字段块，无六列长表 |
| ClauseNode 多级编号 | 同上 `_ensure_clause_numbering`/`_apply_numbering` | 动态生成多级 `abstractNum`（decimal/lowerLetter/lowerRoman，`%N.`/`(%N)` lvlText）+ `num`，clause 段落 `w:numPr` 引用 numId/ilvl；编号不入 text |
| 页眉页脚 + 动态页码 | 同上 `_add_header_footer`/`_add_page_number_field` | 页眉 short_title；页脚居中 `PAGE` 域（`w:fldChar`/`w:instrText`） |
| citation 统一 style | 同上 `_render_citation_note` | 附录逐条 `[N]/N.` + `CitationNote` style |
| renderer 单测 | `backend/common/reporting/tests/test_docx_renderer.py` | 15 项：OOXML ZIP 完整、python-docx 重开、Heading 顺序、原生表数、numbering.xml 多级定义、keepNext/页码/页眉页脚、无 `\| --- \|`/强调/模板残留、finding/clause 计数、compiler render gate、未知 block/profile/缺 ref 拒绝、摘要短表、hash 稳定、render_to_file |
| BCR 集成 | `backend/domains/eu/bcr_review/tests/test_schema_first_renderer.py::test_bcr_ir_renders_to_docx_with_native_structure` | 真实 BCR v4 IR 经 DocxRenderer 渲染，8 section / 3 finding 齐全、无六列表、无 Markdown 残留、clause 多级编号在 numbering.xml |

### 20.2 关键设计决策

- **只读 IR、不碰模板**：renderer 唯一输入是 `DocumentIR` + citation registry；不 import、不读取 BCR 模板或 `chapter.content`。
- **编号不入 text**：clause 编号由 Word `numbering.xml` 多级 `abstractNum` 表达，段落 text 只有条款正文（`第一条`/`第一项`/`第二项`），`w:numPr` 引用 numId/ilvl；`_collect_depth_formats` 从每层首个非 `none` 节点取 numbering_style，映射 decimal/lower_alpha/lower_roman → decimal/lowerLetter/lowerRoman。
- **profile 单一裁决**：页面几何/字体/命名 style 统一由 `render_profiles` 提供，`get_profile` fail-closed；renderer 不得为未注册 profile 发明几何。
- **风险/字段不靠颜色**：DOCX 全为纯文本，finding 摘要短表带风险等级列，finding 详情字段标签加粗，正文无颜色依赖。
- **Markdown 语法禁用**：普通段落不含 `**`/`__`/`| --- |`/`{{...}}`，与 compiler `RenderValidationPass` 一致。
- **DOCX 视觉验收不伪造**：LibreOffice/soffice 缺失 → DOCX 视觉验收保持 `BLOCKED_BY_TOOLING`；本阶段只做机器结构门禁，不做远程部署。

### 20.3 T05 验收对照

- [x] OOXML ZIP 完整（`test_docx_is_valid_ooxml_zip`）
- [x] `python-docx` 可重新打开（`test_docx_reopens_with_python_docx`）
- [x] Heading 1/2/3 顺序正确（`test_heading_levels_follow_section_order`）
- [x] 原生 table 数符合设计（`test_native_table_count_matches_design`：1 元数据 + 1 table + 1 key_value + 1 summary = 4）
- [x] `word/numbering.xml` 存在预期编号定义（`test_numbering_part_contains_multilevel_clause_definition`，多级 abstractNum 含 lowerLetter）
- [x] `w:keepNext`、页码 field、页眉页脚关系存在（`test_keep_next_page_field_and_header_footer_present`）
- [x] 普通段落不存在 `| --- |`（`test_no_markdown_table_residue_in_paragraphs`）
- [x] finding 和 clause 计数与 IR 一致（`test_finding_and_clause_counts_match_ir`）
- [ ] DOCX → LibreOffice headless → PDF → pdftoppm PNG 视觉验收 —— 未执行（LibreOffice 缺失，`BLOCKED_BY_TOOLING`）

### 20.4 验证命令与结果

```bash
uv run --frozen pytest -q backend/common/reporting/tests/test_docx_renderer.py
# 15 passed

uv run --frozen pytest -q backend/common/reporting/tests backend/common/citation/tests \
  backend/tests/contracts/test_schema_first_contracts.py \
  backend/domains/eu/bcr_review/tests \
  backend/api/v1/tests/test_artifacts_access.py \
  backend/api/v1/tests/test_artifact_report_ir.py
# 295 passed
```

- T00 结构门禁 `scripts/check_report_layout_environment.py::_check_docx` 对新 renderer 输出的 BCR DOCX 实测 `pass`：86 段落 / 4 原生表 / 11 标题（Heading 1/2/3）/ 页眉页脚 / 10 编号定义。
- 新增 `backend/common/reporting/renderers/docx.py` 与 `render_profiles/__init__.py`、`tests/test_docx_renderer.py`。
- 未改 service 用户产物路径（T09）、未做远程连接/部署、未写入用户正文/敏感内容；DOCX 视觉验收保持 `BLOCKED_BY_TOOLING`。

---

## 二十一、T06 落实记录（2026-08-13）

### 21.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| IR → PDF 定版 renderer | `backend/common/reporting/renderers/pdf.py` | `PdfRenderer.render(document, registry=None) -> bytes` + `render_to_file` + `render_pdf`/`render_pdf_to_file`；只读 `DocumentIR`，`BaseDocTemplate`+`PageTemplate`+`onPage` 产出固定版式 A4 PDF |
| 字体显式注册 | 同上 `_register_fonts`/`_register_ttf` + `render_profiles/__init__.py` | 拉丁字体用可再分发 Liberation Sans（TTF `emb=yes`）；CJK 在有许可+hash 资产时注册 `ReportCJK`（TTF `emb=yes`），否则退化 `STSong-Light`（CID `emb=no`）；`FontAsset`/`discover_font_assets`/`get_cjk_font`/`get_latin_font` |
| 12 类 block 穷尽 | 同上 `_render_block` | paragraph/claim/list/table/warning/key_value/finding_summary/finding_detail/finding_reference/clause_group/page_break/citation_note 全处理；未知 block 抛 `PdfRenderError` |
| 运行页眉 + 动态页码 | 同上 `_build_template`/`_draw_page_chrome` | 页眉 short_title + 细分隔线；页脚居中 `str(doc.page)`（纯数字 → 拉丁字体干净嵌入） |
| finding 摘要短表 | 同上 `_render_finding_summary`/`_render_table` | 4 列（编号/风险等级/标题/状态），`Table(repeatRows=1)` 表头跨页 repeat；无六列长表 |
| finding 详情纵向分块 | 同上 `_render_finding_detail`/`_field_content` | 标题 + 现状/法规依据/整改建议/建议修改文本字段块，字段标签 `keepWithNext=1` |
| ClauseNode 递归多级编号 | 同上 `_render_clause`/`_clause_number_token` | 显式 `decimal`/`lower_alpha`/`lower_roman` 标记 + 递归缩进，编号写入文本层（可复制/可检索，非 ListFlowable 避免编号重置） |
| citation 附录可复制段落 | 同上 `_render_citation_note`/`_format_citation` | 逐条 `N. title — Article x` 普通段落，非栅格图 |
| 确定性输出 | 同上 `render()` 内 `rl_config.invariant = True` | 固定 `/CreationDate` 与 `/ID` digest，字节级 hash 稳定（render_manifest 前提） |
| renderer 单测 | `backend/common/reporting/tests/test_pdf_renderer.py` | 15 项：`%PDF` magic+页数>0、finding ID 可提取、无断词碎片（`HIG`/`ME`/`DIU`/`-C-1`/`.1`）、无 Markdown 分隔残留、finding/clause/citation 计数与 IR 一致、compiler render gate、字体嵌入门禁（退化 `STSong-Light` emb=no / 资产存在时 `cjk_embedded=True`）、未知 block/profile/缺 ref 拒绝、摘要短表、hash 稳定、render_to_file、raster 每页非空且内容不越页边界 |
| BCR 集成 | `backend/domains/eu/bcr_review/tests/test_schema_first_renderer.py::test_bcr_ir_renders_to_pdf_with_fixed_layout` | 真实 BCR v4 IR 经 PdfRenderer 渲染 2 页 A4，3 finding ID 齐全、无六列表/Markdown 残留、citation 附录可检索 |

### 21.2 关键设计决策

- **只读 IR、不碰模板/旧 Markdown 解析器**：renderer 唯一输入是 `DocumentIR` + citation registry；不调用 legacy `_markdown_to_flowables()`。
- **字体失败显式、不伪造**：`_register_fonts` 返回 `FontSpec(body/body_bold/latin/latin_bold/cjk_embedded/cjk_asset)`；仓库无 CJK 资产时 `cjk_embedded=False`，PDF 字体门禁保持 `BLOCKED_BY_FONT`，绝不把 CID `emb=no` 当成 PASS。
- **列表/条款自写编号**：ReportLab `ListFlowable` 会对嵌套列表重置编号、破坏多级法律编号身份，故 list 与 clause 均采用显式标记 + 深度缩进段落，保证文本层确定且可复制。
- **确定性优先**：启用 `rl_config.invariant` 使 `/ID`/`/CreationDate` 固定，PDF 字节 hash 跨运行稳定（供 T07 render_manifest 记录与跨格式等价证明）。
- **字段标签 keepWithNext**：finding 详情字段标签与其内容用 `ParagraphStyle(keepWithNext=1)` 绑定，避免标签孤行于页底。

### 21.3 T06 验收对照

- [x] `%PDF` magic、页数 > 0（`test_pdf_magic_and_positive_page_count`）
- [x] 所有 finding ID 可提取（`test_finding_ids_extractable` + BCR 集成实测 3 ID）
- [x] 不出现断词碎片 `HIG`/`ME`/`DIU`/`-C-1`/`.1`（`test_no_broken_token_fragments` + `pdftotext -layout` 实测 0 命中）
- [x] 不残留 Markdown table separator（`test_no_markdown_table_separator_residue`）
- [x] finding/clause/citation 计数与 IR 一致（`test_finding_clause_citation_counts_match_ir`）
- [x] 未知 block/profile/缺 finding ref/citation ref fail-closed（`test_unknown_block_type_raises`/`test_unknown_render_profile_raises`/`test_missing_finding_ref_raises`）
- [x] 字节级 hash 稳定（`test_render_is_hash_stable`，依赖 `rl_config.invariant`）
- [x] raster 每页非空、无内容越过页面边界（`test_raster_pages_non_empty_and_within_bounds`，`pdftoppm` 缺失时按 T00 门禁策略 skip）
- [ ] PDF 中文字体 `emb=yes` 门禁 —— `BLOCKED_BY_FONT`：仓库无可再分发 CJK 字体，CJK 退化 `STSong-Light`（CID `emb=no`），不得伪造为 PASS；待加入许可+hash 的 CJK TTF 后自动转为 `cjk_embedded=True` 且 `pdffonts` 显示 `emb=yes`

### 21.4 验证命令与结果

```bash
uv run --frozen pytest -q backend/common/reporting/tests/test_pdf_renderer.py
# 15 passed

uv run --frozen pytest -q backend/common/reporting/tests backend/common/citation/tests \
  backend/tests/contracts/test_schema_first_contracts.py \
  backend/domains/eu/bcr_review/tests \
  backend/api/v1/tests/test_artifacts_access.py \
  backend/api/v1/tests/test_artifact_report_ir.py
# 317 passed
```

- `pdffonts` 对 BCR 样例 PDF 实测：

```text
Helvetica                            Type 1            WinAnsi          no  no  no  (reportlab 内置，恒 emb=no)
STSong-Light                         CID Type 0        UniGB-UCS2-H     no  no  no  (无 CJK 资产时的退化，emb=no)
AAAAAA+LiberationSans                TrueType          WinAnsi          yes yes yes (可再分发拉丁字体，emb=yes)
```

- `pdftotext -layout` 实测：中文 finding 正文、`BCR-C-1.1-01` 等 finding ID、`GDPR Article 47(1)[1]` 脚注标记、`1. GDPR (EU) 2016/679 — Article 47(1)` 引用附录均可完整提取；断词碎片扫描 0 命中。
- 全量后端回归：`1092 passed, 3 failed`；3 项失败均为既有的 `cn/pipia` 与 `eu/scc_review` service 证据/引用展示标签用例（未改这些文件，与 T06 无关）。
- 新增 `backend/common/reporting/renderers/pdf.py`、`render_profiles/__init__.py`（字体盘点扩展）、`tests/test_pdf_renderer.py`，并在 `renderers/__init__.py` 与 `reporting/__init__.py` 导出 PDF 入口。
- 未改 service 用户产物路径（T09）、未做远程连接/部署、未写入用户正文/敏感内容；PDF 中文字体嵌入门禁保持 `BLOCKED_BY_FONT`，不伪造 PASS。

---

## 二十二、T07 落实记录（2026-08-13）

### 22.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| render manifest 契约 | `backend/common/reporting/render_manifest.py` | `RenderManifest`（Pydantic `extra="forbid"`，`manifest_version`/`schema_version`/`compiler_version`/`renderer_version`/`profile_id`）+ `ArtifactRecord`/`DocxStats`/`PdfStats`/`FontRecord`/`ClauseIdentity`/`GateResult` |
| 全量渲染编排 | 同上 `build_render_manifest(document, registry, output_dir, *, module, task_id)` | 编译门禁 fail-closed → 渲染 Markdown/DOCX/PDF + canonical `document_ir.json` + 自洽 `citation_map.json` → 写产物 → 原子写 `render_manifest.json`；编译被拒时不产出任何产物 |
| canonical IR 指纹 | 同上 `_canonical_ir_json`/`_sha256_bytes` | 确定性 `model_dump(mode="json")` 序列化 → SHA-256；渲染前后各算一次，断言 renderer 未改 IR（`ir_hash_immutable` gate） |
| 跨格式等价证明 | 同上 `_evaluate_equivalence`/`_extract_*` | section/finding/citation 按序、clause 按存在性，在 Markdown/DOCX/PDF 三种产物上重提取并比对 IR 集合与顺序 |
| 磁盘重校验 | 同上 `verify_render_manifest(manifest, output_dir)` | 重读产物重算 SHA-256（`artifact_hash` gate）+ 重提取 finding 顺序（`equivalence` gate），供篡改/删 finding/改顺序验收 |
| DOCX 定版化 | `renderers/docx.py::_normalize_zip_timestamps` | `python-docx` 给每个 zip 条目盖 `time.localtime()`（秒级）导致跨秒边界字节漂移；渲染后以固定 `(1980,1,1,0,0,0)` 重打包，使 DOCX 字节 hash 稳定 |
| 单测 | `backend/common/reporting/tests/test_render_manifest.py` | 11 项：产物/身份齐全、原子写、DOCX/PDF 统计、等价+hash gate 全过、PDF 字体 gate 保持 blocked、IR hash 与非时间字段稳定、篡改产物 hash 失败、删/改序 finding 等价失败、无正文/密钥敏感残留、编译被拒不写产物 |
| BCR 集成 | `backend/domains/eu/bcr_review/tests/test_schema_first_renderer.py::test_bcr_ir_builds_render_manifest_with_equivalence` | 真实 BCR v4 IR 全量渲染 manifest `success`，section/finding 集合与 IR 一致，equivalence pass，PDF 字体 gate blocked |

### 22.2 关键设计决策

- **canonical IR 是唯一真相源**：manifest 记录 `canonical_ir_sha256`，并在渲染前后各算一次（`ir_hash_immutable` gate），证明 renderer 不改 IR；三个产物 hash 各自入库。
- **等价证明用「可提取身份」而非 node id**：renderer 输出只含展示文本（finding ID、section 标题、clause 文本、`N. title` 引用附录），不含 clause node id；因此跨格式等价用按序 section/finding/citation + 按存在性 clause 重提取，clause 的顺序身份由 manifest 的有序 `clause_identities`（node_id + 逻辑编号 + 文本）在 IR 层固化。
- **clause 用存在性而非首现顺序**：BCR adapter 把 finding 的 `suggested_revision` 同时派生为 ClauseGroupBlock，clause 文本会在 finding 字段与条款块各出现一次，首现顺序会误报；故 clause 只证明「集合相等」，顺序由 manifest 清单承担。
- **原子写 + 最后写 manifest**：产物全部落盘后才 `os.replace` 写 `render_manifest.json`，半途渲染不可能留下「success」manifest；编译被拒时连产物都不写。
- **DOCX 字节确定性**：补齐 `_normalize_zip_timestamps`，否则 manifest 的 DOCX SHA-256 会在跨秒边界漂移，无法满足「非时间字段稳定」验收。
- **字体门禁不伪造**：PDF CJK 无资产时 `pdf_font_embedding` gate 保持 `blocked`（`BLOCKED_BY_FONT`），从不上报 `pass`。

### 22.3 T07 验收对照

- [x] 篡改任一产物后 hash 门禁失败（`test_tampered_artifact_fails_hash_gate`）
- [x] 删除 finding 或改变顺序后等价门禁失败（`test_deleted_finding_fails_equivalence_gate` / `test_reordered_finding_fails_equivalence_gate`）
- [x] 同一输入重跑 manifest 的非时间字段稳定（`test_canonical_ir_hash_and_non_time_fields_stable`，排除 `generated_at`）
- [x] manifest 不含 token、上传正文或敏感配置（`test_manifest_contains_no_finding_body_or_secrets`，finding 正文/密钥标记均不入 manifest）

### 22.4 验证命令与结果

```bash
uv run --frozen pytest -q backend/common/reporting/tests/test_render_manifest.py
# 11 passed

uv run --frozen pytest -q backend/common/reporting/tests backend/common/citation/tests \
  backend/tests/contracts/test_schema_first_contracts.py \
  backend/domains/eu/bcr_review/tests \
  backend/api/v1/tests/test_artifacts_access.py \
  backend/api/v1/tests/test_artifact_report_ir.py
# 330 passed
```

- 真实 BCR IR 实测：`build_render_manifest` 输出 `render_status=success`；8 section / 3 finding / 3 citation 集合与 IR 一致；DOCX 38 段落 / 12 标题 / 4 表 / 10 编号定义；PDF 2 页、`STSong-Light emb=no` + `LiberationSans emb=yes`、finding ID 全提取；`pdf_font_embedding` gate 保持 `blocked`。
- 新增 `backend/common/reporting/render_manifest.py`、`tests/test_render_manifest.py`；在 `reporting/__init__.py` 导出 manifest 契约与 `build_render_manifest`/`verify_render_manifest`；`renderers/docx.py` 增加 ZIP 时间戳定版化。
- 未改 service 用户产物路径（T09）、未做远程连接/部署、未写入用户正文/敏感内容；PDF 中文字体嵌入门禁保持 `BLOCKED_BY_FONT`。

---

## 二十三、T08 落实记录（2026-08-13）

### 23.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| 排版契约 verifier | `backend/common/reporting/layout_gate.py` | 可复用负向检查：`broken_word_fragments`（异常断词 HIG/ME/DIU/-C-1/.1）、`six_column_table`（六列表格退化）、`markdown_table_residue`（Markdown 分隔残留）、`numbering_sequence_diagnostics`（编号重置）、`duplicate_body_diagnostics`（重复正文）；测试与 CLI 门禁共用同一套检查 |
| 对抗样本回归 | `backend/common/reporting/tests/test_layout_contract.py` | 14 项：18 finding 压力样本、中英文长标题、超长英文法规名不逐字符断、三级以上条款编号、finding 跨页身份保持、缺可选字段无空标签、空 finding、未知 block/profile fail-closed、损坏 IR 被 Pydantic 拒绝、跨格式集合等价，以及 4 项变异测试 |
| 变异测试 | 同上 `test_mutation_*` | 注入缺陷后 verifier 必须拦截：编号重置（`(b)`→`(a)`）、表格退化（插入六列表头）、重复正文（重复 finding 语句）、异常断词（HIG\nH / ME\nDIU\nM）；正确产物保持 clean |
| CLI 门禁脚本 | `scripts/check_report_artifacts.py` | `--ir <document_ir.json>` 渲染 Markdown/DOCX/PDF + manifest，重跑全部 verifier 与 manifest hash/equivalence，产出 `status/check/task067/structural_checks.json` 证据；exit 0 pass / 1 fail / 2 blocked |

### 23.2 关键设计决策

- **负向检查独立成模块**：`layout_gate` 的每个 verifier 返回诊断列表（空=通过），测试与 CLI 脚本复用同一套判断，避免「测试里一种正则、脚本里另一种」的漂移。
- **变异测试证明「可重复失败」**：不做只断言文件存在/大于 0 字节/`%PDF` magic 的弱断言；而是在正确产物上注入四类缺陷并断言对应 verifier 拦截，正面回答方案 §T08「mutation tests 能拦截编号重置、表格退化、重复正文和异常断词」。
- **等价锚点做空白归一化**：长英文法规名在 PDF 会按行换行，manifest 等价门禁改为 `re.sub(r"\s+", " ", ...)` 归一化后再 `find`，避免把正常换行误报为「缺失/顺序错误」。
- **Markdown 与 DOCX/PDF 门禁区分**：Markdown 合法含 `| --- |` 表格分隔，故「分隔残留」门禁只施加于 DOCX/PDF；Markdown 施加「无六列表格退化」门禁。
- **CLI 脚本退出码分层**：`blocked`（字体门禁）> `fail`（结构门禁）> `pass`，与 T00 脚本的 `BLOCKED_BY_TOOLING=2` 语义一致，字体缺口绝不伪装成 pass。

### 23.3 T08 验收对照

- [x] 在注入缺陷 fixture 上测试失败（`test_mutation_numbering_reset_is_detected` / `test_mutation_table_degradation_is_detected` / `test_mutation_duplicate_body_is_detected` / `test_mutation_word_break_is_detected`）
- [x] 新 renderer 上测试通过（14 项 layout_contract 全过；CLI 脚本实测 15 个结构/等价/hash 门禁 pass，仅字体门禁 blocked）
- [x] mutation tests 拦截编号重置、表格退化、重复正文、异常断词（见 23.2）
- [x] 测试不依赖真实模型/网络调用（纯 IR 内存构造 + 本地 renderer + 本地 pypdf/python-docx/pdftotext）

### 23.4 验证命令与结果

```bash
uv run --frozen pytest -q backend/common/reporting/tests/test_layout_contract.py
# 14 passed

uv run --frozen pytest -q backend/common/reporting/tests backend/common/citation/tests \
  backend/tests/contracts/test_schema_first_contracts.py \
  backend/domains/eu/bcr_review/tests \
  backend/api/v1/tests/test_artifacts_access.py \
  backend/api/v1/tests/test_artifact_report_ir.py
# 344 passed
```

- CLI 门禁实测：`scripts/check_report_artifacts.py --ir <document_ir.json>` 渲染出 5 个产物（IR/MD/DOCX/PDF/citation_map），15 个门禁中 compile / ir_hash_immutable / required_artifacts / equivalence / six_column_table×3 / broken_fragments×2 / markdown_residue×2 / artifact_hash / equivalence 全 pass，`pdf_font_embedding` 保持 `blocked`，exit 2（`BLOCKED_BY_FONT`）。
- 新增 `backend/common/reporting/layout_gate.py`、`tests/test_layout_contract.py`、`scripts/check_report_artifacts.py`；`render_manifest.py` 等价门禁补空白归一化。
- 未改 service 用户产物路径（T09）、未做远程连接/部署、未写入用户正文/敏感内容；PDF 中文字体嵌入门禁保持 `BLOCKED_BY_FONT`。

---

## 二十四、T09 落实记录（2026-08-13）

### 24.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| 影子渲染/审计模块 | `backend/domains/eu/bcr_review/shadow_render.py` | `render_ir_artifacts`（复用 `build_render_manifest` 全量渲染到指定目录）、`audit_ir_vs_legacy`（finding/section/citation 集合比对，差异只登记不改结论）、`write_audit_json`（`CollectionAudit` Pydantic 契约原子落地，不含用户正文/token/secret） |
| 本地切换开关 | `backend/core/settings.py` | 新增 `bcr_report_ir_rendering_enabled: bool = False`；仅当 `schema_first_bcr_enabled` 也为真时生效，切回 false 即回滚 |
| service 接线 | `backend/domains/eu/bcr_review/service.py` | `_render` 与 `_render_document_driven` 双路径：阶段 A 影子渲染到 `shadow_ir/`（不登记、不改变用户 artifact 集合）；阶段 B 切换后正式 `output_files` 指向 IR renderer（`report.md/docx/pdf` + `citation_map.json` + `document_ir.json` + `render_manifest.json`），legacy 模板停放到 `shadow_legacy/`；切换失败 fail-closed（`manifest.render_status != success` 即抛错）；`render_audit.json` 显式记录 `rendering_mode`/`shadow` |
| 回归测试 | `backend/domains/eu/bcr_review/tests/test_shadow_render.py` | 7 项：audit 集合相等/缺失双向记录/无敏感内容、阶段 A 影子运行不改 artifact 集、阶段 B 切换指向同一 IR、无 schema_first 时开关不生效、回滚不删除已生成 IR/manifest |

### 24.2 关键设计决策

- **影子目录与正式目录解耦**：`outputs/bcr/{task_id}/outputs/` 只放正式产物；`shadow_ir/` 放非切换时的 IR 版本，`shadow_legacy/` 放切换后被停用的 legacy 模板。三个目录互不覆盖，回滚无需清理。
- **切换 = 目录选择，而非条件删除**：`ir_switch = schema_first_enabled and report_ir_rendering_enabled` 只决定「legacy 写哪个目录、IR 写哪个目录、`output_files` 指哪个目录」，不删除任何历史文件，天然满足「回滚不破坏已完成任务」。
- **审计只登记、不改法律结论**：`audit_ir_vs_legacy` 返回 `finding_ids_equal` + `missing_in_ir`/`missing_in_legacy` + `differences` 文本，调用方从不据差异改写 finding；PDF 字体门禁仍如实 `blocked`。
- **切换状态可追溯**：切换后 `render_manifest.json`（正式登记）+ `render_audit.json`（`rendering_mode="ir"`, `shadow=false`）双重可见；非切换时 `render_audit.json`（`rendering_mode="legacy"`, `shadow=true`）落 `shadow_ir/`。

### 24.3 T09 验收对照

- [x] 影子运行不改变用户 artifact 数量（阶段 A：`document_ir_json`/`render_manifest_json` 均不在 `output_files`，`test_stage_a_shadow_run_keeps_user_artifact_set_unchanged`）
- [x] 本地切换后所有正式文件来自同一 IR（阶段 B：`output_files` 六项全部指向 `outputs/report.*` + manifest，`test_stage_b_switch_points_official_outputs_at_ir`）
- [x] ZIP 不混入 legacy 与新版本（切换后不产出 legacy ZIP，`"zip" not in outputs`；非切换 ZIP 只含 legacy 模板产物）
- [x] 回滚不破坏已完成任务（`test_rollback_flag_off_restores_legacy_and_keeps_ir_artifacts`：切回 false 后 legacy 重新正式、已生成 IR/manifest 仍在）
- [x] 切换状态在 run/render manifest 可见（`render_manifest.json` + `render_audit.json` 的 `rendering_mode`/`shadow` 字段）

### 24.4 验证命令与结果

```bash
uv run --frozen pytest -q backend/domains/eu/bcr_review/tests/test_shadow_render.py
# 7 passed

uv run --frozen pytest -q backend/domains/eu/bcr_review/ backend/common/reporting/
# 145 passed（BCR 39 + reporting 106）
```

- 新增 `backend/domains/eu/bcr_review/shadow_render.py`、`tests/test_shadow_render.py`；`settings.py` 增 `bcr_report_ir_rendering_enabled`；`service.py` 双路径接线影子/切换/回滚。
- 未做远程连接/部署、未写入用户正文/敏感内容（audit/manifest 只含结构化 ID 与 hash）；PDF 中文字体嵌入门禁保持 `BLOCKED_BY_FONT`，DOCX 视觉验收仍由 T10 的 LibreOffice 工具门禁决定。

---

## 二十五、T10 落实记录（2026-08-13）

### 25.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| 视觉验收脚本 | `scripts/run_bcr_visual_acceptance.py` | 三个固定案例经 IR renderer 全量渲染后，分别跑 PDF 逐页栅格、DOCX 结构、headless 浏览器三 viewport 截图；退出码 0 pass / 1 fail / 2 无可运行工具 |
| 浏览器截图 helper | `scripts/_t10_browser.py` | playwright（anaconda python）加载自包含报告 HTML，三 viewport 截图 + console/network 采集 + 无横向滚动/无重复正文/章节-finding-条款计数断言 |
| 报告正文 HTML 契约测试 | `backend/common/reporting/tests/test_visual_html_contract.py` | 5 项：`_number_token` 与 React `ClauseTree` 编号一致、多级条款无重置、三案例 12 类 block 穷尽无未知块、finding 卡片 + 长英文法规名完整、风险文本徽标（非纯色） |
| 视觉证据 | `status/check/task067/{bcr_controller_basic,bcr_high_risk_health,issue067_18_finding_stress}/` | 每案例 `artifacts/`（IR/MD/DOCX/PDF/citation_map/render_manifest）、`pdf/pages/*.png` + `pdfinfo/pdffonts/text_layout`、`docx/structure.json`、`web/{1440,1280,390}_report.png` + `browser_result/console/network.json` + `case_summary.json` |
| 汇总证据 | `status/check/task067/visual_acceptance.json` | 三案例 PDF/DOCX/web 结论 + `blocked_tooling` + 每案例 task_id/IR SHA-256/artifact SHA-256 关联 |

### 25.2 关键设计决策

- **报告正文 HTML 与 React `ReportDocumentView` 1:1 对齐**：独立脚本用同一 `report.css` + 同一 block→DOM 映射（`ir-section`/`ir-paragraph`/`ir-table`/`finding-card`/`clause-tree`/`ir-citation-note`…），使 headless 截图在真实 viewport 下验证的正是该版式契约；`test_visual_html_contract.py` 把「block 穷尽 + 条款编号 + 卡片徽标」锁进断言，防止未来 block 静默丢失。
- **DOCX 逐页栅格诚实阻断**：`soffice`/`libreoffice` 缺失 → `BLOCKED_BY_TOOLING`，仍产出 `structure.json`（标题/表格/编号统计），绝不把「无页面 PNG」伪装成 pass，也不因缺失关闭 task067。
- **PDF 中文字体如实分层**：`pdffonts` 解析出 `STSong-Light emb=no`（CJK 阻断）、`LiberationSans emb=yes`（拉丁嵌入）；`cjk_font_gate=blocked` 只记录不伪造；`layout_gates` 三检查（六列表格退化/异常断词/Markdown 分隔残留）全 pass。
- **浏览器视觉只做可本地复现的断言**：无页面级横向滚动（`scrollWidth <= clientWidth`）、无重复正文（标题在 body 仅一次）、finding/条款计数、console 无 error；PDF tab 切换 / DOCX 下载 / 刷新重开等 SPA 场景由既有 `frontend/tests/e2e/bcr-local-firstrun.e2e.ts` + `ReportDocumentView.test.tsx` 覆盖，独立脚本不重复造后端栈。

### 25.3 T10 验收对照

- [x] 三 viewport 通过（1440×900 / 1280×800 / 390×844，三案例共 9 张截图，`web.violations=[]`）
- [x] 两个共享案例 + 压力 fixture 通过（`bcr_controller_basic` / `bcr_high_risk_health` / `issue067_18_finding_stress` 18 finding）
- [x] DOCX 与 PDF 逐页检查完成（PDF 2/5/7 页逐页 PNG + 结构门禁；DOCX 结构完成、页面栅格 `BLOCKED_BY_TOOLING`）
- [x] 无未捕获前端错误（`browser_console.json` 为空、`console_errors=[]`）
- [x] 截图与 JSON 记录能关联 task ID、IR hash、artifact hash（`case_summary.json` 含 `task_id` + `canonical_ir_sha256` + 每 artifact `sha256`）
- [x] LibreOffice 缺失时不得关闭 task067（`BLOCKED_BY_TOOLING` 记入 `blocked_tooling`，exit 0 非致命）

### 25.4 验证命令与结果

```bash
uv run --frozen pytest -q backend/common/reporting/tests/test_visual_html_contract.py
# 5 passed

uv run --frozen python scripts/run_bcr_visual_acceptance.py --out status/check/task067
# 3 案例 render=success；PDF PASS（2/5/7 页栅格，cjk=no/latin=yes，layout_gates 全 pass）；
# DOCX structure 完成 + visual BLOCKED_BY_TOOLING；web 三 viewport PASS、violations=[]；exit 0
```

- 新增 `scripts/run_bcr_visual_acceptance.py`、`scripts/_t10_browser.py`、`backend/common/reporting/tests/test_visual_html_contract.py`；证据落 `status/check/task067/`。
- 未做远程连接/部署、未写入用户正文/敏感内容（证据只含结构化 ID/hash/截图；`grep -ril api_key/token/secret/.env` 为空）；PDF 中文字体嵌入门禁保持 `BLOCKED_BY_FONT`，DOCX 视觉保持 `BLOCKED_BY_TOOLING`，均不伪造 PASS。

---

## 二十六、T11 落实记录（2026-08-13）

### 26.1 已交付

| 交付物 | 路径 | 说明 |
|---|---|---|
| 验收报告 | `status/check/task067/task067_验收报告.md` | 诚实结论：T00-T10 完成、T11 回归完成、旧路径退出 `BLOCKED_BY_TOOLING`+`BLOCKED_BY_FONT`，task067 未标记完成；含解除条件与下一步 |
| 结构门禁证据 | `status/check/task067/structural_checks.json` | 18 finding 压力样本 `render_status=success`，compile/ir_hash/required_artifacts/equivalence/six_column×3/broken×2/residue×2/artifact_hash 全 pass，`pdf_font_embedding=blocked` |
| 顶层 render manifest | `status/check/task067/render_manifest.json` | `document_id=bcr:t10-18-finding-stress`、18 finding / 7 section、compile/ir_hash/required/equivalence pass、字体 blocked |
| 前端构建修复 | `frontend/src/lib/use-report-ir.ts` | 修复 `useReportIr(taskSpaceId, module)` 的 `module` 未使用 TS6133：补入 artifact 过滤与依赖数组，`npm run build` 通过 |

### 26.2 公共回归结果（§T11 回归范围）

| 范围 | 结果 |
|---|---|
| §9.1 `reporting + render + bcr_review` | 176 passed |
| §9.2 `artifact API + contracts + citation` | 206 passed |
| 合并 `reporting+citation+contracts+BCR+artifacts` | 356 passed |
| 后端全量 `pytest backend/` | 1132 passed / 3 failed（`cn/pipia`×2、`eu/scc_review`×1，与 task067 无关） |
| 前端 report/css 组件 | 9 passed |
| 前端全量 `npm test` | 187 passed / 2 failed（`cn/pipia`、`cn/scc` dev-case fixture，与 task067 无关） |
| 前端 `npm run build` | 通过 |
| `git diff --check` | 通过（顺带修复 `knowledge/ingestion_pipeline.py` pre-existing 尾随空格） |
| 证据敏感内容扫描 | 无 token/`.env`/secret/password/真实用户上传正文 |

### 26.3 旧路径退出：为何 BLOCKED

方案 §十 Checkpoint D 要求「LibreOffice 和字体门禁**真实通过**」后才退出 BCR 旧正式路径；本机 `soffice` 缺失且无可再分发 CJK 字体，按 §十一 第 7、8 条不得声称通过，故**不执行**：

- 不把 `bcr_report_ir_rendering_enabled`/`schema_first_bcr_enabled` 默认改 `true`；
- 不删除 BCR 正式路径对 `_build_markdown_table()`/`render_docx_template()`/`render_markdown_template()`/`from_template()` 的依赖；
- 不删除/改判旧模板（其他模块可能复用公共 legacy 函数，§T11 禁止未覆盖前改动）。

当前可见路径满足 §十一 第 13 条「新旧路径不同时对用户可见」：默认双 flag `false` 只可见旧模板（IR 影子渲染）；双 flag `true` 只可见 IR（旧模板停 `shadow_legacy/`）。§9.7 扫描结果已人工区分：`service.py` 中 `_build_markdown_table`/`render_*_template`/`from_template` 属当前正式路径（flag off）仍在运行，属「正式运行路径」，非残留。

### 26.4 T11 验收对照

- [ ] BCR 用户产物只有一条正式生成路径 — **未满足**（受 Checkpoint D 阻断，见 26.3）
- [ ] 旧 BCR 输出路径扫描为 0 — **未满足**（旧模板仍为默认正式路径）
- [x] 公共回归全部通过（后端 1132/3 无关、前端 187/2 无关、合并 356 全过）
- [x] `git diff --check` 通过
- [x] 证据不含 token、`.env` 或真实用户上传正文
- [x] 更新 issue067/task067 状态和 `status/check/task067/` 验收报告（本报告 + §十四）
- [x] 未连接远程、未部署

### 26.5 验证命令与结果

```bash
uv run --frozen pytest -q backend/common/reporting/tests backend/common/render/tests backend/domains/eu/bcr_review/tests
# 176 passed
uv run --frozen pytest -q backend/api/v1/tests/test_artifacts_access.py backend/api/v1/tests/test_artifact_report_ir.py backend/tests/contracts/test_schema_first_contracts.py backend/common/citation/tests
# 206 passed
uv run --frozen pytest -q backend/
# 1132 passed, 3 failed（cn/pipia ×2、eu/scc_review ×1，均与 task067 无关）
cd frontend && npm run build && npm test -- --run
# build 通过；187 passed, 2 failed（cn/pipia、cn/scc fixture，无关）
git diff --check
# 通过
```

- 解除条件（缺一不可）：① 引入已授权+登记 hash 的可再分发 CJK 字体使 `pdf_font_embedding` → pass；② 安装 LibreOffice headless 使 DOCX 视觉 → pass；③ 复查 Checkpoint D 后默认切双 flag `true`、旧模板归档、删除影子 flag 分支。
- 未做远程连接/部署、未伪造 DOCX/PDF 视觉 PASS、未写入用户正文/敏感内容。

## 二十七、收尾（2026-08-10）

`好，开始收尾` 指令落地：按 §T11「删除临时影子 flag 分支**或明确设定短期移除日期**」的替代项，在环境门禁仍 BLOCKED（LibreOffice 缺失 + 无可再分发 CJK 字体）无法实际删除影子分支的前提下，完成**短期移除日期的显式设定**，把收尾边界固化到代码与文档。

### 27.1 短期移除日期（已固化）

| 位置 | 内容 |
|---|---|
| `backend/core/settings.py` `bcr_report_ir_rendering_enabled` | 注释标注 `TEMPORARY shadow switch — REMOVE by 2026-09-01`，并列出移除三步（默认切 `true`、归档旧模板、删除 `shadow_legacy/shadow_ir` 目录分支） |
| `backend/domains/eu/bcr_review/service.py` `_render` | `ir_switch` 上方注释标注 `TEMPORARY shadow switch (task067 §T11) — REMOVE by 2026-09-01`，指向 settings.py 的移除步骤 |

移除日期取 **2026-09-01**（自今日 2026-08-10 起 3 周），条件为 §26.5 三项解除条件全部满足；到期若环境门禁仍阻塞，须在验收报告复核后再顺延并回写日期，不静默放任。

### 27.2 收尾动作清单

- [x] 影子 flag 明确设定短期移除日期（settings.py + service.py 双侧注释）；
- [x] 不删除公共 legacy 函数（其他模块仍复用，§T11 禁止未覆盖前改动）；
- [x] 不伪造旧路径退出 / 不宣称 `BLOCKED` 已解除；
- [x] 保持 §26.4 验收对照的诚实结论（旧路径退出两项「未满足」维持不变）。

### 27.3 收尾验证

```bash
git diff --check                      # 无新增空白错误
uv run --frozen ruff check backend/core/settings.py backend/domains/eu/bcr_review/service.py
uv run --frozen pytest -q backend/domains/eu/bcr_review/tests backend/common/reporting/tests
# 全绿（BCR 定向 + reporting 公共回归）
```

收尾不改动任何业务结论、Gold 数据、用户正文；不触碰远程/部署。
