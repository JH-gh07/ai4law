# 项目最新现状与事实基线更新（2026-08-13）

> 本文件为「当前项目现状」的权威快照，取代此前分散在 `CURRENT_AI4Law_项目事实基线与真实系统理解.md`、`CURRENT_DataComplyFlow_全功能运行验证与问题汇报_20260805.md` 中的过期事实。
> 数据采集口径：`config/` 注册表与清单、`backend/tests/` 全量 pytest、`runs/` 运行清单、`git log` 提交历史、`status/todo` 任务档案。

---

## 一、一句话结论

DataComplyFlow 已从「能跑通的合规报告生成器」演进为**11 模块、26 个 CLI 用例 / 603 条断言 / 28 个前端案例、全部 CLI 最新运行 PASS、后端全量 1032 通过**的工程化合规平台。近期焦点从「模块功能正确性」转向「三格式（MD/DOCX/PDF）渲染一致性、生产报告质量、前端状态持久化治理、async 任务重启恢复」。

---

## 二、模块全景（11 模块）

| 别名 | module_id | 主方法 | 规则引擎 | 智能体数 | CLI 案例/断言 | 前端案例 |
|---|---|---|---|---|---|---|
| diagnosis | cn.transfer_diagnosis | evaluate | decision_tree.json | 4 | 3/35 | 3 |
| assessment | cn.security_assessment | generate_report | WorkflowPipeline | 0 | 2/42 | 2 |
| pipia | cn.pipia | generate_report | 专项服务 | 0 | 5/140 | 3 |
| cn_flow | us.eo14117_flow_review | generate_report | 流量向 | 0 | 2/38 | 2 |
| eu_scc | eu.scc_review | generate_report | scc_rule_engine.py | 6 | 3/78 | 3 |
| bcr | eu.bcr_review | generate_report | 双通道 | 10 | 2/47 | 3 |
| dpia | eu.dpia | generate_report | need_detector | 9 | 2/50 | 2 |
| tia | eu.tia | generate_report | 结构化四层 | 3 | 2/70 | 2 |
| us_14117 | us.eo14117 | generate_report | rule_engine.py | 5 | 3/66 | 3 |
| cpra | us.cpra | generate_report | gap_rules.py | 4 | 1/28 | 3 |
| review | cn.document_review | generate_from_request | rulebook_loader | 专项组件 | 1/9 | 2 |
| **合计** | — | — | — | **41** | **26/603** | **28** |

> 数据源：`config/module_registry.json`（11 模块）、`config/case_inventory.json`（26 CLI / 603 断言 / 28 前端）、`config/dev_case_catalog.json`（28 案例）。

---

## 三、真实运行状态（runs/ 实测）

最新一次运行清单（`run_manifest.json`）全部 `PASS`，两种模式并存：
- **`no_llm`（确定性路径）**：diagnosis（0.4s）、pipia（2.0s）、us_14117（1.4s）——零 token、毫秒级。
- **`live`（LLM 路径，siliconflow / deepseek-ai/DeepSeek-V3.2）**：assessment（400s/50.3k token）、dpia（600s/42.9k）、review（480s/20.0k）、tia（267s/19.4k）、cn_flow（222s/16.9k）、cpra（266s/12.8k）、eu_scc（163s/8.9k）、bcr（154s/6.7k）。

详见同目录 `20260813_各模块CLI功能代码流程逻辑原理与数据运行真实情况.md`。

---

## 四、测试规模与结果

| 层 | 规模 | 结果 |
|---|---|---|
| 后端 pytest | 158 个 `test_*.py` | **1032 passed / 3 failed** |
| 前端 vitest | 27 个 `*.test.ts(x)`（lib/api/components/pages/features） | 通过（详见下方说明） |
| CLI harness | 26 案例 / 603 断言 | **全部 PASS（100%）** |
| 前端行为案例 | 28 案例 | 已登记，待全量回跑 |

- 后端 3 个失败为**历史遗留 golden IR fixture 失败**（`test_scc_evidence_drives_source_findings`、`test_certification_evidence_drives_path_findings`、`test_uploaded_scc_document_drives_core_review`），来自此前「报告迁移」上报方式变更，**与 task061 无关**（已通过 `git diff --name-only` 确认不在 task061 变更集内）。
- 后端测试目录 `backend/tests/<module>/` 与 CLI 案例目录 `backend/tests/<module>/cases/` 并存，分别承载单元测试与白盒 CLI 案例。

---

## 五、近期关键工程进展（2026-08-10 → 08-13）

1. **async 任务状态持久化（task061 阶段四，本会话核心）**
   - 新增 `backend/models/async_task.py`（`async_tasks` 表）；`backend/core/db.py` 的 `init_db()` 纳入建表。
   - `backend/common/tasks/manager.py` 新增 `configure_task_persistence(session_factory)` 与 `_persist_status()`，在 CREATED/RUNNING/COMPLETED/FAILED/CANCELED 全状态转换点 best-effort 落库。
   - `backend/api/v1/endpoints/me.py` 的 `list_my_tasks()` 通过 `AsyncTaskModel ⋈ TaskOwnershipModel` 还原用户任务清单，服务重启后不再「Task not found」；`delete_project_history()` 同步删除 async 记录。
   - `backend/schemas/me.py` 的 `MyTaskItem` 增加 `error` 字段，失败态可下钻错误原因。

2. **SCC FAIL 残留跨模块防护（task061 阶段一/二/三/五/六）**
   - `IssueItem` 平台级 `@field_validator` 守卫空 `recommended_action`；SCC/DPIA/EO14117/PIPIA/SecurityAssessment 各 `_issue` 均加空值守卫。
   - 核验脚本 `scripts/verify_task061.sh`（7 项检查）7 PASS / 0 FAIL。

3. **resources/new 全量迁移（Phase 5 完成）**
   - 旧目录 `resources/new` 已删除（commit `5590843f`），资源分层迁移到 `resources/` 正式目录并登记；执行记录见 `status/check/DataComplyFlow_RESOURCES-NEW全量迁移执行记录_20260812.md`。

4. **统一 Scenario 共享基准迁移**
   - 全模块案例输入/期望收敛到 `benchmarks/cases/<module>/<scenario>/{scenario.json,expected.json}`，前端与 CLI 案例同源；验收记录见 `status/check/DataComplyFlow_统一Scenario全模块迁移验收_20260811.md`。

5. **进行中（未完成）任务**
   - task067：BCR 三格式专业排版与统一 IR 渲染。
   - task068：文档审查及多模块三格式同源渲染与输入追溯。
   - task063/064/065/066：工作台目录显示、evidence center 重构、全模块本地闭环与生产报告质量、前端状态持久化超限治理。

---

## 六、关键路径速查

| 关注点 | 路径 |
|---|---|
| 模块注册表 | `config/module_registry.json` |
| 案例清单 | `config/case_inventory.json` |
| 开发案例目录 | `config/dev_case_catalog.json` |
| CLI 执行器 | `backend/tests/harness/runner.py` |
| 断言器 | `backend/tests/harness/validators.py` |
| 流水线 | `backend/common/workflow/pipeline.py` |
| async 任务 | `backend/common/tasks/manager.py` + `backend/models/async_task.py` |
| 共享基准 | `benchmarks/cases/<module>/<scenario>/` |
| 运行清单 | `runs/<module>/<run_id>/run_manifest.json` |
| 任务档案 | `status/todo/`、`status/check/`、`status/view/` |

---

## 七、事实基线变更对照（旧 → 新）

| 旧事实（07-13 / 08-05） | 新事实（08-13） |
|---|---|
| 290 passed / 18 failed / 17 errors | **1032 passed / 3 failed** |
| 15 个 CLI 案例 | **26 个 CLI 案例 / 603 条断言** |
| 26 个前端案例 | **28 个前端案例** |
| SCC uuid 断裂、CN Flow per_issue_rag 断裂 | **已修复，全模块 PASS** |
| `qa/` 路径 | **`benchmarks/cases/` 共享基准** |
| `resources/new` 未跟踪 | **已迁移删除，正式目录登记** |
| 无 `legal_index_intl` | **已建立国际法规索引** |
| async 任务不持久、重启丢状态 | **已持久化，重启可恢复** |

---

*本快照由 2026-08-13 会话生成；如需追溯单项细节，请以同目录 `20260813_各模块CLI功能代码流程逻辑原理与数据运行真实情况.md` 及 `status/todo/`、`status/check/` 对应档案为准。*
