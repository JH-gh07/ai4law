# issue071 — us_14117「🧪 测试案例」按钮重复（应只保留顶部）

> **日期**: 2026-08-15
> **严重程度**: P3（重复入口，非阻断，易造成交互混淆）
> **影响模块**: `us_14117`（EO 14117 行政令合规）Wizard
> **关联文件**: `frontend/src/components/workspace/ModuleRunPanel.tsx`

---

## 一、问题概述

`us_14117` 模块的 Wizard 里出现了 **两个功能完全相同的「🧪 测试案例」按钮**，都调用 `setShowCasePicker(true)` 打开同一个案例选择器，属于重复入口。其余模块均只有顶部一处，唯 `us_14117` 多出底部一处。

## 二、两处按钮的精确区分

### ① 前端顶头（应保留）

- **位置**: `ModuleRunPanel.tsx:2849`
- **上下文**: `<section className="schema-wizard">` 内，位于 `schema-wizard-head`（`EO 14117 Wizard` 标题）之前
- **样式**: `style={{ marginBottom: 12 }}`
- **代码**:
  ```tsx
  {DEV_ACCEL_ENABLED && devTestCases.length > 0 ? (
    <button type="button" className="pill-btn" onClick={() => setShowCasePicker(true)} style={{ marginBottom: 12 }}>🧪 测试案例</button>
  ) : null}
  ```

### ② 其他（底部运行条，应删除）

- **位置**: `ModuleRunPanel.tsx:2938`
- **上下文**: 位于底部 `schema-actions-row` 内，与「运行 EO 14117 评估」「一键运行」平齐
- **样式**: `style={{ marginLeft: 8 }}`
- **代码**:
  ```tsx
  {DEV_ACCEL_ENABLED && devTestCases.length > 0 ? (
    <button type="button" className="pill-btn" onClick={() => setShowCasePicker(true)} style={{ marginLeft: 8 }}>🧪 测试案例</button>
  ) : null}
  ```

## 三、结论

两处按钮**功能重复**（同为打开 `showCasePicker` 案例选择器）。按统一入口原则，**只保留顶部（`2849` 行），删除底部运行条内的一处（`2938` 行）**。

## 四、处置建议

1. 删除 `ModuleRunPanel.tsx:2938` 的底部「🧪 测试案例」按钮。
2. 保留 `ModuleRunPanel.tsx:2849` 的顶部按钮，与其他模块的 Wizard 顶部入口保持一致。
3. 底部 `schema-actions-row` 最终应只保留：`上一步 | 下一步 | 运行 EO 14117 评估 | 一键运行`。

## 五、对照：其他模块现状

| 模块 | 顶部「🧪 测试案例」 | 底部运行条「🧪 测试案例」 |
|---|---|---|
| `diagnosis` / `assessment` / `eu_scc` / `bcr` / `dpia` / `tia` / `pipia` / `review` / `cn_flow` / `cpra` | 有（各 1 处） | 无 |
| `us_14117` | 有（`2849`） | **有（`2938`，重复）** |
