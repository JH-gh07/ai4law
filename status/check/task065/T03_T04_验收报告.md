# task065 T03–T04 验收报告

> 日期：2026-08-13
> 范围：本地开发/测试环境；未连接远程，未部署

## T03：Seed Level B 模块身份裁决

### 3.1 裁决结果

50 个 seed case 的模块身份已裁决：

| mapping_status | 数量 | 说明 |
|---|---|---|
| partial | 40 | 已支持模块（task 1/2/3/5/9/10 原有 + task 4/6 裁决） |
| gap | 10 | 无产品能力（task 7/8） |

裁决明细见 `status/check/task065/seed-mapping-adjudication.md`。

### 3.2 裁决变更（相对冲突基线）

| task | 原 module_id | 原 status | 裁决后 module_id | 裁决后 status |
|---|---|---|---|---|
| 4（豁免情形诊断） | `""` | conflict | `cn.transfer_diagnosis` | partial |
| 6（TIA 审查） | `""` | conflict | `eu.tia` | partial |
| 7（GDPR 合规诊断） | `""` | conflict | — | gap |
| 8（BD ROD 判断） | `""` | conflict | — | gap |

### 3.3 Gap Ledger

`status/check/task065/seed-gap-ledger.json` 记录 2 个 gap：

- task 7：缺「GDPR 合规诊断」产品能力（七项诊断表单 ≠ DPIA/BCR/SCC/TIA）
- task 8：缺「GDPR 适用性与数据处理角色判断」产品能力

### 3.4 Level B request adapter

`scripts/build_seed_case_requests.py` 将 50 个 Level A 抽取记录裁决为 `gap` / `rejected` / `converted` 三类，并生成 `benchmarks/datasets/seed-cases-v1/levelb-disposition.v1.json`。

当前结果：**`rejected: 40`、`gap: 10`、`converted: 0`**。

`converted: 0` 不是 bug，而是 source-fidelity 抽取的直接结论：这 50 个 seed 原件是「模拟用户填写」的灰色问题陈述（`XX公司`、`张某`、`几万`、`≥1万`、无附件、无 SCC 原文等），关键事实被匿名化或缺失。按方案 §T03 要求 3「适配器必须拒绝关键事实缺失，不得使用默认公司、国家、人数、接收方或附件」，这些案例必须被拒绝而非伪造映射。

每个 supported 案例的 `blocking_missing_facts` 已逐条记录在 ledger 中，可作为后续「补齐事实后进入模块」的明确清单。

### 3.5 验收清单

- [x] 每个 seed case 有唯一 module_id 或明确 gap
- [x] gap 案例不进入默认 runner（`mapping_status=gap` 且 `module_id=""`）
- [x] 模块身份变更已更新脚本 `TASKS` 映射（`mapping_status: {'partial': 40, 'gap': 10}`）
- [x] 裁决理由与 reviewer 已记录
- [x] request adapter 已实现，适配器拒绝关键事实缺失（不伪造默认公司/国家/人数/接收方/附件）
- [x] gap 案例不进入默认 runner 且可追溯（`seed-gap-ledger.json` + `levelb-disposition.v1.json`）

---

## T04：正式案例总表与来源分类门禁

### 4.1 案例总表

`scripts/build_case_disposition.py` 生成 `status/check/task065/case-disposition.v1.json`，对账四个案例面：

| 案例面 | 数量 |
|---|---|
| 共享 Scenario（`benchmarks/cases/**/scenario.json`） | 22 |
| 前端案例（`config/dev_case_catalog.json`） | 28 |
| CLI 案例（`config/case_inventory.json`） | 26 |
| Seed 抽取记录（`benchmarks/datasets/seed-cases-v1/inputs/`） | 50 |

`validation_errors: 0`，所有 classification 均为合法枚举（`source_exact` / `source_derived` / `product_extension` / `compatibility_adapter` / `gap`）。

### 4.2 来源分类现状

- 22 个共享 Scenario：全部 `source_exact`，且均有 `expected.json`。
- 28 个前端案例：25 个 `source_exact`、1 个 `product_extension`（review-02）、2 个 `compatibility_adapter`（cn_flow-01/02 复用 us_14117）。
- 50 个 seed case：40 个 `partial`（已支持，分类待 Level B 转换后落入 source_exact/source_derived）、10 个 `gap`。

### 4.3 已确认缺口（需后续处置，不阻断本报告）

1. **TIA 正式案例二「集团内部从法国向英国传输 HR 数据（充分性决定下的复杂情况）」尚未进入共享 Scenario**。源文件 `“TIA草案生成”测试案例及预期输出.docx` 含 3 个案例，当前 Scenario 仅覆盖案例一（`innovate_crm_us_saas`）与案例三（`leiden_clinical_india`），案例二缺失。→ 待法域专家签署后补建 Scenario。

2. **Review 第二案例分类为 `product_extension`**。`review-02` 缺与文档专项审查正式案例的明确来源对应，已在 `dev_case_catalog.json` 明确标注。→ 待来源对应确认。

3. **PIPIA 附件 fixture 的来源分类需澄清**。issue065 分析指出「PIPIA 三例派生补值和附件 fixture 必须保留来源、原因和哈希，不能改写为 source_exact」。当前 `benchmarks/cases/pipia/*/scenario.json` 的 `classification` 均为 `source_exact`，但 `weilan_hr_exemption_us` 与 `zhifutong_eurocert_de` 的附件是派生的 `.txt` fixture（`backend/tests/pipia/fixtures/`），并非源 DOCX 原件。→ 需要裁决：核心业务事实 source_exact 与派生附件是否应分别登记。

4. **cn_flow 是兼容适配**。`cn_flow` 复用 `us_14117` 事实，分类为 `compatibility_adapter`，不应统计为新增独立业务案例。

### 4.4 业务事实深比较

Scenario / CLI / 前端请求的深比较由 `scripts/check_case_parity.py` 强制（603 leaf checks），当前通过。任何公司、国家、人数、接收方或附件 Hash 差异会使该门禁失败。

```bash
uv run --frozen python scripts/check_case_parity.py
# 11 modules / 26 CLI cases / 603 leaf checks / 28 developer cases，通过
```

### 4.5 验收清单

- [x] 正式案例总表已生成（`case-disposition.v1.json`），四个案例面均已枚举
- [x] 每个案例有唯一 disposition（或明确标注待裁决）
- [x] 深比较由 case parity 门禁强制且通过
- [x] TIA 案例二、Review 第二案例、cn_flow 兼容案例均已记录明确状态
- [x] PIPIA 派生附件分类需澄清已记录（见 4.3 缺口 3）

---

## Checkpoint A 状态

Checkpoint A（T01-T04）中：

- [x] T01-T02 全部通过（baseline 冻结 + seed Level A 修复）
- [x] 50 extraction records 已生成（`inputs/` 目录，50 个 `.input.json`）
- [x] 已支持案例 request schema 已固定（Level B request adapter 已实现，见 §3.4）
- [x] 后续不得改写事实字段（已冻结）
- [x] 失败回到 T03/T04 的路径已建立（mutation tests + 裁决文档）

**结论**：Checkpoint A 的前提工作（T01-T04）已全部完成。Level B 适配器以「拒绝缺失事实」为正确行为，40 个 supported seed case 因匿名化/缺失关键事实被逐条记录为 `rejected`，10 个 gap 案例保持可追溯。后续 T05+ 浏览器/报告验收使用已冻结的共享 Scenario（22 个 `source_exact`），不得在截图阶段临时补值。
