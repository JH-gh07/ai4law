# 引用跳转：移除 Drawer 直连知识库中心 — 验收记录

> 日期：2026-08-09
> 相关方案：`status/todo/引用跳转_移除Drawer直连知识库中心方案_20260809.md`
> 提交：`3c4793e`（删除 Drawer）+ `b9c7cd5`（fix URL 格式）

## 改动汇总

| 层面 | 改动 | 验证方式 |
|---|---|---|
| 前端组件 | 删除 `CitationArticleDrawer.tsx` + `.test.tsx` | 构建通过 |
| 前端组件 | `CitationMarkdownRenderer.tsx` — click → `navigate(/evidence?source=ID&article=N)` | 测试通过 (4 cases) |
| 前端页面 | `EvidenceCenterPage.tsx` — `useSearchParams` 读 `?source=`/`&article=` 参数，auto-expand + scrollIntoView | 构建通过，浏览器待验 |
| 前端 Popover | 保留 hover 浮层，加 title 提示 | 构建通过 |
| 后端核心 | `build_knowledge_url` → `/evidence?source=ID&article=N`（原 `/knowledge/laws/ID?article=N`） | 49 测试通过 |
| 后端知识投影 | `knowledge_projection.py` 硬编码 URL 同步改 | — |
| e2e 测试 | pipia/bcr/scc 三份 — 移除 drawer 断言，改断 evidence URL + 内容可见 | Playwright 待跑 |

## 回归结果

| 测试 | 结果 |
|---|---|
| 后端全量 | **881 passed, 0 new regressions**（4 个 flaky/非本次改动） |
| 前端单元测试 | **25/26 files, 129/131 tests**（1 skip: browser dep） |
| 前端构建 | **通过**（tsc + vite build） |
| `test_build_knowledge_url` | 5/5 参数组合断言 `/evidence?source=...` |
| `test_domain_citation_paths` | 4/4 模块 citation 路径断言 `/evidence?source=...` |
| `test_citations_api` | 1/1 API 返回 knowledge_url 字段断言新格式 |
| `test_citation_url_normalization` | 全 41 tests 中涉及 knowledge_url 的断言全部更新为新格式 |

## 跳转路径确认

```
[N] 脚注点击
  → citation.source_id + citation.article_no
  → `/evidence?source=CN-LAW-003&article=39`
  → EvidenceCenterPage: setSelectedSourceId + fetchArticleDetail + scrollIntoView

【依据：法规名 第X条】点击
  → buildResolvedCitation source_id + article_no
  → `/evidence?source=CN-LAW-003&article=13`
  → 同上
```

## 未验证项

| 项 | 说明 |
|---|---|
| 浏览器端跳转闭环 | 需启动前端 dev server + 后端，创建任务后点击 [N] 验证 URL 变化和条文定位 |
| e2e (Playwright) | 3 个 e2e 文件已改，待远端/本地 Playwright 环境运行 |
</EOF>
echo "验收记录写入完成"