# 阶段 3 TIA 本地首跑验收记录

> 日期：2026-08-08
> 功能：欧盟 TIA 草案生成
> 环境：本地 `new` 分支；未连接远端服务器
> 对应方案：`status/todo/DataComplyFlow_架构统一迁移与可核验实施方案_20260808.md` §12
> 判定：**部分完成**

## 1. 入口和输入

| 项目 | 事实 |
|---|---|
| 输入模型 | `backend/domains/eu/tia/schema.py::TIARequest` |
| 服务入口 | `backend/domains/eu/tia/service.py::TIAService.generate_report` |
| 渲染入口 | `backend/domains/eu/tia/report_renderer.py::TIAReportRenderer.render` |
| Schema-first 开关 | `AI4LAW_SCHEMA_FIRST_TIA_ENABLED`，默认关闭 |
| 固定案例 | `backend/tests/tia/cases/01_minimal.json` |
| 输入 SHA-256 | `87f4a138f905a936dafc1926a0c068481ab18f5fe19c6fc8a40b8c87c6c8acb1` |
| 附件 | `storage://uploads/scc_agreement.pdf`，是虚构 URI，不是真实文件 |

## 2. no-LLM 服务首跑

使用同一固定输入分别运行旧流程和 `AI4LAW_SCHEMA_FIRST_TIA_ENABLED=true` 新流程，证据在本目录 `old/`、`new/`，摘要在 `new/run_evidence.json`。

| 检查 | 结果 |
|---|---|
| 是否调用真实 service | 是，`TIAService.generate_report` |
| 新流程耗时 | 1852 ms |
| LLM 调用/token | 0 次 / 0 token（明确关闭 LLM） |
| Compiler | `success`，`diagnostics=[]` |
| DocumentIR | 6 sections、7 blocks、0 diagnostics |
| 新流程产物 | MD、DOCX、PDF、ZIP、`document_ir.json`、`citation_map.json` |
| 旧流程产物 | MD、DOCX、PDF、ZIP、`citation_map.json` |
| 新旧正文内容 | 固定案例 MD/DOCX/PDF 内容对比通过 |
| ZIP 差异 | 新 ZIP 仅额外包含内部 `document_ir.json` |
| CitationMap | `all_items=2`，`footnote_map=0`；固定案例没有正文脚注 |
| 附件解析 | 记录 `File not found: storage://uploads/scc_agreement.pdf`，因此不构成真实附件验收 |

这证明 no-LLM 服务、开关、DocumentIR、Compiler 和产物落盘可以跑通，但没有证明正文引用闭环，也没有证明真实文件解析。

## 3. LLM 模式现场

另一次本地运行使用 provider 配置，task id 为 `phase3-tia-service-20260808`，trace 在 `trace/`：

| 序号 | 事件 | 结果 |
|---:|---|---|
| 1-2 | 状态、路径判断 | 已执行 |
| 3-4 | RAG 查询规划 | provider 返回 749 tokens |
| 5 | 规划结果 | 已进入下一步 |
| 6-7 | 附件证据审查 | provider 返回 906 tokens |
| 8 | 附件审查结果 | 输出 4 项 |
| 9 | 第一章生成 `tool_start` | 开始后长时间无新事件，已停止 |

该次没有生成完整 MD/DOCX/PDF/ZIP、DocumentIR、CitationMap 或 `final` 事件。因此 LLM 模式不能算成功首跑；已确认 token 仅为 1655（749+906），不是完整运行总量。

## 4. 验收表

| 检查项 | 结果 | 证据 |
|---|---|---|
| 输入 Schema | 通过 | `TIARequest` |
| 真实 service 调用 | no-LLM 通过；LLM 部分通过 | `new/run_evidence.json`、`trace/` |
| RAG | no-LLM 复用索引；LLM 规划已调用 | `new/run_evidence.json`、trace 3-5 |
| 得理 API | 未执行 | TIA 路径没有得理调用证据 |
| Token | no-LLM=0；LLM 部分记录 1655 | `new/run_evidence.json`、trace 4/7 |
| 完整耗时 | no-LLM 1852ms；LLM 未完成 | `new/run_evidence.json` |
| DocumentIR/Compiler | 通过（no-LLM） | `new/document_ir.json`、`new/run_evidence.json` |
| CitationMap | 已生成，但正文脚注为 0 | `new/citation_map.json` |
| MD/DOCX/PDF/ZIP | 通过（no-LLM old/new） | `old/`、`new/` |
| 真实附件解析 | 未通过 | fixture URI 不是真实文件 |
| 回退验证 | old/new 均有产物 | `old/`、`new/` |
| 浏览器引用跳转 | 未执行 | 没有浏览器截图和网络日志 |

## 5. 当前问题和代码位置

| 问题 | 位置 | 事实 | 处理要求 |
|---|---|---|---|
| LLM 章节生成无可见超时边界 | `backend/domains/eu/tia/service.py::generate_report` | `tool_start` 后无新事件，人工停止 | 为检索和 LLM 步骤增加超时、失败事件、取消信号 |
| 附件 fixture 不是真实文件 | `TIARequest.attachments[0].storage_uri` | 解析被跳过 | 补脱敏 PDF，并验证解析内容进入报告 |
| 固定案例无正文引用 | `backend/tests/tia/cases/01_minimal.json` | `footnote_map=0`、ClaimBlock=0 | 增加带 `[1]`/CitationRegistry 的 service fixture |
| ZIP 未包含 CitationMap | `backend/domains/eu/tia/report_renderer.py` | ZIP 成员只有用户文件和 DocumentIR | 明确 CitationMap 是内部产物还是必须入包，并固定验收规则 |

## 6. 下一步（仍只在本地）

1. 复用既有索引，给 RAG 和 LLM 步骤增加明确超时，重跑 LLM 模式；
2. 补真实脱敏 PDF fixture，重新验证附件解析；
3. 增加带正文引用的 service fixture，确认正文 `[N]`、CitationMap、DocumentIR ClaimBlock 一致；
4. 完成 TIA 后，再按同一模板验收 PIPIA、BCR、SCC、CPRA、EO14117、document review；
5. 本地浏览器完成引用 exact/source_overview/not_found/not_unique/missing 五类跳转后，才更新阶段状态；远端部署和删除旧流程继续禁止。

## 7. 汇总统计

| 项目 | 结果 |
|---|---:|
| no-LLM 完整产物组 | 2（old/new） |
| LLM 完整产物组 | 0 |
| no-LLM 新流程耗时 | 1852 ms |
| LLM 已确认 token | 1655（非完整运行） |
| 正文脚注 | 0（固定案例不覆盖） |
| 当前 TIA 状态 | 部分完成 |
| 远端部署 | 禁止 |
