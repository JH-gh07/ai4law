# 阶段 3 TIA 本地服务首跑记录

> 日期：2026-08-08
> 功能：欧盟 TIA 草案生成
> 环境：本地 `new` 分支；未连接远端服务器
> 对应方案：`status/todo/DataComplyFlow_架构统一迁移与可核验实施方案_20260808.md` §12
> 判定：**未完成（服务进入执行流，但未产出完整用户报告）**

## 1. 输入与运行方式

- 输入模型：`backend/domains/eu/tia/schema.py::TIARequest`
- 服务入口：`backend/domains/eu/tia/service.py::TIAService.generate_report`
- 渲染入口：`backend/domains/eu/tia/report_renderer.py::TIAReportRenderer.render`
- Schema-first 开关：`AI4LAW_SCHEMA_FIRST_TIA_ENABLED=true`
- task id：`phase3-tia-service-20260808`
- 输入包含：`transfer_tool=scc`、欧洲子公司→中国母公司、第三国风险描述、补充措施、SCC 附件元数据。
- 附件路径为 `storage://uploads/scc.pdf`，本次未提供真实文件内容，因此附件解析只能记录缺失证据。

执行命令：

```bash
AI4LAW_SCHEMA_FIRST_TIA_ENABLED=true uv run python - <<'PY'
# 构造 TIARequest，调用 TIAService.generate_report(..., trace=TraceRecorder(...))
PY
```

## 2. 已确认的执行证据

运行在 `status/check/phase3_tia_firstrun_20260808/trace/` 写入了 9 个事件文件：

| 序号 | 事件 | 结果 |
|---:|---|---|
| 1 | `status` | 服务开始执行 |
| 2 | `thought` | 完成路径判断 |
| 3-4 | `llm_chat` | RAG 查询规划调用成功，provider 返回 `prompt=321`、`completion=428`、`total=749` |
| 5 | `thought` | RAG 规划结果进入下一步 |
| 6-7 | `llm_chat` | 附件证据审查调用成功，provider 返回 `prompt=246`、`completion=660`、`total=906` |
| 8 | `thought` | 附件审查输出 4 项结果 |
| 9 | `tool_start` | 开始第一章报告生成调用 |

由此可以确认：

1. `TIAService` 确实被调用，不是只调用 renderer 或 adapter；
2. TraceRecorder 能记录中间事件；
3. LLM provider 实际返回了 usage，token 统计链路在事件级别可见；
4. TIA 在进入章节生成前已经完成 RAG 规划和附件审查步骤。

## 3. 未完成和失败原因

第 9 个 `tool_start` 之后，进程在本地检索/章节生成阶段长时间无新事件。为避免继续占用本地进程，已停止本次运行。现场没有生成：

- `MD/DOCX/PDF/ZIP`
- `document_ir.json`
- 完整 `citation_map.json`
- `final`/`final_brief` 事件
- 可用于新旧产物比较的完整输出目录

因此不能声称 TIA 服务首跑成功，也不能把本次 token 数据当作“完整运行耗时”。本次只能证明“服务已启动并执行到章节生成前”。

## 4. 代码和数据问题

| 问题 | 代码位置 | 现象 | 后续动作 |
|---|---|---|---|
| RAG/章节生成阶段无超时或取消边界 | `backend/domains/eu/tia/service.py::generate_report` | 第 9 个 `tool_start` 后无进度事件，任务无法在可接受时间内结束 | 为检索和 LLM 步骤增加超时、失败事件和可取消信号；先用关闭 LLM 的固定 fixture 重跑 |
| 附件只有元数据没有真实文件 | 本次 `TIARequest.attachments[0].storage_uri` | 事件中记录“无 transfer agreement evidence” | 补一份脱敏 PDF fixture，并在 service 首跑中验证解析结果 |
| schema-first 只在 renderer 阶段生效 | `backend/domains/eu/tia/report_renderer.py::render` | 本次未到 renderer，无法验证 Compiler Gates、DocumentIR 和 CitationMap | 服务层首跑必须完成到 `render`，并保存 `document_ir.json` 与产物哈希 |

## 5. 验收表

| 检查项 | 结果 | 证据 |
|---|---|---|
| 输入 Schema | 通过 | `TIARequest`，见 `backend/domains/eu/tia/schema.py` |
| 真实 service 调用 | 部分通过 | `TIAService.generate_report` 已执行，trace 1-9 |
| RAG 调用 | 部分通过 | trace 3-5；尚未确认最终命中质量 |
| 得理 API | 未执行 | 本次 TIA 路径没有得理调用证据 |
| token 统计 | 部分通过 | trace 4、7 有 provider usage |
| 完整耗时 | 未完成 | 没有 final 事件和结束时间 |
| DocumentIR | 未完成 | 未生成 `document_ir.json` |
| CitationMap | 未完成 | 未生成完整产物 |
| MD/DOCX/PDF/ZIP | 未完成 | 未生成 |
| 回退验证 | 未执行 | 需先有完整新流程成功产物 |
| 浏览器引用跳转 | 未执行 | 本次只做后端服务首跑 |

## 6. 下一步（仍然只在本地）

1. 使用真实脱敏 PDF fixture，避免附件解析分支被空文件掩盖；
2. 关闭自动 RAG 索引构建，复用已生成索引，给每个检索和 LLM 调用设置明确超时；
3. 在 no-LLM 模式完成一次可重复 service 首跑，保存完整 MD/DOCX/PDF/ZIP、`document_ir.json`、`citation_map.json` 和 trace manifest；
4. 再用 LLM 模式跑一次，单独记录 provider token 和 wall-clock time；
5. 只有两次都生成完整产物并通过旧/新差异检查后，才把 TIA 标为“部分完成”以上。

## 7. 汇总

| 项目 | 数量/状态 |
|---|---:|
| 已写入 trace 事件 | 9 |
| 已确认 provider LLM 调用 | 2 次完成，1 次开始后中止 |
| 已确认 token | 1,655（749+906，不含中止调用） |
| 完整用户产物 | 0 组 |
| 当前 TIA 状态 | 未完成 |
| 远端部署 | 禁止 |
