# task068 T08 验收报告：工作台正文/输入/产物/附件预览纠正

- 日期：2026-08-10
- 范围：`frontend/src/components/workspace/WorkspaceShell.tsx`、`DocxArtifactPreview.tsx`、`styles/app/workspace.css`
- 性质：机器/结构验收；真实浏览器矩阵（1440/1024/390、截图、像素）并入 T10

## 1. 变更点

| # | 方案项 | 落地 | 证据 |
|---|---|---|---|
| 1 | 报告正文固定使用 T05 IR renderer | `reportIr.status === "ready"` 时渲染 `<ReportDocumentView document={reportIr.document}/>`；error 受控提示；loading 进度；仅 IR 不可用时回退 `reportPreviewSections` | `WorkspaceShell.tsx` 报告 tab |
| 2 | PDF artifact 使用 PDF viewer | `reportViewMode === "pdf"` 继续走 `<PdfViewer>`（既有实现保留） | `WorkspaceShell.tsx` |
| 3 | DOCX 不进入 Markdown renderer | 新增 `DocxArtifactPreview`，`renderTextArtifactPreview` 的 `isDocxPreview` 分支直接返回，不调用 `normalizeMarkdownForRender()`/`ReactMarkdown`/`CitationMarkdownRenderer` | `DocxArtifactPreview.tsx` + `WorkspaceShell.tsx` |
| 4 | 辅助抽取文本默认折叠 | `<details>` + `<summary>辅助抽取文本（点击展开）</summary>` + `<pre class="workspace-plain-preview">` | `DocxArtifactPreview.tsx` |
| 5 | 去除正文/导出预览重复长文本 | 删除 `buildSectionsFromArtifactPreview`，`reportPreviewSections` 不再从 `artifactPreview` 反向拼正文 | `WorkspaceShell.tsx` |
| 6 | 输入材料读 T03 manifest | 沿用既有 `ResourcePanel` 按 `manifest.entries` 分组（source_kind） | `ResourcePanel.tsx`（T03 已完成） |
| 7 | task/run/artifact ID 关联 | 沿用 `useReportIr(taskSpace.id, taskSpace.module)` + 既有 artifacts/preview 鉴权通路 | `use-report-ir.ts` |

## 2. 真实命令与输出

```text
$ cd frontend && npx tsc --noEmit -p tsconfig.app.json
(无输出，退出码 0)

$ npx vitest run src/components/workspace/DocxArtifactPreview.test.tsx
Test Files  1 passed (1)
     Tests  5 passed (5)

$ npm test -- --run
Test Files  2 failed | 29 passed | 1 skipped (32)
     Tests  2 failed | 195 passed | 2 skipped (199)
```

2 个失败为既有无关用例，非本次改动引入：

- `src/lib/dev-test-cases.test.ts`：PIPIA payload 字符串顺序快照差异
- `tests/contract/dev-case-fixtures.test.ts`：`benchmarks/.../认证_标准合同路径测试案例及预期输出.docx` 未被 Git 跟踪

## 3. 验收项对照

| 验收项 | 结论 | 说明 |
|---|---|---|
| component test 证明 DOCX 不调用 Markdown renderer | PASS | `DocxArtifactPreview.test.tsx` 断言抽取文本仅存在于 `<pre>`，且 `.workspace-report-richtext` 不存在、无 heading 节点 |
| 同一 report body 只出现一次 | PASS | 正文由 IR 渲染一处；同一 artifact 抽取文本不再同时进入 `reportPreviewSections` 与导出预览 |
| 刷新恢复仍正确 | PASS（结构） | `useReportIr` 组件内存态 + `/me/workspace-recovery` 注入 manifest；真实浏览器恢复并入 T10 |
| 键盘可操作、焦点可见、390px 无遮挡 | PASS（结构） | 原生 `<button>` 键盘可操作、`<details>` 可键盘展开、`.workspace-docx-actions` flex-wrap；真实像素截图并入 T10 |

## 4. 诚实声明

- 未伪造浏览器截图/像素验收；该部分按方案归入 T10 浏览器矩阵。
- DOCX 内嵌视觉预览仍受 LibreOffice 缺失阻断（`BLOCKED_BY_TOOLING`），T08 只保证 DOCX 不落入 Markdown 猜测路径。
