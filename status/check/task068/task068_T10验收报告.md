# task068 T10 验收报告：跨格式集合门禁 + fail-closed mutation（自动部分）

- 日期：2026-08-10
- 范围：`backend/common/reporting/compiler/core.py`、`backend/common/reporting/render_manifest.py`、`backend/common/reporting/tests/test_t10_cross_format_gate.py`、`backend/tests/contracts/test_schema_first_contracts.py`
- 性质：机器/结构验收；浏览器矩阵与真实视觉按方案归入本阶段但 `BLOCKED`（见 §4）

## 1. 变更点

| # | 方案项 | 落地 | 证据 |
|---|---|---|---|
| 1 | section_ids/finding_ids/citation_ids 三格式等价 | 既有 `render_manifest.equivalence` gate 保持（section 顺序 + finding 顺序 + citation 顺序 + clause presence） | `render_manifest.py` `_evaluate_equivalence` |
| 2 | basis_entries 三格式等价（每条 citation_ref+label 一致，rationale 不新增） | 新增 `BasisEntryIdentity(label, citation_ref)`，manifest 记录 `basis_entry_identities`（不含 rationale）；equivalence 校验 label 在 MD/DOCX/PDF 三格式 presence；`verify_render_manifest` 同步补 basis 校验 | `render_manifest.py` |
| 3 | action refs 等价 | 说明：Review 的 `priority_actions` 走 `ListBlock` 文本（已由 block 文本等价覆盖）；typed `ActionRecord` 未接 block renderer，Review 未 populate `document.actions` → 不虚构 | `schema_first.py`（Review `review.s8`） |
| 4 | 两个相同章节 ordinal fail-closed | 编译器 `StructureValidationPass` 新增 `SECTION_ORDINAL_DUPLICATE` | `compiler/core.py` |
| 5 | 原始 JSON / 绝对路径进正文 fail-closed | 新增 `BodyContentValidationPass`（`BODY_RAW_JSON` / `BODY_ABSOLUTE_PATH` / `BODY_DEBUG_FIELD`）扫正文/表格/列表/子句/finding 文本 | `compiler/core.py` |
| 6 | 多条引用「；」压单行 fail-closed | `basis_entries` 逐条渲染，测试断言两个 label 不落在同一 `；` 拼接行 | `test_t10_cross_format_gate.py` |
| 7 | 契约静态层误报 | `{{CIT-` 标记语法替代裸 `CIT-` 判断；清理 F401/F541 | `test_schema_first_contracts.py` |

## 2. 真实命令与输出

```text
$ uv run --frozen ruff check backend/common/reporting/ backend/tests/contracts/test_schema_first_contracts.py
All checks passed!

$ uv run --frozen pytest backend/common/reporting/tests/test_t10_cross_format_gate.py -q
11 passed

$ uv run --frozen pytest \
    backend/common/reporting/tests/ backend/common/workflow/tests/ \
    backend/tests/contracts/test_schema_first_contracts.py \
    backend/domains/cn/document_review/tests/ \
    backend/api/v1/tests/test_review_async.py backend/api/v1/tests/test_artifacts_access.py \
    backend/domains/us/cpra/tests/ backend/domains/us/eo14117/tests/ backend/domains/eu/dpia/tests/ -q
363 passed, 1 warning
```

## 3. mutation 对照（T10 清单）

| mutation | 结论 | 证据/诊断码 |
|---|---|---|
| 删除 finding | fail | `equivalence`（`verify_render_manifest`） |
| 重复 finding | fail | `FINDING_ID_DUPLICATE` / `FINDING_PRIMARY_DISPLAY_COUNT` |
| 重排 finding | fail | `equivalence` 顺序 |
| citation 指向不存在 finding | fail | `FINDING_REF_NOT_REGISTERED` |
| 同一 finding 两个主展示 | fail | `FINDING_PRIMARY_DISPLAY_COUNT` |
| basis_entries 引用未登记 citation | fail | `BASIS_CITATION_NOT_REGISTERED` |
| rationale LLM 签名/占位符 | fail | `BASIS_RATIONALE_ILLEGAL_GENERATED` |
| 多条引用 `；` 压单行 | fail | basis 逐条渲染测试 |
| 两个相同章节 ordinal | fail | `SECTION_ORDINAL_DUPLICATE` |
| 原始 JSON / 绝对路径进正文 | fail | `BODY_RAW_JSON`/`BODY_ABSOLUTE_PATH`/`BODY_DEBUG_FIELD` |
| 修改附件一个字节 hash 不符 | fail | `artifact_hash`（既有 `test_render_manifest.py`） |
| manifest 宣称 consumed 但文件不存在 | fail | input_manifest 测试（既有 `test_input_manifest.py`） |
| renderer 遗漏未知 block | fail | `RenderError`（既有 `test_layout_contract.py`） |
| PDF 未嵌入 CJK 字体 | blocked | `pdf_font_embedding` = `BLOCKED_BY_FONT` |
| DOCX 无原生 heading/numbering | fail | `docx_stats.heading_count/numbering_def_count`（既有） |
| Web 让 DOCX 进 Markdown renderer | fail | `DocxArtifactPreview.test.tsx`（T08） |
| 旧失败任务重跑后仍显旧 artifact/status | （待 T11 shadow/回滚） | task manager（未在本阶段改） |

## 4. 跨模块测试隔离排查（4 个失败根因定位）

T10 编译器/契约改动落地后，跑其它模块套件出现 4 个失败。用 `git stash` 隔离确认：

| 失败测试 | 根因 | 是否本阶段回归 | 处置 |
|---|---|---|---|
| `bcr_review/...::test_bcr_document_ir_matches_golden_snapshot` | `FindingRecord` 新增 `basis_entries`/`clause_id`/`source_position` 等可选字段（本阶段 I068-22 + 输入追溯），BCR golden 未同步；cpra/eo/review golden 因 findings 为空不触发 | **是（本阶段回归）** | 已重新生成 `bcr_document_ir.golden.json`，测试通过 |
| `scc_review/...::test_uploaded_scc_document_drives_core_review` | 断言 `display_label` 含 `2021/914`，但 citation_map 只有 EDPB 引用；`scc_review` 模块文件未改 | 否（HEAD 既有） | 记录为 out-of-scope，不动 |
| `pipia/...::test_scc_evidence_drives_source_findings` | 规则路径 issue title 期望 `告知不充分`，实际为 `个人信息行权机制不完善` 等；`pipia` 模块文件未改 | 否（HEAD 既有） | 记录为 out-of-scope，不动 |
| `pipia/...::test_certification_evidence_drives_path_findings` | issue title 期望 `法律文件管辖条款…`，实际缺失 | 否（HEAD 既有） | 记录为 out-of-scope，不动 |

**隔离证据**（真实命令）：

```text
# 1) 仅 revert 编译器/契约两文件，4 失败仍复现 → 与 T10 编译器无关
$ git stash push -- backend/common/reporting/compiler/core.py backend/tests/contracts/test_schema_first_contracts.py
$ uv run --frozen pytest <4 tests> -q
4 failed

# 2) 进一步 stash scc/pipia/knowledge/citation 相关 tracked 改动，3 失败仍复现 → HEAD 既有
$ git stash push -m "t10-verify-preexisting" -- backend/domains/eu/scc_review backend/domains/cn/pipia backend/common/knowledge/registry.py backend/common/citation
$ uv run --frozen pytest <3 tests> -q
3 failed
```

结论：`scc_review`×1 与 `pipia`×2 在 HEAD 基线即失败，非 task068 回归，不纳入本阶段修改；BCR golden 漂移为本阶段 schema 扩展所致，已修复并回归。

## 5. 诚实声明（BLOCKED，未伪造）

- **浏览器矩阵**（1440/1024/390、真实上传 DOCX、异步完成、SSE、鉴权、恢复、截图、PDF canvas 像素）未执行：本环境无远程部署、无真实浏览器二进制运行条件；按方案不允许只调 service 绕过 HTTP/SSE 冒充完成。
- **DOCX 视觉**：`BLOCKED_BY_TOOLING`（LibreOffice 缺失），不宣称视觉通过。
- **PDF 字体**：`BLOCKED_BY_FONT`（无可再分发 CJK 字体，STSong-Light emb=no），不宣称字体验证通过。
- `action refs` 等价：Review 走自由文本 `ListBlock`（已由文本等价覆盖），typed `ActionRecord` 尚无 block renderer，未虚构该 gate 的通过证据。
