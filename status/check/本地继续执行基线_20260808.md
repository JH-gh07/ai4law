# DataComplyFlow 本地继续执行基线

> 日期：2026-08-08
> 分支：`new`
> 范围：本地检查、测试和服务首跑；远端部署禁止
> 目的：承接 `DataComplyFlow_架构统一迁移与可核验实施方案_20260808.md`，修正旧基线与当前工作区不一致的问题。

## 1. 当前代码基线

| 项目 | 当前值 |
|---|---|
| 当前 HEAD | `c775e5b fix: enforce citation registry across report paths` |
| 工作区 | 非干净；存在 3 个源码修改、DPIA 产物修改、未跟踪脚本和 TIA 首跑证据 |
| 远端 | 未连接、未部署、未修改 |
| 旧流程 | 保留，未删除 |

工作区脏改动没有在本记录中当作迁移成果提交；后续提交必须按文件边界拆分。

## 2. 本次可重复检查

### 引用单一入口

```text
uv run python scripts/check_citation_single_source.py
Citation single-source guard passed.
```

### 知识库结构完整性

```text
source_count:                   102
article_row_count:              3030
duplicate_source_article_count: 0  (pair_count=0)
missing_article_ref_count:      0
missing_source_url_count:       2620
invalid_article_number_count:   0
All blocking checks PASSED.
```

结论：唯一定位键和条号字段通过；来源 URL 仍有 2620 条缺失，不能宣称官方跳转覆盖完成。

### 前端引用组件

命令：

```bash
cd frontend
npm test -- --run src/components/citation/CitationMarkdownRenderer.test.tsx src/components/citation/CitationArticleDrawer.test.tsx
```

结果：2 个测试文件、5 个测试全部通过。该结果只证明组件行为，不替代浏览器端正文点击和知识库定位验收。

## 3. TIA 本地服务首跑现场

证据目录：`status/check/phase3_tia_firstrun_20260808/`。

- `TIAService.generate_report` 已真实进入执行；
- trace 已记录 RAG 规划和附件审查两个 provider 调用；
- 已记录 token：749 + 906 = 1655；
- 第三次章节生成调用只写入 `tool_start`，之后长时间无新事件，已停止进程；
- 没有生成完整 MD/DOCX/PDF/ZIP、DocumentIR、CitationMap 或 final 事件。

因此 TIA 状态为“未完成”，本次不能作为成功首跑，也不能作为完整耗时统计。

详细记录见：[phase3 TIA 本地服务首跑记录](phase3_tia_firstrun_20260808/验收报告.md)。

## 4. 当前阶段判定

| 阶段 | 判定 | 依据 |
|---|---|---|
| 公共能力 | 已完成 | 引用单一入口门禁通过，公共测试存在 |
| assessment | 部分完成 | fixture 产物存在，生产形态完整首跑仍待补 |
| 其他报告模块 | 部分完成 | 适配器和单测存在，真实 service 首跑证据不足 |
| 知识库治理 | 部分完成 | 唯一键通过，URL 和零条文来源仍有缺口 |
| 前端引用闭环 | 部分完成 | 组件 5/5 通过，无浏览器截图和网络证据 |
| 旧流程退出 | 未开始 | 新旧真实对比和回退条件未满足 |

## 5. 下一步验收顺序

1. 先用 no-LLM、复用既有索引的 TIA service 运行完成一组固定输入，保存完整产物；
2. 再补真实脱敏附件和一次 LLM 模式运行，单独记录 token/time；
3. 按同一模板补 PIPIA、BCR、SCC、CPRA、EO14117、document review；
4. 重跑 assessment 生产形态完整链路；
5. 启动本地前端，完成 exact/source_overview/not_found/not_unique/missing 五类引用点击截图和网络日志；
6. 所有本地证据齐全并经用户同意前，不得远端部署，不得删除旧流程。
