# 阶段 3 BCR 本地首跑验收记录

> 日期：2026-08-08
> 功能：BCR 审核
> 环境：本地 `new` 分支；未连接远端服务器
> 判定：**部分完成**

## 1. old/new 服务首跑

固定输入：`backend/tests/bcr/cases/01_minimal.json`。

两次都真实调用 `BCRService.generate_report`，LLM 关闭，使用表单驱动路径。旧流程和新流程都生成 MD、DOCX、PDF、ZIP、CitationMap；修复后新流程额外暴露并打包 `document_ir.json`。

| 项目 | 旧流程 | 新流程 |
|---|---:|---:|
| 风险评级 | 部分缺失 | 部分缺失 |
| 章节数 | 4 | 4 |
| 问题数 | 1 | 1 |
| LLM/token | 0/0 | 0/0 |
| CitationMap | 4 个候选依据，0 个正文脚注 | 4 个候选依据，0 个正文脚注 |
| DocumentIR | 无 | 4 sections、4 blocks、diagnostics=[] |
| ZIP | MD/DOCX/PDF/CitationMap | MD/DOCX/PDF/CitationMap/DocumentIR |

## 2. 发现并修复的问题

原始代码位置：`backend/domains/eu/bcr_review/service.py::_render`。

问题：Schema-first 分支已经写出 `outputs/bcr/<task>/outputs/document_ir.json`，但没有把路径放入 `result.output_files`，ZIP 也没有包含它。服务调用方无法发现这个内部产物，和其他 Schema-first 模块行为不一致。

修复内容：

1. `_render` 增加 `document_ir_path` 局部变量；
2. Schema-first 开启时将 `document_ir_json` 加入返回字典；
3. ZIP 写入 `document_ir.json`；
4. 新增回归测试 `test_bcr_schema_first_service_exposes_document_ir`。

## 3. 测试证据

```bash
uv run pytest -q \
  backend/domains/eu/bcr_review/tests/test_service.py \
  backend/domains/eu/bcr_review/tests/test_schema_first_adapter.py
```

结果：修复前新增测试失败；修复后 `8 passed`，1 个弃用警告，无失败。

## 4. 引用边界

本轮使用 no-LLM 占位章节。CitationRegistry 注册了候选依据，但正文没有 `[N]`，所以不能宣称 BCR 正文引用、DocumentIR ClaimBlock 和 CitationMap 已闭环。

此外，固定案例的 `uploaded_documents=[]`，实际走的是表单驱动路径；BCR 的文档上传审查路径仍需单独用真实本地 BCR 文档复验。

## 5. 当前状态

| 项目 | 状态 |
|---|---|
| old/new 表单驱动 service | 通过 |
| Schema-first DocumentIR 暴露 | 已修复并通过测试 |
| ZIP 内部产物分离 | 新流程已包含 DocumentIR |
| 正文引用闭环 | 未完成 |
| 文档上传审查路径 | 未验收 |
| 浏览器跳转 | 未执行 |
| 远端部署 | 禁止 |

## 6. 文档上传路径补验

新增测试：`test_bcr_document_upload_path_exposes_document_ir`。

测试使用本地临时 BCR 文本文件，通过 `uploaded_documents` 进入文档驱动路径。补验过程中发现两个真实参数契约错误：

1. `BCRActorRoleAgent.run()` 要求 `scenario_context`，service 错传了不存在的 `entities` 参数；
2. `BCRApprovalRiskAgent.run()` 要求 `current_rating`，service 错传了不存在的 `rating` 参数。

已在 `backend/domains/eu/bcr_review/service.py` 修正。修复后文档驱动服务能够完成并通过 Schema-first Compiler，`document_ir.json` 会返回并写入 ZIP。

定向回归结果：`10 passed`。

## 7. 当前状态

| 项目 | 状态 |
|---|---|
| 表单驱动 old/new service | 通过 |
| 文档驱动 uploaded_documents | 通过 |
| 两条路径的 DocumentIR 输出 | 已接通 |
| 正文引用闭环 | 未完成，当前 no-LLM 证据没有正文脚注 |
| 浏览器跳转 | 未执行 |
| 远端部署 | 禁止 |

下一步补文档驱动 BCR 的带引用章节案例，再进入 SCC。
