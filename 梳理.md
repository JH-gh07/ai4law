# 一、项目整体概述

## 1. 项目背景与目标

- **参赛背景**：本项目面向第十七届中国大学生服务外包创新创业大赛（D06 赛道），定位为"AI + 跨境数据合规"方向的综合性解决方案。
- **核心目标**：帮助企业在多法域（中国、欧盟、美国）场景下，快速完成合规路径诊断、安全评估、合同审查与报告生成，降低合规门槛与人工成本。
- **技术愿景**：探索"规则先行 + RAG 证据注入 + 大模型生成 + 可追溯输出"的新型 AI 合规工作流，而非简单的问答式大模型调用。

## 2. 主要解决的问题

| 痛点 | 传统方式 | 我们的方案 |
|------|---------|-----------|
| 合规路径复杂、法规繁多 | 依赖专业律师手动判断 | 基于决策树的自动诊断 + 风险评分 |
| 报告撰写耗时长 | 人工收集法规、逐章起草 | RAG 检索 + LLM 一键生成，附带引用溯源 |
| 文档审查易遗漏条款 | 人工逐条对照检查清单 | 自动解析文档 + 条款分类 + 缺失项识别 |
| 输出结果不可解释 | 黑盒式 AI 生成 | 每条结论关联法规条款，可回溯、可验证 |
| 多法域切换成本高 | 不同地区需不同团队 | 统一平台覆盖 CN→海外、EU→海外、US→海外 |

## 3. 系统整体定位

一个 **面向跨境数据合规的 AI 辅助工作平台**，具备：
- **诊断能力**：自动判断适用的合规路径（安全评估 / SCC / 认证 / 豁免）
- **生成能力**：一键生成安全评估报告、PIA 报告、DPIA、TIA 等
- **审查能力**：对上传的合同/制度文档进行条款级合规审查
- **追溯能力**：所有生成内容均可回溯到具体法规条款

## 4. 最终想实现的核心能力

```
用户输入场景信息 → 规则引擎确定合规路径 → RAG 检索相关法规与案例
    → LLM 生成报告草案 → 一致性检查 → 可下载报告（docx/pdf/xlsx）
```

---

# 二、功能设计与页面展示

## 1. 主要功能模块划分

系统按 **法域维度** 划分为 11 个核心功能模块：

### 中国 → 海外（CN 模块）
| 模块 | 功能名称 | 作用 |
|------|---------|------|
| 2.1 合规路径诊断 | Diagnosis | 基于 CIIO 身份、数据类型、个人信息数量级等，自动推荐合规路径 |
| 2.2 安全评估 | Assessment | 生成数据出境安全评估报告，含 RAG 法规引用 |
| 2.3 个人信息保护影响评估 | PIPIA | 生成个人信息保护影响评估报告 |
| 2.4 通用服务 | CN Flow | 通用合规报告生成 |
| 2.5 文档审查 | Document Review | 上传合同/制度文件，自动审查缺失合规条款 |

### 欧盟 → 海外（EU 模块）
| 模块 | 功能名称 | 作用 |
|------|---------|------|
| 3.1 SCC 审查 | SCC Review | 标准合同条款合规审查 |
| 3.2 BCR 审查 | BCR Review | 约束性企业规则审查 |
| 3.3 DPIA 草案 | DPIA Draft | 数据保护影响评估草案生成 |
| 3.4 TIA 草案 | TIA Draft | 传输影响评估草案生成 |

### 美国 → 海外（US 模块）
| 模块 | 功能名称 | 作用 |
|------|---------|------|
| 4.1 中美数据流动 | CN-US Flow | 风险项分析，导出报告 + xlsx 风险清单 |
| 4.2 CPRA 全景图 | CPRA Panorama | 加州消费者隐私法合规差距分析 |

### 公共能力
- **报告中心**：统一查看、下载所有生成的报告
- **知识中心**：浏览法规来源、验证引用可追溯性
- **Copilot**：AI 合规助手，支持自然语言问答

## 2. 每个功能模块的作用与实现细节

### 2.1 合规路径诊断模块（Diagnosis）

**API 端点**：
- `POST /api/v1/diagnosis/evaluate` — 评估并返回诊断结果
- `POST /api/v1/diagnosis/report` — 评估 + 生成 HTML/PDF 报告

**输入**（`DiagnosisReportRequest`）：
| 字段 | 类型 | 说明 |
|------|------|------|
| `company_name` | string | 企业名称 |
| `q1_is_ciio` | yes/no/unknown | 是否为关键信息基础设施运营者 |
| `q2_has_important_data` | yes/no/unknown | 是否涉及重要数据 |
| `q3_pii_count` | int | 过去 12 个月累计出境个人信息数量 |
| `q4_spi_count` | int | 过去 12 个月累计出境敏感个人信息数量 |
| `q5_no_personal_info` | yes/no | 是否不含个人信息和重要数据 |
| `q6_scenario` | enum | 出境场景：合同履行/人力资源管理/紧急情况/法定职责/其他 |
| `q7_receiver_type` | enum | 接收方类型：集团内/第三方 |
| `q8_purpose` | string | 出境目的说明 |

**处理逻辑**：
```
DiagnosisService.evaluate(answers)
    |
    +-- 加载 decision_tree.json（9 条规则 + 1 条默认）
    +-- 按顺序逐条匹配（短路机制，首次匹配即返回）
    |     |
    |     +-- 豁免规则（优先级最高，5 条）：
    |     |     不含个人信息 → 豁免
    |     |     合同履行 + PII<100万 + SPI<1万 + 非CIIO + 无重要数据 → 豁免
    |     |     人力资源管理 + 集团内 + PII<100万 + SPI<1万 + 非CIIO → 豁免
    |     |     紧急情况 + 非CIIO + 无重要数据 → 豁免
    |     |     法定职责 + 非CIIO + 无重要数据 → 豁免
    |     |
    |     +-- 强制安全评估规则（4 条）：
    |     |     CIIO = yes → 安全评估
    |     |     重要数据 = yes → 安全评估
    |     |     PII >= 100万 → 安全评估
    |     |     SPI >= 1万 → 安全评估
    |     |
    |     +-- 默认规则：标准合同 + 认证路径
    |
    +-- _build_result()：
          - 翻译法理依据（i18n 中文解释）
          - 计算风险等级（HIGH/MEDIUM/LOW）
          - 分配行动建议（安全评估 3 项 / 标准合同 3 项）
    |
    +-- 返回 DiagnosisResult
```

**输出**（`DiagnosisResult`）：
| 字段 | 说明 |
|------|------|
| `recommended_path` | 推荐路径：`security_assessment` / `scc_or_certification` / `exemption` |
| `legal_basis` | 适用的中国法规列表 |
| `rationale` | 决策理由的中文解释 |
| `action_items` | 3 条下一步建议 |
| `risk_level` | `HIGH` / `MEDIUM` / `LOW` |

**结果保存与呈现**：
- 报告生成：`DiagnosisReportRenderer.render()` 组装 4 个章节（企业回答、诊断结果、AI 摘要、机器可读附录）
- AI 摘要：可选调用 LLM（系统提示："你是一位资深跨境数据合规律师"）生成 2-3 句总结
- 文件输出：`outputs/diagnosis/{企业名称}_diagnosis_report.html` + `.pdf`
- 前端呈现：诊断页面直接展示结果卡片 + 风险等级可视化；报告页面提供下载链接

**与其他模块的协作**：
- **被 Assessment 模块调用**：安全评估报告生成时，先调用 `DiagnosisService.evaluate()` 验证路径是否为 `security_assessment`，否则拒绝生成（除非强制覆盖）
- **被 SCC 模块间接引用**：诊断结果作为 SCC 审查的背景参考

---

### 2.2 安全评估报告生成模块（Assessment）

**API 端点**：
- `POST /api/v1/assessment/generate` — 同步生成
- `POST /api/v1/assessment/generate_async` — 异步提交（返回 task_id）
- `GET /api/v1/assessment/tasks/{task_id}` — 轮询任务状态
- `POST /api/v1/assessment/tasks/{task_id}/retry` — 重试失败任务

**输入**（`AssessmentRequest`）：企业名称、行业、是否 CIIO、是否含重要数据、PII/SPI 数量、出境目的、接收国、是否强制覆盖路径、上传文件列表

**处理逻辑（7 步流水线）**：
```
AssessmentService.generate_report(request)
    |
    +-- Step 1: ProfileExtractor.extract()
    |     解析 uploaded_files → 提取文本前 160 字符作为 extracted_notes
    |     返回 CompanyProfile（结构化用户画像）
    |
    +-- Step 2: DiagnosisService.evaluate() 路径验证
    |     将输入映射为 DiagnosisAnswers → 调用决策树
    |     若 recommended_path != "security_assessment" 且 !force_override → 抛 ValueError (400)
    |
    +-- Step 3: AssessmentRetriever.search(profile) — RAG 检索
    |     构建查询字符串："{industry} {transfer_purpose} {receiver_country} CIIO/非CIIO 重要数据/个人信息"
    |     调用 retrieve_regulations(jurisdiction="cn", path="assessment", mode="hybrid", top_k=8)
    |     并行执行：本地向量检索 + 得理 API（如启用）→ 合并去重 → 返回 RegulationHit[]
    |
    +-- Step 4: AssessmentChapterGenerator.generate() — LLM 章节生成
    |     生成 8 章：出境活动概述 → 数据类型与规模 → 出境必要性与合法性 → 接收方保障能力
    |                → 个人信息权益影响 → 安全措施与传输机制 → 剩余风险与整改 → 综合结论
    |     每章 prompt：系统提示（合规专家角色）+ 章节指令 + 上下文块（画像 + Top5 法规摘要）+ 严格约束
    |     后处理：ensure_paragraph_citations() 为每段追加 [依据：法规名]
    |
    +-- Step 5: ConsistencyChecker.check() — 一致性检查
    |     - 检查章节是否包含法规引用
    |     - 检查高风险画像是否最终章节标记为 HIGH
    |     - check_cn_alignment()：正则匹配生成内容与输入参数是否矛盾（如输入非 CIIO 但文中提到 CIIO）
    |
    +-- Step 6: AssessmentReportRenderer.render() — 报告渲染
    |     读取模板 doc/v2/assets/templates/2.2_risk_assessment_template_v0.docx/.md
    |     将 24 个 key-value 映射到模板占位符（company_name, transfer_purpose, legal_risk 等）
    |     章节内容通过 summarize_for_slot() 压缩为 2-3 句、260 字符以内
    |     生成 .docx + .md + .zip 三件套
    |
    +-- Step 7: 异步任务管理（如为 async 模式）
          InMemoryTaskManager.submit(runner) → CREATED → RUNNING → COMPLETED
          前端通过 /api/v1/assessment/tasks/{id} 轮询状态
```

**输出**（`AssessmentResult`）：
| 字段 | 说明 |
|------|------|
| `task_id` | 任务 ID |
| `report_path` | .docx 文件路径 |
| `output_files` | `{"markdown": "...md", "docx": "...docx", "zip": "...zip"}` |
| `profile` | 结构化企业画像（含 extracted_notes） |
| `regulations` | 检索到的法规命中列表 |
| `chapters` | 8 章生成内容 |
| `consistency_issues` | 一致性检查发现的警告 |

**结果保存与呈现**：
- 文件输出：`outputs/assessment/{企业}_数据出境风险自评估报告_草案_{YYYY-MM-DD}.docx/.md/.zip`
- 模板填充：使用 `render_docx_template()` 替换 Word 模板中的 `{{company_name}}`、`{{transfer_purpose}}` 等 24 个占位符
- ZIP 包：包含 .docx 和 .md 两个文件，方便打包下载
- 前端呈现：工作台提交后显示进度条 → 完成后在报告中心显示报告卡片 + 预览 + 下载

---

### 2.3 PIPIA 报告生成模块

- **API 端点**：`POST /api/v1/pipia/generate`（同步）+ 异步变体
- **处理逻辑**：与安全评估模块共享相同的基础设施（RAG 检索 + LLM 章节生成 + 一致性检查 + 模板渲染）
- **差异点**：专注于个人信息保护影响评估，章节内容围绕 PII 处理活动的合法性、必要性、影响分析
- **输出**：`outputs/pipia/` 目录下生成 .docx/.md/.zip 报告

---

### 2.4 通用服务模块（CN Flow - CN 侧）

- **API 端点**：通过 `/api/v1/cn-flow/` 提供
- **作用**：通用合规报告生成入口，适配中国侧非标准场景
- **处理逻辑**：同 Assessment 模块架构，但 prompt 模板和输出格式更灵活

---

### 2.5 文档审查模块（Document Review）

**API 端点**：
- `POST /tasks` — 创建审查任务
- `POST /tasks/{task_id}/files` — 上传文件
- `POST /tasks/{task_id}/analyze` — 触发分析流水线
- `GET /tasks/{task_id}/status` — 查看进度
- `GET /tasks/{task_id}/issues` — 获取问题清单
- `GET /tasks/{task_id}/report` — 下载报告

**输入**：用户上传的 PDF 或 DOCX 文件（合同、隐私政策、制度文件等）

**处理逻辑（6 步流水线，带实时进度推送）**：
```
ReviewService._run_pipeline(task_id)
    |
    +-- Step 1: 文件解析（progress 10 → 25）
    |     FileService 接收文件 → 验证扩展名（仅 .pdf/.docx）
    |     保存到 {upload_dir}/{task_id}/{uuid}.{ext}
    |     PDF → pypdf 逐页提取文本
    |     DOCX → python-docx 提取段落 + 表格内容
    |     提取文本存入 uploaded_files 表（extracted_text 列）
    |
    +-- Step 2: 文档分段（SEGMENTING, progress 25 → 45）
    |     ClauseSegmenter.segment(raw_text)
    |     按中文条款编号正则切分：第一条、1.、一、等
    |     每段生成 Clause 对象：clause_id + file_id + text + heading + position
    |
    +-- Step 3: 条款分类（CLASSIFYING, progress 45 → 70）
    |     ClauseClassifier.classify(clause)
    |     加载 review_rulebook.json（6 类合规规则 + 关键词表）
    |     对每个条款统计各规则关键词命中数 → 分配最高匹配的 ClauseType
    |     类型包括：数据处理范围、同意与告知、安全措施、跨境传输、主体权利、违约责任、其他
    |
    +-- Step 4: 条款审查（REVIEWING, progress 70 → 85）
    |     ClauseReviewer.review(classified_clause)
    |     两种策略（LLM 优先，规则兜底）：
    |       A. LLM 方式：将条款文本 + 法规引用发送给 LLM → 解析 JSON 返回的 Issue 列表
    |       B. 规则方式（始终可用）：
    |          - 缺失必填组检查：required_groups 中每组至少一个关键词未出现 → 标记 MISSING_REQUIREMENT
    |          - 模糊语言检测：发现"尽最大努力""必要时"等 → 标记 AMBIGUOUS_LANGUAGE
    |          - 按条款类型分配严重级别（HIGH/MEDIUM/LOW）
    |     每条 Issue 包含：issue_id + clause_id + severity + title + problem_type + risk_analysis + recommendation + citation_sources
    |
    +-- Step 5: 审查聚合（AGGREGATING, progress 85 → 95）
    |     ReviewAggregator.aggregate(all_issues)
    |     - 按 (clause_id, problem_type, title) 去重
    |     - 按严重级别排序（HIGH > MEDIUM > LOW）
    |     - 统计各级别数量
    |     - 综合评级：有 HIGH → "高风险"；2+ MEDIUM → "中风险"；否则 "低风险"
    |     - 生成优先级整改行动清单
    |
    +-- Step 6: 报告渲染与存储（RENDERING, progress 95 → 100）
          ReviewReportRenderer.build_sections() 构建 3 个章节：
            1. "文件概要与总体评级"
            2. "优先整改建议"
            3. "逐条问题清单"（每条标注 [HIGH] 条款类型 问题类型 原文摘录 风险分析 法规依据 修改建议）
          ReportService 生成 .docx 文件
          保存到 {report_dir}/review/{task_id}/review_report.docx
          数据库写入 ReportArtifactModel 记录（owner_type="review", owner_id=task_id）
```

**数据库存储**：
| 表 | 关键字段 |
|---|---------|
| `review_tasks` | id (UUID), status, progress (0-100), summary_json, issues_json |
| `uploaded_files` | id, task_id (FK), filename, content_type, storage_path, extracted_text |
| `report_artifacts` | owner_type="review", owner_id=task_id, artifact_type="docx", file_path |

**实时推送**：每个阶段完成后通过 WebSocket 推送进度更新，前端可实时显示进度条

**结果保存与呈现**：
- 文件：`storage/reports/review/{task_id}/review_report.docx`
- 数据库：`review_tasks` 表记录状态和摘要；`report_artifacts` 表记录报告路径
- 前端呈现：审查页面上传文件 → 显示进度条 → 完成后展示问题清单表格 + 总体评级徽章 + 下载报告

**与其他模块的协作**：
- **依赖 RAG 引擎**：审查时通过 `LocalRegulationKnowledgeBase.lookup()` 检索相关法规条款作为审查依据
- **可选依赖 LLM**：LLM 审查为优先策略，失败时自动降级到规则方式
- **依赖得理 API**（如启用）：搜索相关案例作为审查参考

---

### 3.1 SCC 审查模块（EU → 海外）

**处理逻辑**：
1. 解析上传文件（SCC 草案文本）→ 提取备注
2. 计算风险等级（基于 PII/SPI 数量阈值）
3. RAG 检索中国 PIPL 相关 SCC 法规（`jurisdiction="cn", path="scc"`）
4. LLM 生成 4 章：审查依据说明 → 总体合规评级 → 条款级问题清单 → 修订建议与行动计划
5. 规则式发现：无草案文本=HIGH、SPI>0=MEDIUM、PII≥100万或SPI≥1万=HIGH
6. 中英文一致性检查
7. 渲染 docx + md 模板

**输出**：`outputs/scc/{公司}_SCC_合规审查报告_草案_{日期}.docx/.md`

---

### 3.2 BCR 审查模块（EU → 海外）

**输入**：企业名称 + 10 个核心审查项（C1-C10，每项含评分：compliant/partial/non_compliant + 发现 + 法律依据 + 建议 + 证据）

**处理逻辑**：
1. 筛选问题项（跳过 compliant）
2. 评级：≥2 个 non_compliant = "高风险"；≥1 个 non_compliant 或 partial = "部分缺失"
3. 一致性检查：缺失必要代码（C1-C10）、无附件、高风险标记
4. RAG 检索 GDPR/BCR 法规（`jurisdiction="eu"`）
5. LLM 生成 4 章：报告摘要 → 合规评级 → 详细审查结果 → 风险优先级与整改建议
6. 渲染 docx + md + zip

**输出**：`outputs/bcr/{公司}_BCR-C_合规审查报告_草案_{日期}.docx/.md/.zip`

---

### 3.3 DPIA 草案模块（EU → 海外）

**输入**：项目名称 + 处理描述 + 目的与必要性 + 合法基础 + 风险评估 + 缓解措施 + 剩余风险 + 附件清单

**处理逻辑**：
1. 从 residual_risk 文本解析风险等级（关键词匹配）
2. RAG 检索 GDPR 第 35 条相关法规
3. 解析附件
4. LLM 生成 7 章：DPIA 背景 → 数据处理活动描述 → 目的合法性评估 → 风险识别 → 控制措施 → 剩余风险判断 → DPO 复核
5. 一致性检查：无附件、合法利益无平衡测试、高风险无咨询提及
6. 渲染 docx + md + zip

**输出**：`outputs/dpia/{项目}_DPIA_报告_草案_{日期}.docx/.md/.zip`

---

### 3.4 TIA 草案模块（EU → 海外）

**输入**：传输工具类型（scc/bcr/derogation）+ 数据出口方画像 + 数据进口方画像 + 第三国评估 + 补充措施 + 最终结论 + 附件

**处理逻辑**：
1. 从 third_country_assessment + final_conclusion 文本解析风险等级
2. RAG 检索 EDPB 传输相关法规
3. 解析附件
4. LLM 生成 6 章：传输场景识别 → 传输工具判断 → 第三国法律评估 → 补充措施可执行性 → 剩余风险与结论 → 持续复审
5. 一致性检查：无国家法律分析附件、SCC 工具无 SCC 结论提及、高风险无暂停语言
6. 渲染 docx + md + zip

**输出**：`outputs/tia/{出口方}_{工具}_TIA_报告_草案_{日期}.docx/.md/.zip`

---

### 4.1 中美数据流动模块（US → 海外）

**输入**：出境目的 + 数据类别 + 敏感数据标志 + 接收方实体列表（含名称/角色/是否受限方）+ 传输链条 + 附件（数据清单/实体清单）+ 企业名称

**处理逻辑**：
1. **规则式风险识别**：
   - CNF-R1：命中敏感数据标志 → HIGH
   - CNF-R2：命中受限方 → HIGH
   - CNF-R3：传输链中含 subprocessor → MEDIUM
   - CNF-R0：无触发条件 → LOW
2. RAG 检索 EO 14117 相关法规（`jurisdiction="us"`）
3. LLM 生成 4 章：场景定义 → 数据与实体分析 → 限制条件命中分析 → 风险分级与处置
4. 生成风险矩阵表（实体 × 数据类别）
5. 一致性检查
6. 渲染 docx + md + PDF + XLSX（风险清单）+ zip

**输出**：`outputs/cn_flow/{公司}_14117_风险评估结论报告_草案_{日期}.docx/.md/.pdf/.xlsx/.zip`

---

### 4.2 CPRA 全景图模块（US → 海外）

**输入**：商业模式 + 数据生命周期 + 通知与同意 + 消费者权利流程 + 选择退出与销售共享 + 供应商管理 + 附件（隐私政策等）

**处理逻辑**：
1. **规则式差距识别**：扫描输入文本关键词
   - 通知文本缺失 → HIGH 差距
   - 权利流程中无 "45 天"/"SLA" → MEDIUM 差距
   - 销售/共享无选择退出机制 → HIGH 差距
   - 供应商管理无 DPA → MEDIUM 差距
2. RAG 检索 CPRA/CCPA 法规（`jurisdiction="us"`）
3. LLM 生成 6 章：执行摘要 → 企业适用性 → 数据处理合规分析 → 消费者权利评估 → 敏感信息与第三方管理 → 行动清单
4. 一致性检查：无隐私政策附件、存在 HIGH 差距
5. 渲染 docx + md + PDF + XLSX（差距路线图）+ zip

**输出**：`outputs/cpra/{公司}_CPRA_合规全景报告_草案_{日期}.docx/.md/.pdf/.xlsx/.zip`

---

### 模块间协作关系图

```
                    ┌─────────────────────────────────────┐
                    │         公共基础设施层                  │
                    │  LLMClient │ RAG Pipeline │ Render   │
                    │  TaskManager │ FileParser │ Storage   │
                    └──────┬──────────┬───────────┬────────┘
                           │          │           │
              ┌────────────┘          │           └────────────┐
              │                       │                        │
    ┌─────────▼─────────┐  ┌─────────▼─────────┐  ┌──────────▼──────────┐
    │  CN 模块群          │  │  EU 模块群          │  │  US 模块群            │
    │                     │  │                     │  │                       │
    │ Diagnosis ──► ┐    │  │ SCC ────────► ┐    │  │ CN-Flow ──────► ┐    │
    │               │    │  │ BCR ────────► ┐    │  │                 │    │
    │ Assessment ◄──┘    │  │ DPIA ───────► ┐    │  │ CPRA ───────► ┐  │    │
    │ PIPIA              │  │ TIA ────────► ┐    │  │               │  │    │
    │ CN Flow (CN侧)     │  │               │    │  │               │  │    │
    │ Document Review ───┼──┤               │    │  │               │  │    │
    └────────────────────┘  └─────────────────────┘  └───────────────────────┘
              │                       │                        │
              └───────────────────────┼────────────────────────┘
                                      │
                          ┌───────────▼───────────┐
                          │     报告中心 + 知识中心  │
                          │  统一查看、下载、验证引用  │
                          └───────────────────────┘

关键协作关系：
1. Diagnosis → Assessment：安全评估前必须先通过诊断验证路径
2. Document Review → RAG：审查时检索法规条款作为审查依据
3. 所有模块 → LLM：报告章节生成
4. 所有模块 → RAG：法规检索与证据注入
5. 所有模块 → Render：多格式报告输出
6. 所有模块 → TaskManager：异步任务管理
7. 所有模块 → 报告中心：统一聚合展示
```

## 3. 关键页面/关键交互展示

> *(此处建议配截图)*
>
> - **首页（Home）**：项目介绍 + 法域选择入口
> - **工作台（Workspace）**：表单填写 + 文件上传 + 一键提交
> - **报告中心（Report Center）**：报告列表 + 预览 + 下载（支持 docx/pdf/xlsx/zip）
> - **知识中心（Knowledge Center）**：法规浏览 + 条款检索 + 引用溯源
> - **任务创建页**：选择模块 → 填写参数 → 提交任务 → 查看进度

## 4. 功能之间如何形成完整流程

```
[首页选择法域] → [工作台填写场景信息] → [上传相关文档(可选)]
    → [提交任务] → [后端异步执行] → [轮询进度]
        → [完成] → [报告中心预览/下载] → [知识中心验证引用]
```

---

# 三、功能是怎么设计出来的

## 1. 功能设计思路

- **从法规出发**：先梳理中国《数据出境安全评估办法》《个人信息保护法》、欧盟 GDPR、美国各州隐私法等核心法规，再反向设计系统需要提供的功能模块
- **从用户场景出发**：模拟企业法务/合规人员的实际操作流程——"我该走哪条路？"→"需要准备什么材料？"→"怎么生成报告？"→"我的合同合规吗？"
- **从比赛要求出发**：D06 赛道强调"AI 能力 + 业务场景结合"，因此不仅仅是表单+模板，而是引入了决策树推理、RAG 检索增强、一致性检查等 AI 增强环节

## 2. 为什么要这样分模块

- **按法域拆分**：不同法域的法规体系差异大，强行统一会导致逻辑混乱
- **按职责拆分**：诊断、生成、审查是三个不同的 AI 任务，分开实现便于独立迭代和替换
- **按复用性拆分**：RAG 引擎、LLM 客户端、文档渲染、存储管理等作为公共基础设施，各模块共享

## 3. 用户使用流程是怎么串起来的

1. **选择法域** → 首页选择"中国→海外"或"欧盟→海外"或"美国→海外"
2. **填写场景** → 进入对应工作台，填写表单（企业情况、数据类型、出境规模等）
3. **上传文档（可选）** → 上传需要审查的合同或制度文件
4. **提交任务** → 系统创建异步任务，返回任务 ID
5. **查看进度** → 前端轮询任务状态（pending → running → completed/failed）
6. **获取结果** → 报告生成完成后，在报告中心预览、下载
7. **验证引用** → 在知识中心查看报告引用的法规条款原文

## 4. 在交互和页面布局上做了哪些考虑

- **Streamlit 优先**：快速搭建多页面应用，适合演示和迭代
- **表单分区清晰**：按"企业基本信息 → 数据情况 → 出境场景 → 附件上传"分区，降低填写负担
- **结果可视化**：报告预览 + 法规引用高亮 + 风险等级可视化
- **双前端架构**：同时提供 Streamlit（主）和 React/Vite（备选）两套前端，适应不同演示场景

## 5. 如何兼顾专业性、可用性和演示效果

| 维度 | 做法 |
|------|------|
| 专业性 | 决策树基于法规条文构建；RAG 检索范围限定在法规与案例库；一致性检查保证输出格式合规 |
| 可用性 | 表单提供默认值与提示；异步任务避免长时间等待；报告支持多种格式下载 |
| 演示效果 | Streamlit 页面直观易展示；报告输出格式规范；知识中心提供引用溯源，增强可信度 |

---

# 四、系统是怎么实现的（高层次）

## 1. 前端整体实现思路

### 技术选型
- **主前端**：Streamlit（Python），15 个页面，快速原型 + 演示友好
- **备前端**：React 18 + TypeScript + Vite + TailwindCSS，适合生产部署

### 页面结构怎么组织
- `app_streamlit/pages/` 下按功能模块组织：
  - `Home.py` — 首页（项目介绍 + 法域选择入口）
  - `2_CN_Diagnosis.py` — 合规路径诊断
  - `3_CN_Assessment.py` — 安全评估报告
  - `4_CN_PIPIA.py` — 个人信息保护影响评估
  - `5_CN_Review.py` — 文档审查
  - `6_Report_Center.py` — 报告中心
  - `7_Knowledge_Center.py` — 知识中心
  - 以及 EU/US 法域对应页面
- 共享组件封装在 `app_streamlit/components/`，数据模型定义在 `app_streamlit/schemas/`
- API 客户端封装在 `app_streamlit/services/client.py`，统一调用后端 REST API

### 组件怎么拆分
- **表单组件**：各模块独立表单页面，按法域隔离（`st.form` + `st.selectbox` + `st.number_input` 等）
- **展示组件**：报告卡片（`st.card` 风格）、法规条目、引用链接
- **状态组件**：任务进度指示器（轮询 `/api/v0/tasks/{id}/status`）、加载状态（`st.spinner`）、错误提示（`st.error`）

### 工作台/资源管理器/报告预览怎么实现
- **工作台**：Streamlit form + `st.file_uploader` + `st.button` 提交 → 调用后端 API → 获取 task_id → 轮询状态 → 展示结果
- **报告预览**：从后端获取报告内容，用 `st.markdown` / `st.dataframe` 渲染；诊断结果用卡片式布局展示风险等级（`st.metric`）
- **下载**：后端生成 docx/pdf/xlsx 文件，前端通过 `st.download_button` 提供下载
- **知识中心**：`st.dataframe` 展示法规列表，支持筛选（jurisdiction/path），点击可查看条款原文

## 2. 后端整体实现思路

### 技术栈
- **FastAPI**（>=0.116）：REST API 框架，自动生成交互式 API 文档（`/docs`）
- **Pydantic**（>=2.11）：请求/响应数据校验
- **Uvicorn**：ASGI 服务器
- **SQLAlchemy + SQLite**：持久化存储（任务记录、报告元数据、上传文件信息）

### 应用组装
```python
# backend/main.py -> app.py -> create_app()
app = FastAPI(title="AI4Law API")

# 挂载 v1 路由（各模块独立 Router）
app.include_router(diagnosis_router, prefix="/api/v1/diagnosis")
app.include_router(assessment_router, prefix="/api/v1/assessment")
app.include_router(scc_router, prefix="/api/v1/scc")
app.include_router(bcr_router, prefix="/api/v1/bcr")
app.include_router(dpia_router, prefix="/api/v1/dpia")
app.include_router(tia_router, prefix="/api/v1/tia")
app.include_router(cn_flow_router, prefix="/api/v1/cn-flow")
app.include_router(cpra_router, prefix="/api/v1/cpra")
app.include_router(review_router, prefix="/api/v1/review")
app.include_router(copilot_router, prefix="/api/v1/copilot")
app.include_router(knowledge_router, prefix="/api/v1/knowledge")

# 挂载 v0 路由（通用任务管理）
app.include_router(task_router, prefix="/api/v0/tasks")
```

### 任务流转怎么组织
```
[前端提交] → [FastAPI Router 接收请求] → [Pydantic Schema 校验]
    |
    +-- 同步模式：直接调用 Service.generate() → 阻塞等待 → 返回结果
    |
    +-- 异步模式：
          InMemoryTaskManager.submit(runner) → 返回 task_id (CREATED)
              → 前端轮询 GET /api/v0/tasks/{id}
                  → 子线程执行 runner() → 状态变为 RUNNING → COMPLETED
                      → 前端获取 result（含 report_path 等）
```

### 输入、处理、输出怎么串联
1. **输入层**：FastAPI Router 接收请求，Pydantic Schema 校验字段类型、必填项、枚举值、数值范围
2. **服务层**：各模块 Service 类编排业务逻辑
   - 文件解析 → ProfileExtractor
   - 路径验证 → DiagnosisService
   - 法规检索 → RAG Pipeline
   - 内容生成 → LLM ModuleGenerator
   - 一致性检查 → ConsistencyChecker
3. **输出层**：ReportRender 生成 docx/pdf/xlsx/md，写入 `outputs/{module}/`，返回文件路径

### 结果如何回传到前端
- 同步模式：直接返回 Pydantic Response Model（含 output_files dict）
- 异步模式：任务完成时，后端将报告路径写入 TaskSnapshot.result
- 前端通过 `/api/v0/tasks/{id}` 获取完整结果，包含下载链接
- 报告中心聚合所有任务的输出，统一展示为报告列表

## 3. 数据与文档处理思路

### 用户输入如何结构化
- 所有表单输入通过 Pydantic Schema 定义（`backend/schemas/` + `app_streamlit/schemas/`）
- 自动校验字段类型、必填项、枚举值、数值范围（如 pii_count >= 0）
- Schema 前后端共享，确保接口契约一致

### 文档如何上传、解析、审查、生成结果
1. **上传**：`st.file_uploader` 接收文件 → 后端 `python-multipart` 处理
2. **解析**：
   - PDF → `pypdf.PdfReader` 逐页提取文本（不支持扫描件 OCR）
   - DOCX → `docx.Document` 提取段落文本 + 表格单元格值
   - 其他格式（txt/md/json/csv）→ 直接读取
3. **分段**：`ClauseSegmenter` 按中文条款编号正则（`第一条`、`1.`、`一、`）切分
4. **分类**：`ClauseClassifier` 加载 `review_rulebook.json`，统计关键词命中数，分配 6 类 ClauseType
5. **审查**：`ClauseReviewer` 采用 LLM 优先 + 规则兜底策略，识别缺失条款与模糊语言
6. **聚合**：`ReviewAggregator` 去重、排序、评级、生成整改行动清单
7. **输出**：`ReviewReportRenderer` 生成 .docx 审查报告，存入 `storage/reports/review/{task_id}/`

### 报告如何预览与导出
- 后端使用 `python-docx` 生成 .docx，`reportlab` 生成 .pdf，手搓 XML 生成 .xlsx
- 渲染管道在 `backend/common/render/` 中，支持 4 种格式 + ZIP 打包
- 前端通过 Streamlit `st.download_button` 提供下载，或 `st.markdown` 内嵌预览
- 文件名规范：`{公司/项目名}_{报告类型}_草案_{YYYYMMDD}.{ext}`

## 4. AI 能力接入思路

（已在上一节"四-0 公共基础设施层"和"AI 能力接入思路"中详细展开，包含腾讯混元 LLM 接入细节、ModuleGenerator、RAG 引擎架构、后处理等）

**后处理（Postprocess）**：
- `ensure_paragraph_citations(text, citations, max_items=3)`：按段落拆分，为每段追加 `[依据：法规1; 法规2; 法规3]`，最多 3 条引用

---

# 四、系统是怎么实现的（高层次）

## 0. 公共基础设施层（Shared Infrastructure）

所有 11 个模块共享以下基础设施，位于 `backend/common/` 目录下：

### LLM 客户端（`common/llm/`）
- **LLMClient**：封装腾讯混元 API 调用，OpenAI 兼容协议，优雅降级
- **ModuleGenerator**：共享 prompt 引擎，按模块+章节生成内容
- **Postprocess**：段落引用追加、确定性 v0 适配器（测试/LLM 禁用时回退）

### RAG 引擎（`common/rag/`）
- **Embedding 层**：
  - `HashingEmbedder`：离线确定性嵌入，无需外部模型，基于 hashing 的稀疏向量
  - `SemanticEmbedder`：基于 `moka-ai/m3e-base` 模型（768 维，中英双语），强制 CPU 运行
- **向量存储**：
  - `LocalVectorStore`：稀疏向量存储，JSON 文件持久化
  - `DenseVectorStore`：稠密向量存储，JSON 文件持久化
- **检索服务**（`RegulationRAGService`）：
  1. 查询预处理：检测无关查询、法域不匹配、路径不匹配
  2. 查询重写：追加相关法域/路径提示词
  3. 向量搜索：top_k = max(requested, candidate_pool_size)
  4. 分数融合：vector / lexical / hybrid 三种模式
  5. 重排序：`HeuristicReranker` 多维度重新打分（短语加分 + 重叠加分 + 关键词加分 + 元数据加分）
  6. 去重返回
- **外部 API 并行**：本地检索 + 得理 API 同步执行，合并去重

### 渲染引擎（`common/render/`）
- **DOCX**：`render_docx_report()` 新建文档 + `render_docx_template()` 替换模板占位符
- **PDF**：`render_pdf_report()` 基于 reportlab，A4 画布，自动换行分页，CJK 字体支持
- **XLSX**：`render_simple_xlsx()` 直接写入 XML 格式 Excel 文件
- **Markdown**：`render_markdown_report()` / `render_markdown_template()`
- **ZIP 打包**：`bundle_files()` 多文件压缩
- **工具函数**：`safe_filename()` 文件名清洗、`summarize_for_slot()` 文本压缩、`attach_citations()` 引用附加

### 存储与解析（`common/storage/`）
- **FileParser**：统一文档解析器，支持 6 种格式（txt/md/json/csv/pdf/docx）
- PDF → pypdf 逐页提取；DOCX → python-docx 提取段落+表格

### 任务管理（`common/tasks/`）
- **InMemoryTaskManager**：线程池异步执行器（2 worker），内存 dict 存储任务状态
- 状态机：CREATED → RUNNING → COMPLETED / FAILED / CANCELED，支持 RETRYING
- `submit(runner, max_attempts=2)` → 提交任务 → `_execute()` 在子线程执行
- 结果自动 Pydantic `.model_dump()` 序列化
- **限制**：纯内存存储，进程重启后丢失（生产环境需迁移至 Redis/Celery）

---

# 五、腾讯工具 / AI 工具 / 平台能力如何协同

## 1. 在设计阶段用了哪些工具

| 工具/平台 | 使用场景 | 作用 |
|-----------|---------|------|
| 腾讯混元大模型（Hunyuan） | 方案调研与可行性分析 | 快速了解跨境数据合规领域的法规框架、技术要求与行业最佳实践 |
| AI 辅助文档工具 | 需求文档梳理 | 辅助整理竞赛需求、功能列表、模块划分草案 |
| 腾讯文档/在线协作 | 团队方案讨论 | 多人同时编辑需求文档、功能清单、任务分工 |

## 2. 在开发阶段用了哪些工具

| 工具/平台 | 使用场景 | 作用 |
|-----------|---------|------|
| AI 代码辅助工具 | 后端 API 搭建 | 辅助生成 FastAPI Router、Pydantic Schema、Service 层骨架代码 |
| AI 代码辅助工具 | 前端页面搭建 | 辅助生成 Streamlit 页面模板、表单组件、样式布局 |
| AI 代码辅助工具 | 问题排查 | 解析报错日志、分析依赖冲突、调试异步任务逻辑 |
| 腾讯混元大模型（Hunyuan） | 核心 AI 能力接入 | 作为后端 LLM 引擎，通过 OpenAI 兼容接口（`hunyuan-turbos-latest`）提供报告生成、审查建议、摘要等服务 |
| GitHub Copilot / 同类工具 | 日常编码 | 代码补全、函数命名、注释生成、单元测试编写 |

## 3. 在测试阶段用了哪些工具

| 工具/平台 | 使用场景 | 作用 |
|-----------|---------|------|
| AI 辅助测试 | 功能验证 | 对比不同 LLM 参数下的报告质量，辅助调整 prompt 与 temperature |
| AI 辅助测试 | 结果对比 | 将 AI 生成的审查结果与人工审查结果对比，评估准确率 |
| pytest | 自动化测试 | 单元测试 + 集成测试，保证核心接口稳定性 |
| FastAPI 自动文档 | 接口联调 | 通过 `/docs` 交互式 Swagger 页面快速调试 API |

## 4. 在内容生成与知识处理阶段用了哪些工具

| 工具/平台 | 使用场景 | 作用 |
|-----------|---------|------|
| 腾讯混元 LLM | 报告草案生成 | 按章节模板，基于 RAG 检索到的法规条款生成合规报告 |
| 腾讯混元 LLM | 审查建议生成 | 对识别出的缺失条款自动生成整改建议 |
| 腾讯混元 LLM | 法规摘要 | 对长篇幅法规条文生成结构化摘要 |
| RAG 自研引擎 | 知识检索 | 自建 embedding + vector store + retriever + reranker 管道，从 15 部法规 + 7 个案例中精准检索 |
| 得理（DeliLegal）API | 外部法规检索（可选） | 接入第三方法律法规数据源，增强检索覆盖面 |

## 5. 腾讯工具/平台在其中承担了什么角色

### 模型能力
- **腾讯混元大模型**：作为核心 LLM 引擎，承担报告生成、审查建议、摘要总结等所有需要自然语言生成的环节
- 接入方式：通过 OpenAI 兼容客户端（`backend/common/llm/client.py`）， endpoint 为 `api.hunyuan.cloud.tencent.com/v1`

### 开发平台能力
- **腾讯云开发环境**（如使用）：提供云端代码托管与调试环境
- **API 网关**（如使用）：提供后端服务的公网暴露与鉴权

### 工作流/智能体能力
- **RAG + LLM 混合工作流**：不是简单调用大模型，而是构建了"规则诊断 → RAG 检索 → LLM 生成 → 一致性检查"的完整 pipeline
- 每个环节可独立替换和升级（如替换 embedding 模型、reranker 算法、LLM 版本）

### 知识问答/内容处理能力
- **Copilot 接口**：基于 LLM 实现的自然语言问答助手，为用户提供合规咨询
- **知识中心**：结合 RAG 检索结果，提供法规条款的原文展示与引用溯源

---

# 六、项目实施过程中的协同与分工

## 1. 需求分析是谁负责

- **核心成员共同完成**：梳理竞赛要求、分析目标用户场景、确定功能边界
- **法规研究**：团队成员分工研读中国、欧盟、美国相关法规，提取关键合规要求
- **决策树构建**：基于法规条文，共同设计 `decision_tree.json` 的规则结构

## 2. 页面设计和前端实现怎么推进

- **Streamlit 主前端**：由熟悉 Python 的成员快速搭建，适合竞赛演示场景
- **React 备前端**：由前端经验较丰富的成员搭建，使用 TypeScript + Vite + TailwindCSS，面向生产环境
- **迭代方式**：先做最小可用版本（MVP），再根据测试结果和导师反馈逐步完善表单字段与页面交互

## 3. 后端和 AI 能力如何协同

- **后端开发**：FastAPI 路由搭建、Service 层编排、异步任务管理、数据库模型设计
- **AI 能力接入**：
  - LLM 客户端封装（`common/llm/client.py`）：统一接口，屏蔽不同模型的差异
  - RAG 引擎开发（`common/rag/`）：embedding、vector store、retriever、reranker 全链路
  - Prompt 工程（`ai_engine/prompts/`）：各模块独立 prompt 模板，针对场景调优
- **协同方式**：AI 能力作为 Service 层的一个依赖注入，通过接口解耦，方便替换模型或调整参数

## 4. 测试和结果修正怎么做

- **单元测试**：对核心函数（决策树推理、条款分类、报告渲染）编写 pytest 用例
- **集成测试**：端到端测试完整流程（提交任务 → 生成报告 → 下载报告）
- **质量校验**：生成报告后运行一致性检查，自动发现路径类型不匹配、附件角色错误等问题
- **人工复核**：团队成员对生成的报告进行抽查，对比人工起草版本，修正不合理内容

## 5. 团队内部如何迭代和分工

| 角色 | 主要职责 |
|------|---------|
| 项目负责人 | 整体进度把控、需求确认、竞赛材料准备 |
| 后端开发 | FastAPI 接口、Service 编排、任务管理、数据库 |
| 前端开发 | Streamlit 页面、React 组件、API 联调 |
| AI/算法 | RAG 引擎、LLM 接入、Prompt 调优、质量评估 |
| 法规研究 | 法规梳理、决策树规则设计、测试用例编写 |
| 测试与文档 | 测试执行、文档编写、竞赛材料整理 |

- **迭代节奏**：按模块推进，每完成一个模块即进行集成测试与演示验证
- **沟通方式**：线上会议 + 即时通讯工具 + 在线文档协作

---

# 七、当前实现成果

## 1. 已完成的核心功能

| 模块 | 状态 | 说明 |
|------|------|------|
| 合规路径诊断（CN） | ✅ 已完成 | 决策树推理 + 风险评分 + 路径推荐 |
| 安全评估报告生成（CN） | ✅ 已完成 | RAG 检索 + LLM 生成 + 一致性检查 + docx 导出 |
| PIPIA 报告生成（CN） | ✅ 已完成 | 个人信息保护影响评估报告 |
| 文档审查（CN） | ✅ 已完成 | PDF/DOCX 解析 + 条款分类 + 缺失项识别 + 审查报告 |
| SCC 审查（EU） | ✅ 已完成 | 标准合同条款合规审查 |
| BCR 审查（EU） | ✅ 已完成 | 约束性企业规则审查 |
| DPIA 草案（EU） | ✅ 已完成 | 数据保护影响评估草案 |
| TIA 草案（EU） | ✅ 已完成 | 传输影响评估草案 |
| 中美数据流动（US） | ✅ 已完成 | 风险项分析 + xlsx 导出 |
| CPRA 全景图（US） | ✅ 已完成 | 加州消费者隐私法合规差距分析 |
| 报告中心 | ✅ 已完成 | 统一查看、下载所有报告 |
| 知识中心 | ✅ 已完成 | 法规浏览 + 引用溯源 |
| Copilot | ✅ 已完成 | AI 合规助手 |

## 2. 可演示的核心流程

1. **CN 场景完整流程**：选择法域 → 填写场景 → 提交诊断任务 → 获取路径推荐 → 提交安全评估任务 → 下载报告 → 验证引用
2. **文档审查流程**：上传合同/隐私政策 → 自动解析与审查 → 获取审查报告（标注缺失条款 + 整改建议）
3. **EU 场景演示**：选择欧盟法域 → 提交 SCC 审查或 DPIA 草案任务 → 下载报告
4. **知识中心验证**：查看报告引用的法规条款原文，验证可追溯性

## 3. 当前能生成/输出哪些结果

| 输出类型 | 格式 | 说明 |
|---------|------|------|
| 安全评估报告 | docx / pdf | 完整的《数据出境安全评估报告》 |
| PIPIA 报告 | docx / pdf | 个人信息保护影响评估报告 |
| 审查报告 | docx / pdf | 文档审查结果，标注缺失条款 |
| 风险清单 | xlsx | 中美数据流动风险项 Excel 表格 |
| DPIA / TIA 草案 | docx / pdf | 欧盟场景评估草案 |
| CPRA 差距分析 | docx / pdf | 加州隐私法合规差距报告 |

## 4. 哪些模块已经跑通

- **CN 场景全链路**：诊断 → 评估 → 报告 → 审查 → 下载，完整跑通
- **EU 场景全链路**：SCC/BCR 审查 + DPIA/TIA 草案生成，完整跑通
- **US 场景**：中美数据流动 + CPRA 分析，完整跑通
- **公共能力**：报告中心、知识中心、Copilot 均可正常使用

## 5. 哪些地方还在优化中

- **RAG 检索精度**：部分复杂场景下检索结果不够精准，正在优化 reranker 策略
- **报告质量调优**：部分章节生成内容的专业度仍需提升，正在迭代 prompt 模板
- **多法域统一 UI**：React 前端仍在完善中，计划逐步替代 Streamlit 作为生产前端
- **外部 API 接入**：得理（DeliLegal）API 为可选接入，正在评估是否作为默认配置

---

# 八、项目亮点与创新点

## 1. 功能组织上的亮点

- **多法域统一平台**：一个系统覆盖中国、欧盟、美国三大法域的合规需求，而非单点工具
- **诊断 → 生成 → 审查闭环**：不是孤立的功能堆砌，而是形成完整的合规工作流
- **报告中心 + 知识中心联动**：生成结果可回溯验证，增强可信度

## 2. AI 与业务场景结合上的亮点

- **"规则先行 + RAG + LLM"三层架构**：
  - 第一层：决策树规则推理，确定合规路径，不依赖大模型
  - 第二层：RAG 从法规库精准检索条款，为生成提供依据
  - 第三层：LLM 基于证据生成报告，受限于检索结果，不自由发挥
- **可解释、可追溯**：每段生成内容标注引用条款，用户可在知识中心验证原文，解决了"AI 黑盒"问题

## 3. 工作流/智能体/知识库设计上的亮点

- **自研 RAG 引擎**：不依赖第三方向量数据库，自建 embedding → vector store → retriever → reranker 全链路
  - 支持 hashing 嵌入模型和 `moka-ai/m3e-base` 可选切换
  - 知识库包含 15 部法规 + 7 个典型案例，结构化存储为 JSONL
- **异步任务管理**：长耗时任务（报告生成）采用异步执行 + 前端轮询，避免阻塞
- **一致性检查门**：生成报告后自动校验路径类型、附件角色、字段约束，不通过则标记问题

## 4. 结果呈现与可追溯性上的亮点

- **多格式导出**：docx（可编辑）、pdf（正式提交）、xlsx（风险清单）、zip（打包下载）
- **引用溯源**：报告中的每条结论都关联具体法规条款，点击可跳转至知识中心查看原文
- **审查报告可视化**：缺失条款以表格形式呈现，标注位置、类型、严重程度、整改建议

## 5. 相比传统方式提升了什么

| 维度 | 传统方式 | 本系统 |
|------|---------|--------|
| 路径判断 | 律师经验依赖 | 决策树自动推理 + 风险评分 |
| 报告起草 | 数天到数周 | 分钟级生成 |
| 法规检索 | 手动查找、易遗漏 | RAG 精准检索、附带引用 |
| 文档审查 | 人工逐条对照 | 自动解析 + 条款分类 + 缺失识别 |
| 可解释性 | 人工报告可解释 | AI 生成内容可溯源验证 |

---

# 九、实际效果与应用价值

## 1. 对用户来说解决了什么问题

- **降低合规门槛**：中小企业无需聘请专业合规律师即可完成初步诊断与报告起草
- **节省时间成本**：从"数天收集法规 + 起草报告"缩短到"填写表单 + 分钟级生成"
- **减少遗漏风险**：自动条款审查覆盖全部合规要点，避免人工检查疏漏
- **增强决策信心**：每条建议都附带法规依据，用户可自主判断是否采纳

## 2. 对流程效率提升了什么

- **合规路径判断**：从"查阅多部法规 + 经验判断" → "填写表单 → 自动推荐"
- **报告生成**：从"人工收集法规 + 逐章起草" → "RAG 检索 + LLM 一键生成"
- **文档审查**：从"人工逐条对照检查清单" → "自动解析 + 条款分类 + 缺失识别"
- **多法域切换**：从"不同地区需不同团队" → "统一平台选择法域即可"

## 3. 对报告生成、材料审查、风险识别有什么帮助

- **报告生成**：生成结构规范、内容完整的合规报告草案，人工只需审核与微调
- **材料审查**：自动识别合同/隐私政策中的缺失条款，标注位置与整改建议
- **风险识别**：基于风险评分模型，自动标记高风险场景并给出应对建议

## 4. 在真实业务场景中的潜在价值

- **中小企业出海**：快速完成跨境数据合规自查，降低法律成本
- **律师事务所**：作为辅助工具提升服务效率，减少重复性工作
- **大型企业合规部门**：作为日常自查工具，及时发现合规风险
- **监管机构**：作为合规培训与案例演示工具

## 5. 后续扩展空间

- **更多法域**：可扩展至东南亚、日韩、澳洲等地区
- **更多场景**：数据入境、数据本地化、跨境数据传输监控等
- **与企业系统集成**：通过 API 与企业 OA/ERP/法务系统对接
- **多语言支持**：英文、日文等多语言界面与报告输出
- **SaaS 化部署**：云端多租户服务，支持中小企业订阅使用

---

# 十、总结与后续规划

## 1. 当前阶段的完成情况总结

- **已完成**：覆盖中国、欧盟、美国三大法域的 11 个核心功能模块，完整的"诊断 → 生成 → 审查 → 报告"工作流，RAG 增强 + LLM 驱动的 AI 能力，双前端架构（Streamlit + React）
- **技术栈**：FastAPI + Pydantic + Streamlit + React + 腾讯混元 LLM + 自研 RAG 引擎
- **知识库**：15 部法规 + 7 个典型案例，结构化存储，支持精准检索与引用溯源
- **输出能力**：支持 docx、pdf、xlsx、zip 多格式导出，报告内容可回溯验证

## 2. 还存在的问题

- **RAG 检索精度**：部分复杂场景下，检索结果的相关性不够理想，需优化 embedding 和 reranker 策略
- **生成内容的专业度**：部分章节（如风险评估、安全措施）的生成内容深度不够，需要更精细的 prompt 工程和知识库扩充
- **系统稳定性**：异步任务在极端情况下可能出现状态异常，需加强容错与重试机制
- **外部依赖**：腾讯混元 LLM 为外部 API 调用，网络波动可能影响服务可用性

## 3. 下一步准备继续完善什么

- **优化 RAG 管道**：引入更先进的 reranker 模型，提升检索精准度
- **扩充知识库**：增加更多法规与典型案例，覆盖更多细分场景
- **完善 React 前端**：逐步替代 Streamlit，提供生产级用户界面
- **增强审查能力**：引入更细粒度的条款分类模型，提升审查准确率
- **性能优化**：优化异步任务调度、报告渲染管道，缩短生成时间

## 4. 后续如何从演示版走向更完整版本

- **生产环境部署**：从本地 SQLite 迁移至 PostgreSQL，引入 Redis 缓存与 Celery 任务队列
- **用户与权限管理**：增加注册/登录、角色权限、多租户隔离
- **计费与用量管理**：按调用次数或报告数量计费，支持企业订阅
- **监控与日志**：引入 APM 监控、错误追踪、性能指标采集
- **安全加固**：数据加密、API 限流、防注入攻击、审计日志
- **合规认证**：争取通过第三方安全与合规认证，增强产品可信度
