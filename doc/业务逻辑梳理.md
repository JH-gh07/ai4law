# 业务逻辑梳理


**1\) 入口与任务形态（同步/异步）**

- 同步：`POST /assessment/generate` → 直接跑完整流程并返回 `AssessmentResult`（`backend/modules/assessment/router\.py:26`）。

- 异步：`POST /assessment/generate\_async` → 先做路径校验，再把完整流程丢进 `InMemoryTaskManager` 后台执行，用户用 `GET /assessment/tasks/\{task\_id\}` 轮询（`backend/modules/assessment/router\.py:47`，`backend/modules/assessment/service\.py:95`）。

    

**2\) 第一个关键判断：是否“允许生成安全评估报告”（路径校验）**

- service 一开始会先调用 `DiagnosisService\(\)\.evaluate\(\.\.\.\)` 用诊断决策树给出 `recommended\_path` 和 `rationale`（`backend/modules/assessment/service\.py:46`，`backend/modules/diagnosis/service\.py:31`）。

- 然后 `\_validate\_path\(\)` 做硬判断：  

    - 若 `recommended\_path == \&\#34;security\_assessment\&\#34;`：通过（`backend/modules/assessment/service\.py:152`）。  

    - 否则：

        - `force\_override\_path=false`（默认）→ **直接抛错**，API 返回 400（`backend/modules/assessment/service\.py:155`）。  

        - `force\_override\_path=true` → 不阻断，但会返回一个 `warning`，后面会被写进结果的 `consistency\_issues` 和报告结论里（`backend/modules/assessment/service\.py:154`，`backend/modules/assessment/report\_renderer\.py:64`）。

            

这一步的“逻辑原理”是：先用确定性的诊断模块判路由；assessment 只服务于“安全评估路径”，防止用户在不满足条件时误生成“安全评估报告”。



**3\) 企业画像抽取（把输入变成可控上下文）**

- `ProfileExtractor\.extract\(\)`：把表单字段原样落到 `CompanyProfile`，并对 `uploaded\_files` 做一次文本解析，只取每个文件前 160 字做 `extracted\_notes`（失败则记 `\[parse skipped\]`）（`backend/modules/assessment/profile\_extractor\.py:9`）。

- 这里没有 LLM 判断；属于“把输入结构化 \+ 可追溯摘要”，给后续检索/写作当上下文。

    

**4\) 法规检索（RAG）：把“企业画像”变成 TopK 条文命中**

- `AssessmentRetriever\.search\(\)` 拼一个检索 query（行业 \+ 出境目的 \+ 接收国家 \+ CIIO/non\-CIIO \+ important data/personal information），然后调用通用 `retrieve\_regulations\(\)` 拿 8 条（默认）法规命中（`backend/modules/assessment/retriever\.py:16`）。

- 这一步的“判断逻辑”主要在 RAG 内部：可能会做 query rewrite、过滤、打分、重排、低命中时外部补检索等（`backend/common/rag/retriever\.py:337`）。

但注意：当前 `AssessmentRetriever\.search\(\)` **没有传 ****`path=\&\#34;assessment\&\#34;`**，所以你在 RAG 里为 `PATH\_STRONG\_HINTS\[\&\#34;assessment\&\#34;\]` 做的“强路径约束/拦截”在本链路里不会生效（`backend/common/rag/retriever\.py:108`）。



**5\) 第二个关键判断：总体风险等级（用于章节写作口径 \+ 最终一致性校验）**

- 风险等级由 `risk\_level\(\)` 计算：  

    - `is\_ciio` 或 `contains\_important\_data` 或 `pii\_count\&gt;=1\_000\_000` 或 `spi\_count\&gt;=10\_000` → `HIGH`  

    - 否则 `pii\_count\&gt;=100\_000` → `MEDIUM`  

    - 否则 `LOW`（`backend/common/risk/scoring\.py:1`）。

- 这个等级会写进章节生成的上下文块里（让 LLM 用同一口径写作）（`backend/modules/assessment/chapter\_generator\.py:99`）。

    

**6\) 章节生成（LLM 受控写作 \+ 引用约束）**

- `AssessmentChapterGenerator\.generate\(\)` 会：

    - 用企业画像 \+ 前 5 条法规 snippet 组装 `context\_block`（`backend/modules/assessment/chapter\_generator\.py:75`）。

    - 按 `\_CHAPTER\_PROMPTS` 逐章调用 `\_generate\_chapter\(\)`（`backend/modules/assessment/chapter\_generator\.py:21`）。

- 这里的“判断/分支”是：

    - 若 LLM 配置且启用：走真实生成，并强制“每段末尾至少一个引用”（未满足会后处理补齐）`ensure\_paragraph\_citations\(\)`（`backend/modules/assessment/chapter\_generator\.py:131`）。  

    - 若 LLM 未启用：直接返回“占位内容”（`backend/modules/assessment/chapter\_generator\.py:144`）。

        

**7\) 第三个关键判断：一致性与对齐检查（防幻觉/防逻辑矛盾）**

- `ConsistencyChecker\.check\(\)`：

    - 任意章节没有 citations → 记 issue（`backend/modules/assessment/consistency\_checker\.py:10`）。

    - 若满足“强制安全评估条件”（CIIO/重要数据/PII≥100万）但最后一章 `risk\_level` 不是 `HIGH` → 记 issue（`backend/modules/assessment/consistency\_checker\.py:14`）。

    备注：这里的 `high\_risk` 没包含 `spi\_count\&gt;=10\_000`，和 `risk\_level\(\)` 的 HIGH 条件不完全一致。

- `check\_cn\_alignment\(\)`（文本对齐）：

    - 输入 `is\_ciio=False` 但正文提到 CIIO 且没否定语 → issue

    - 输入 `contains\_important\_data=False` 但正文提到重要数据且没否定语 → issue

    - 行业/接收国出现明显不一致 → issue（`backend/common/quality/alignment\.py:19`）。

- 路径不匹配 warning（如果用户强制 override）也会被塞进 issues（`backend/modules/assessment/service\.py:72`）。

    

**8\) 报告渲染（把章节“落到模板槽位”，并输出 docx/md/zip）**

- `AssessmentReportRenderer\.render\(\)`：

    - 把章节内容映射到 docx/md 模板字段，且会对一些槽位做摘要压缩（比如“合法性基础/安全措施/整改建议/结论”取 1–2 句）并附加引用（`backend/modules/assessment/report\_renderer\.py:42`）。

    - 若有 `path\_warning` 或 `alignment\_warning`：会在结论前加醒目提示（`backend/modules/assessment/report\_renderer\.py:64`）。

    - 输出 `outputs/assessment/\.\.\.docx`、`\.\.\.md`、`\.\.\.zip`（`backend/modules/assessment/report\_renderer\.py:27`）。

        

**9\) 最终结果怎么形成（****`AssessmentResult`****）**

- `AssessmentService\.generate\_report\(\)` 把上述产物汇总成 `AssessmentResult`：  

    - `profile`（结构化画像）  

    - `regulations`（TopK 命中）  

    - `chapters`（逐章内容 \+ risk\_level \+ citations）  

    - `consistency\_issues`（包含一致性、对齐、路径 warning）  

    - `report\_path/output\_files`（docx/md/zip 路径）（`backend/modules/assessment/service\.py:75`，`backend/modules/assessment/schema\.py:46`）。

        

如果你愿意，我可以按你们真实的一个 `AssessmentRequest` 样例，把每一步“输入→输出（关键字段）”用一段最小可读的 trace 形式写出来，对照 `service\.generate\_report\(\)` 的每一行执行顺序讲。





# 详细原理细节：

下面按你最新要求，改成**业务流程细节版**来落实：不从代码讲，而从“企业实际做合规工作时，系统怎样帮助判断、准备、审查、输出”来讲。

---

# AI4Law / DataComply Flow 业务流程细节落实版

## 一、整体业务定位

这个项目不是单纯“生成报告”的工具，而是把企业数据跨境合规工作拆成一套可执行流程：

```Plain Text
明确业务场景
→ 判断适用法域
→ 判断合规路径
→ 收集企业事实和材料
→ 依据法规/指南识别风险
→ 生成报告、合同审查意见或整改清单
→ 区分内部决策用途和外部提交用途
```

原始业务设计本身就是按中国、欧盟、美国三类跨境数据合规场景展开：首页需要介绍中、美、欧等数据跨境框架，并分别提供“中国大陆向境外传输数据”“欧盟向境外传输数据”“美国向境外传输数据”的专项服务入口。

---

# 二、中国数据出境合规业务线

中国业务线的核心是判断企业从中国大陆向境外提供数据时，应当走哪条路径：

```Plain Text
安全评估
个人信息出境标准合同备案
个人信息出境认证
可能豁免或暂不触发申报
```

原始调研文档已经明确，中国框架重点解析数据出境“三条路径”：安全评估、标准合同、个人信息保护认证。

---

## 中国路径诊断：企业到底该走哪条路？

### 1\.1 企业需要提供什么

企业需要先说明以下事实：

### 1\.2 系统依据什么判断

中国路径判断主要依据这些规则：

原始调研文档中写明，2024年《促进和规范数据跨境流动规定》明确了两类应当申报安全评估的条件：一是CIIO向境外提供个人信息或者重要数据；二是非CIIO向境外提供重要数据，或者自当年1月1日起累计向境外提供100万人以上个人信息、1万人以上敏感个人信息。

### 1\.3 具体业务判断流程

#### 第一步：先判断是否直接进入安全评估

系统应按以下顺序判断：

**例子：**

某互联网企业不是CIIO，也不涉及重要数据，但一年内计划向新加坡总部同步120万名用户的注册信息、订单信息。

这时系统应判断：

```Plain Text
普通个人信息人数 ≥ 100万人
→ 触发安全评估
→ 建议准备《数据出境风险自评估报告》
```

#### 第二步：如果不触发安全评估，再判断标准合同或认证

如果企业不属于CIIO，不涉及重要数据，也未达到安全评估门槛，则进入标准合同或认证路径判断。

调研文档中写明，个人信息出境标准合同备案适用于非CIIO，并且自当年1月1日起累计向境外提供10万人以上、不满100万人个人信息，或不满1万人敏感个人信息的情况。

**例子：**

某跨境电商企业向境外客服系统传输30万名用户的订单、联系方式，不涉及敏感个人信息，也不涉及重要数据。

系统应判断：

```Plain Text
非CIIO
不涉及重要数据
普通个人信息 30万
未达到100万安全评估门槛
达到标准合同备案区间
→ 建议走个人信息出境标准合同备案
→ 同时需要准备PIPIA
```

#### 第三步：如果企业事实不清楚，不直接下绝对结论

如果企业没有说清楚是否涉及重要数据、是否属于CIIO、个人信息数量是否准确，系统不能直接“猜一个路径”。正确表达应该是：

```Plain Text
当前结论：暂无法最终确定，需要补充关键事实。

在以下情形下，可能触发安全评估：
1. 如果企业被认定为CIIO；
2. 如果出境数据属于重要数据；
3. 如果普通个人信息累计达到100万人以上；
4. 如果敏感个人信息累计达到1万人以上。

在以下情形下，可能适用标准合同或认证：
1. 企业不属于CIIO；
2. 不涉及重要数据；
3. 普通个人信息未达到100万人；
4. 敏感个人信息未达到1万人；
5. 但仍属于个人信息出境活动。

建议企业补充：
- 行业主管部门是否认定为CIIO；
- 数据是否落入重要数据目录；
- 近一年实际出境人数；
- 是否包含敏感个人信息。
```

这不是“AI猜测”，而是**基于法规条件的分情景判断**。

### 1\.4 输出是什么、给谁用

这份报告主要是**内部决策材料**，不是直接上交监管的正式申报材料。

---

## 中国安全评估报告生成：生成《数据出境风险自评估报告》

### 2\.1 企业需要提供什么

企业需要提供的不是“随便写几句话”，而是一组能支撑安全评估报告的材料：

调研文档中列明，安全评估申报需要上传统一社会信用代码证件、法定代表人身份证件、经办人身份证件、授权委托书、申报表、数据出境相关合同或者其他法律文件，以及数据出境风险自评估报告扫描件。

### 2\.2 业务处理流程

#### 第一步：确认是否真的应当生成安全评估报告

系统应先复核路径诊断结论：

```Plain Text
如果已经触发安全评估条件
→ 进入安全评估报告生成

如果没有触发安全评估条件
→ 不建议直接生成正式安全评估报告
→ 可生成“安全评估路径适用性说明”
```

这样做的原因是：安全评估报告不是所有数据出境企业都需要准备。如果企业实际只需要标准合同备案，却生成安全评估报告，会导致业务路径混乱。

#### 第二步：检查“出境必要性”

系统要判断企业有没有说清楚：

原始调研中明确提到，系统应自动检查报告中对“出境必要性”的论证是否充分，是否符合《个人信息保护法》第三十八条“确需向境外提供”的要求。

**例子：**

企业填写：“为了全球业务协同，需要将用户数据传给境外总部。”

系统不能直接认为充分。应该提示：

```Plain Text
该表述过于概括。
建议补充：
1. 境外总部具体承担什么业务功能；
2. 为什么境内团队无法完成；
3. 出境哪些字段；
4. 每类字段与业务目的的对应关系；
5. 是否采取最小化出境。
```

#### 第三步：检查“告知和单独同意”

系统要判断企业是否已经向个人说明：

原始调研中也明确要求系统提示企业是否履行了《个人信息保护法》第三十九条规定的“告知并取得单独同意”义务。

**例子：**

合同或隐私政策中只写：

```Plain Text
我们可能会将您的信息提供给境外合作伙伴。
```

系统应识别为高风险表述，因为它没有明确境外接收方、联系方式、处理目的、处理方式、个人信息种类和行权方式。建议改成：

```Plain Text
我们将为完成跨境订单履约，将您的姓名、联系方式、订单编号提供给位于新加坡的XX公司。该公司仅用于订单客服和物流协同，不得用于其他目的。您可通过XX邮箱行使查询、更正、删除、撤回同意等权利。
```

#### 第四步：检查“接收方安全能力和合同义务”

系统要判断境外接收方是否具备保护能力：

**例子：**

合同只写：

```Plain Text
乙方应采取必要措施保护数据安全。
```

系统应指出：该条款过于抽象。建议补充：

```Plain Text
乙方应至少采取传输加密、存储访问控制、操作日志留存、权限分级管理、定期安全评估等措施；发生数据泄露、篡改、丢失或非法访问时，应在24小时内通知甲方，并配合采取补救措施。
```

#### 第五步：形成风险结论

安全评估报告不是简单“能不能出境”，而是要给出风险识别和控制措施。

### 2\.3 输出文书和用途

这份文书是**内部准备 \+ 外部申报双重用途**。正式提交前仍需要企业法务或外部律师复核。

---

## 中国标准合同备案：检查合同和备案材料是否够用

### 3\.1 企业需要提供什么

标准合同备案指南要求提交个人信息处理者基本情况、法定代表人信息、经办人信息、承诺书、个人信息出境场景、标准合同、个人信息保护影响评估报告等材料。

### 3\.2 业务判断流程

#### 第一步：判断是否适合走标准合同备案

#### 第二步：检查标准合同核心条款

系统需要围绕标准合同文本检查：

### 3\.3 输出文书和用途

这类输出主要是**内部整改 \+ 备案准备**，标准合同和PIPIA本身具有外部提交属性。

---

## 中国PIPIA：个人信息保护影响评估

### 4\.1 企业需要提供什么

### 4\.2 业务判断流程

PIPIA的重点不是“国家安全和公共利益”口径，而是围绕个人权益影响展开。你们现有实现中也把PIPIA定位为《个人信息保护影响评估》类报告，重点是个人信息权益影响和风险控制措施，不同于安全评估报告的网信申报口径。

系统应当判断：

### 4\.3 输出文书和用途

这份文书具有**内部评估 \+ 外部配套提交**属性。

---

# 三、欧盟GDPR合规业务线



```Plain Text
欧盟GDPR合规业务线包含三类业务任务：
1. DPIA：处理活动风险评估
2. TIA：跨境传输影响评估
3. BCR：集团内部跨境规则审查

**DPIA**：评估“我做的事情本身”风险 → 只管处理活动 
**TIA**：评估“我传数据去哪里”风险 → 只管跨境传输有效性 
**BCR/SCC**：提供“合法传输手段/规则” → SCC 面向合同约束外部，BCR 面向集团内部长期规则 
 

欧盟个人数据出境决策流程：
├─ 目的地有充分性认定 → 可以直接传
├─ 目的地无充分性认定：
│    ├─ 使用 SCC → 做 TIA（检查第三国风险）
│    ├─ 使用 BCR → 做 TIA（检查第三国风险）
│    └─ 没有 SCC/BCR，但满足克减条件 → 做 TIA（特殊例外评估）
├─ 处理活动高风险 → 做 DPIA
```

GDPR第五章规定了向第三国传输的合规路径，包括充分性认定、适当保障措施中的SCC、BCR，以及特定情况下的克减；同时，GDPR第35条要求对可能产生高风险的处理活动进行DPIA。







---

## 欧盟DPIA：处理活动是否会对个人造成高风险？

### 1\.1 企业需要提供什么

### 1\.2 业务判断流程

#### 第一步：识别是否需要DPIA

GDPR Article 35要求，当处理活动特别是使用新技术，并结合处理性质、范围、背景和目的，可能对自然人权利和自由产生高风险时，控制者应当在处理前进行DPIA。文档中列举了Article 35\.3要求DPIA的典型情况：系统性和广泛的个人评估、基于自动化处理的画像或决策、大规模处理特殊类别数据、大规模系统性监控公共区域等。

**例子：**

某保险公司用AI模型分析客户健康数据、消费记录、历史理赔记录，决定保费价格。

系统应判断：

```Plain Text
涉及健康数据
涉及自动化评估
可能显著影响个人权益
→ 需要DPIA
```

#### 第二步：评估必要性和比例性

系统应检查：

#### 第三步：评估对个人权利的风险

#### 第四步：判断控制措施是否足够

### 1\.3 输出文书和用途

DPIA主要是**内部合规决策材料**，但在监管询问或高风险场景下可作为外部说明材料。

---

## 欧盟TIA：跨境传输到第三国是否安全？

### 2\.1 企业需要提供什么

### 2\.2 业务判断流程

#### 第一步：确定传输工具

GDPR第五章下，常见路径包括充分性认定、SCC、BCR，以及特殊情况下的克减。文档说明，企业依赖GDPR第46\.2条和第46\.3条所列传输工具，如SCC或BCR进行传输时，有义务评估目的地第三国保护水平及是否需要额外保障措施，这就是TIA。

#### 第二步：分析第三国法律和实践风险

系统要判断：

#### 第三步：判断补充措施是否足够

**例子：**

欧盟公司把客户数据传给第三国云服务商。第三国法律允许政府机构在较宽条件下要求服务商提供数据。企业只签了SCC，但没有加密、没有访问限制、没有政府请求通知机制。

系统应判断：

```Plain Text
SCC本身不足以消除第三国法律风险
补充措施不足
→ 高风险
→ 建议暂停传输或补充强加密、访问隔离、政府请求通知机制
```

### 2\.3 输出文书和用途

TIA主要是**内部决策和审计留痕材料**，监管机构询问时可以作为外部说明材料。

---

## 欧盟BCR：集团内部跨境传输规则是否完整？

### 3\.1 企业需要提供什么

### 3\.2 业务判断流程

GDPR Article 47要求BCR具有法律约束力，并明确赋予数据主体可执行的权利。原始调研文档中也列明，BCR至少应明确集团成员结构、数据转移类型、处理目的、数据主体类型、第三国、法律约束力、数据保护原则、主体权利、责任、投诉、审计、变化报告、监管合作、员工培训等内容。

**例子：**

某跨国集团BCR写明“集团成员应保护个人数据”，但没有说明：

- 哪些集团成员受约束；

- 数据主体如何投诉；

- 欧盟实体是否对第三国子公司违规承担责任；

- 如何审计第三国成员。

系统应判断：

```Plain Text
规则缺少可执行机制
责任链不闭环
数据主体权利保障不足
→ BCR高风险缺口
```

### 3\.3 输出文书和用途

BCR文书主要是**集团内部治理材料**，在申请监管认可或对外说明时具有外部呈现属性。

---

# 四、美国数据合规业务线

美国业务线应分成两类，不要混在一起：

```Plain Text
CPRA：面向加州消费者隐私权利和企业隐私治理
EO 14117：面向受关注国家的数据交易和国家安全风险
```

你们调研文档已经说明，美国联邦层面没有像中国、欧盟那样统一的数据跨境法，而是通过政策文件、政府间协定、州隐私法等组合形成监管。

---

## CPRA：企业隐私治理是否存在缺口？

### 1\.1 企业需要提供什么

### 1\.2 业务判断流程

#### 第一步：判断企业是否涉及CPRA义务

系统需要判断企业是否在加州开展业务，是否处理加州消费者个人信息，是否达到CPRA适用门槛。

#### 第二步：检查告知是否完整

CPRA要求企业清晰、提前告知个人信息类别、敏感信息类别、用途、是否出售或共享、保留期限等。调研文档中也写明，企业收集信息时必须清楚告知收集了哪类信息、用途、是否卖出或共享、留存多久，不能隐藏在冗长协议里。

#### 第三步：检查消费者权利机制

原始调研文档中写明，CPRA要求提供“请勿出售或分享我的个人信息”和“限制敏感个人信息处理”的选项，并确保有效。

**例子：**

某网站把用户浏览记录共享给广告平台用于跨站追踪，但页面没有“Do Not Sell or Share My Personal Information”。

系统应判断：

```Plain Text
存在出售/共享个人信息行为
缺少退出机制
→ 高风险缺口
→ 建议补充退出入口、更新隐私政策、调整广告共享流程
```

#### 第四步：检查服务商和第三方管理

调研文档中写明，企业将数据交给第三方、服务商或承包商时，必须签合同，要求对方遵守隐私保护要求，并有监督机制。

### 1\.3 输出文书和用途

CPRA报告主要是**内部治理和管理层决策材料**，也可用于监管问询或第三方审计说明。

---

## EO 14117：是否涉及向受关注国家传输敏感数据？

### 2\.1 企业需要提供什么

### 2\.2 业务判断流程

#### 第一步：判断“传给谁”

调研文档写明，美国14117行政令下，企业需要判断接收方是否为“受关注国家”或“被覆盖的人”，并对接收方做尽职调查，明确最终所有权、控制权及所在地。

#### 第二步：判断“传什么”

调研文档写明，14117重点关注批量敏感个人数据和美国政府相关数据；敏感个人数据包括基因组、生物识别、健康、地理位置、财务、特定个人标识符等。

#### 第三步：形成红黄绿结论

**例子：**

一家美国公司计划将包含精确地理位置数据和健康数据的数据集传给一家受中国公司控制的新加坡数据分析供应商。

系统应判断：

```Plain Text
数据类型：精确地理位置 + 健康数据
接收方：可能受受关注国家控制
交易对象和数据类别同时高风险
→ 至少黄灯，可能红灯
→ 需要补充股权控制调查、数据最小化、禁止明文传输、法律评估
```

### 2\.3 输出文书和用途

这份报告主要是**内部交易决策材料**，在审计、监管问询、客户尽调时也可作为外部说明材料。

---

# 五、文档审查业务线：合同原文如何变成修改建议

文档审查不是单纯生成一篇报告，而是帮企业看清楚：

```Plain Text
原文哪里有问题
为什么有问题
依据什么规定
风险等级是什么
应该怎么改
```

你们实现细节中也明确，review不是单次模板化报告，而是“任务\+文件→条款切分→分类→逐条审查→聚合→渲染”的闭环。

---

## 企业需要提供什么

原始调研文档也要求文档专项审查覆盖数据处理协议、数据出境标准合同、云服务协议、数据安全与保密协议、隐私政策等文件。

---

## 审查业务流程

### 第一步：定位原文

系统先把文件拆成可审查的条款，保留：

```Plain Text
文件名称
章节位置
条款编号
原文内容
```

### 第二步：识别条款类型

系统判断该条款属于哪类问题：

### 第三步：逐条判断是否缺少必要内容

**例子1：告知与同意条款**

原文：

```Plain Text
用户同意我们根据业务需要向境外合作方提供相关信息。
```

系统判断：

风险等级：高风险。

建议：

```Plain Text
建议补充境外接收方名称、联系方式、处理目的、处理方式、个人信息种类、个人行权方式，并设置单独同意机制。
```

**例子2：安全措施条款**

原文：

```Plain Text
乙方应采取合理措施保护数据安全。
```

系统判断：

风险等级：中高风险。

建议修改：

```Plain Text
乙方应采取包括但不限于传输加密、存储访问控制、最小权限管理、操作日志留存、定期安全评估等技术和管理措施。发生数据泄露、篡改、丢失或非法访问时，应在约定期限内通知甲方，并配合采取补救措施。
```

---

## 审查结果展示方式

你希望的展示方式应表达为：

```Plain Text
左侧：原合同文本
右侧：审查意见
绿色：建议新增或修改内容
旁边：风险等级、问题类型、修改理由、法规依据
```

示例：

这类审查结果主要用于**企业内部合同修改、审计留痕、与境外接收方谈判**；在监管问询时，也可作为整改依据。

---

# 六、共性能力：证据链、引用、Agent、输出格式

## RAG证据链应当如何业务化表达

不要只说“系统检索法规”。应表达成：

```Plain Text
企业事实
→ 生成法规检索问题
→ 检索法律、指南、模板、案例
→ 形成候选依据
→ 将候选依据和企业事实匹配
→ 生成初步判断
→ 检查判断是否被依据支持
→ 输出结论、证据、置信度
```

你们当前实现中，不同法域已经有检索约束：中国相关功能使用中国法域，欧盟相关功能使用欧盟法域，美国相关功能使用美国法域；但欧盟和美国现阶段更多使用固定关键词，path级分流还不够细。

应补强为：

---

## 引用呈现方式

引用不应只是段尾一个编号，而应是：

```Plain Text
结论：该企业可能触发安全评估。
依据：
1. 触发条件：普通个人信息达到100万人以上；
2. 法规来源：《促进和规范数据跨境流动规定》；
3. 适用事实：企业预计出境120万人普通个人信息；
4. 证据强度：高。
```

每个关键结论都应该绑定：

---

## Agent应该放在哪里

Agent不是直接替代规则，而是增强几个业务环节：

前提是：业务规则先清楚，Agent只做增强，不做无依据的最终判断。

---

## 输出格式应精简

建议每个业务任务默认输出：

不要每个任务都机械生成 docx、md、pdf、xlsx、zip。输出应按业务用途决定。

---

# 七、最终可以这样对外汇报

可以概括为：

> 本系统按照中国、欧盟、美国三类数据跨境合规场景设计业务流程。
> 中国业务线重点解决“路径怎么选、材料怎么准备、报告怎么生成、合同怎么审查”；欧盟业务线重点解决GDPR下DPIA、TIA、BCR三类合规工件；美国业务线重点解决CPRA消费者隐私治理和EO 14117敏感数据交易风险。
> 每个功能不是简单调用大模型生成文本，而是先收集企业事实，再依据对应法律法规和监管指南进行路径判断、风险识别、材料完整性检查、证据检索与引用验证，最后输出可供内部决策、外部申报或第三方沟通的合规文书。
> 
> 



