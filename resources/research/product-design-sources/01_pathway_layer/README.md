# 路径层法律文件

路径层用于判断某个业务场景是否适用特定合规路径。

当前目标子类：

```text
01_cn_diagnosis/     中国路径诊断
02_us_eo14117/       美国 EO14117 对华数据流动适用性判断
03_eu_pathway_tbd/   欧洲路径层，待设计
```

本层后续应优先补充 `01_RULE` 和 `02_SOURCE` 文件，用于支持规则主判、法规依据校验和一致性复核。
